---
draft: true
---

同学们，大家好，今天我准备分享的是关于 agent memory 的一些论文，这一开始所介绍的是这来自关于 agent memory 的一个综述，我感觉这个综述讲的部分比较综述部分讲的很易懂。我就来给大家介绍一下记忆。什么是 agent memory 记忆？它是一个智能体，实现长程推理、持续进化和复杂交互的一个核心。这个综述主要讲了三个部分，第一是记忆的形态功能与演进，动态记忆由什么承载？二记忆为什么存在？第三个是记忆如何演形成与演进。首先我想首先第一份首先讲一下，想叙述一下什么是记忆，它的定义是什么呢？它指出智能体记忆绝非是普通的数据存储，而是一个原生支持read、 write 交互的可演进状态，它不仅被动地保存了信息，而且更深层次地参与到了后续的交互推理与策略更新中。强调了跨任务的持续性、可写入、可更新以及可被主动调用。
以简单来说，就是他不仅把信息保存到数据库里，而且再把这些信息提取出来，参与之后的交互推理与策略更新，这就是他对智能体记忆的一个定义，大模型内部的记忆来进行了辨析，大模型内部的记忆是更侧重于静态的参数模型或者单次推理中的KV， cache 或者那些隐藏状态。
比如说那些中间状态的那些输出，而智能体记忆它更强调外部的支架和长期可持续的任务状态和 Rag 对比和传统的增检索增强增成生成来说， RAG 它是更面向于静态的知识检索的，比如说就像给那些 AI 塞了一本固定的书，然后它就遇到不会的书，它就去翻它不会的东西，它去翻这本书里，而这个智能体记忆它就像随着每一次的交互而持续生长和动态更新的。
对于至于上下文工程来说，上下文工程往往只关注于单次任务里信息的及时调度和窗口填满，它就只关注于这种及时调度的东西，而智能体记忆则关注智能体生命周期的。长期的认知连续性，这就是智能体记忆的一个定核心概念。接下来讲他又叙述了记忆的形态，记忆到底是由什么来承载的。
目前学术界和工程界有三种主流的记忆载体，一种是 Token 级的记忆， Token 的记忆级别记忆的核心特点就是显示离散且极易编辑，显示性就是它的最大优势，比如我们平常使用的对话形式，什么对话存进去，或者什么自然语言文本块、日常对话记录以及项目数据库这种东西存进去。他在项目库里存一条 JSON 或者结构化文本，这种东西就算就属于 TOKEN 级别的记忆，这种记忆人类一眼就能看懂，而且它增删改都非常的方便，而这也是当前主流的地方。
第二种就是参数化的记忆，参数化的记忆将知识直接固化在模型的神经网络权重中，具有极强的抽象性和泛化能力。它一般通过什么监督、微调、 Lora 或者一些强化学习的方法去把这些模型的参数进行改变，比如说我们只可有时候会用优一些优质的代码规范去对这些模型进行强化或者微调，或者对一些。那些最，比如说数学题一些优质的解题方法去给它进行微调，对它进行参数微调能让它在不依赖外部的提示词的情况下本能地写出符合规范的代码。
第三种是潜空间记忆，这种形式是一种非常机器原生化的路线记忆，以隐藏状态激活值或者高度压缩的 KVKV 形式的存在就是机器能读懂的语言，而并非我们人人能看懂的语言。它的特点极其紧凑具，通常具备极高的计算与 TOKEN 利用效率，但缺点是人类没有办法直接直观地阅读，解释性和可编辑性都非常弱。
这是这三种记忆的优势，以及它们适用的场景。比如 TOKEN 型的记忆，它的特点就是符号化、可寻址以及高度透明，它写入向量库的东西人一眼就能看懂，它很也很容易增删改查，所以它适合的场景，比如说我们经常使用多轮对话这种机器人，什么个性化的推荐系统，或者比如说一些要留痕的东西，比如说一些你就是你必须要严，必须一些什么医疗、法律、金融这种东西，它就不能AI，不能凭它的直觉胡编乱造东西，我们必须它的推理必须依据百分百的，就是透明和可追溯的东西。
所以这叫依赖 TOKEN 级的记忆，这种参数化的记忆。它的特点是隐式抽象，具备强的算法能力，这种东西它就不像 TOKEN 型机器，就是背书从里边去抽抄东西，而是它真正懂得了某种逻辑，它通常能带来更高的性能上限，因为它变成这种模型的参数本能就不需要再去检索之后再去推理。但是它的缺点就是更新极其缓慢，而且还存在严重的灾难性遗忘，它学会了新技能，可能就把之前的参数覆盖掉，就忘了旧的参数了。
它适合的场景就是一些，比如说角色扮演的，你去给它把特定角色的性格、语气和它的这些什么价值观，就给它喂进去，它就以后每次都遵循这个规则，而不需要在提示词里去提示它。或者一些推理密集型的任务，或者需要掌握根本性强、能力强的任务，就比如说让模型掌握一种全新的复杂代码框架，就告诉它这种该怎么写，然后就让把它强化到它的参数里。第三种就是潜空间记忆，就是这种 latent memory，它的特点就是第一是不可读，因为它省略了那种 KV 缓存的形式，或者这种中间层输出的形式，再向那种人类可读的形式的转化，而是直接用机器的这种原生的形式就存了，而且它很。
节省TOKEN，因为它绕开这种 TOKEN 型记忆去当做 prompt 去塞爆它的上下文窗口，而且它还方便多模态的融合，所以它适合的很多场景，比如说多模态的记忆端侧或者边缘的那种，部署的那种需要高压缩比的这种，它也有时候可以用了于这种，比如这种长时期、长时间的这种记忆也是可以的，就参数化记忆，就是直接改，就是它参数化记忆，就是直接改它的TOKEN，就是直接改它的参数，对不对？把它参数进行微调，而潜空间记忆就是。
诶，等一下潜空间记忆上页PPT，有点没太看懂它和参数化记忆的区别是啥呀？
就是我没太懂他这个隐空间什么是什么叫隐空间？
这个 TOKEN 级别的，它就直接存那些，比如说文字，把那些什么对话存进项存进去。而这个潜空间就是它比如说我把中间的 KV 的向量，我直接存 KV 向量进去，而不是存 kv 向量输出的这些字，它就省略了中间这个步骤，或者去存它中间的，比如说它经过某个中间层输出的东西，它直接把某个中间层输入的东西直接存起来。
它就省略的，对，它这样，就它这样可解释性就很差，但是这个东西是机器它自己能看懂的语言，这是它的特点。但是它就很快，因为它不需要经过再转成人能看懂这个语言这一步骤，对吧？在至于 Token 的记忆，它是现在的绝对的主流，它的结构演进也很，它的应用是最广的，它的组织方式的也进行了相对的演进，它从初始的一维，它逐渐进化到了三维，它结构化的能力也得到了持续性增强，一开始是这种一维的平坦结构。
就是模型的中间输出，或者说是 KV CACHE。
OK。
比如说就这种全量对话的这种列表，就是你一句我一句这种对话形式，或者什么经验数据池，什么切分的这种块之类的。它的优点就是开发和写入很简单，但它缺点就是检索的粒度很粗。比如说你把过去一年的聊天记录不做任何分类，就全堆到一个记事本里，然后你去找它，找某个东西的时候它就很难去找，至于至于演化它就图跨越到了这种二维的平面结构，比如说最近很火的什么知识图谱结构，什么树状结构，什么关系表结构，它就能显示的表示中不同的对象和概念之间的拓扑关系。
这种二维结构，比如知识图谱，它就能建立节点边之间的联系，就可以通。得到两个实体之间它们有什么关系的存在？通过这个节点和关系的存在，它在检索的时候就能顺藤摸瓜去通过节点关系节点，然后去那种进行那种长跳的推理，在这种长跳推理的方面它就表现得很好。
这种 3D 的 3D 分层结构目前也有一部分，它把这种 2D 结构再进行了一个上升，它比如它做了多层的摘要，多层的图，或者这种金字塔结构，它从底层的这种原始稀疏数据向高层这种抽象概念进行了垂直导航这种跨层级检索，比如说底部是 100 层的这种具体 100 次这种具体的报错，然后向上提炼成这种10。
十次 10 个的这种核心模块，再往下提炼成一条的这种总体的这种系统部署，就大概是这种概念，就是这种结构化的升级，它就使它这种孤立的文本去把它们之间的关联，而进行把它们之间的关联得到了那种更好的提炼，可以更好地进行这种多跳推理，或者进行的这种更高层次的这种抽象的这种能力，这是这种 TOKEN 级别记忆的演变。
接下来它这个综述还回答一个问题，智能体为什。什么需要记忆？这个就是为什么智能体需要记忆呢？这也这我们在使用的时候也很能很明显的体现到智能体它分三，一般分成三种记忆，一种事实记忆，一种经验记忆，一种是工作记忆。事实记忆就主要用来保存用户的偏好，什么身份、社交关系，或者什么当天所处的环境状态发生了什么事。它核心回答的问题就是那种它系统是知道什么东西，比如说你告诉他我我在哪存放了个什么东西，它会牢牢记住如说当前的工作路径，什么你配置服务器用的什么系统，这都属于这种不可动摇的事实。
第二种先说这种工作记忆，这工作记忆像什么呢？比如像智能体在执行当前多步复杂任务的时候，手里边会捏一个临时的这种记忆空间。比如说你这个任务很长，然后他可能会，他可能这个记忆里他要回答的问题是系统当前正在处理什么？比如说他在做一种复杂的文献系统性回顾的时候，他可能会在这个工作记忆里临时写下他第一步已经干了什么，比如说在哪搜了什么关键词，什么剔除了多少文献，他现在正在这种进行。
第二步，哎，这就是他，然后他就会记在这个临时的工作记忆里。而第三种就是第三种，也是现在做的比，也是现在推进的比较前沿一点的，就是这种经验记忆。就是什么呢？就是他负责保存任务执行过程中的成功路径、失败教训和抽象出来的策略模式。他核心回答的问题就是系统在通过过去的经历回学到了什么，哎，比如说他之前，你之前让他帮你做写一个代码，然后用来干啥？他但他写的那个模式很失败，没有达到你想要的效果，然后你告诉他你写的不对应该怎么写，然后他就会把这个东西给记录下来，然后他又告诉他就会自己记录下来。哎，我以后不能这么写，而是要这么写才是正确的。然后他就通过这样他就能把这个 AI 变成了能在实际复杂环境中自我迭代的有了这种能力，这是这种经验记忆。然后又，然后。
到看完了功能，我们再来看一下记忆在智能体内部到底是如何形成演进会被重新调用的智能体它的这个记忆肯定是经过这三个步骤的，因为你形成肯定一，就像一开始一样，你不能把所有的院士对话都存进去，很里边有很多冗杂的无用的信息，对吧？比如说你演进部分，你演进的时候，你的这个记忆之间肯定可能会有所什么碰撞、冲突，或者有些记忆它可能会过时，对不对？这是你演进部分要做的事，检索部分，就比如说你现在要。
去问他一个东西，他肯定要从他记忆库里进行查询，进行检索，然后去告诉你哪个是最有用的部分，这个形成部分就是像刚才所说，智能体不可能把每句废话都存下来，它必须从原始的、海量的原始交互数据中提出真正高价值的信息，这就需要往用大模型进行语义层面的摘要啊。
什么知识的蒸馏或者参数化，把经验注入体内，这是形成部分所进所做的事，演进阶段它就随着时间推移，它新旧的记忆肯定会产生碰撞。系统主要是把负责新提取的记忆单元融进现有的记忆库中，而且要解决新旧记忆的逻辑冲突。比如说你以前说我喜欢啥，然后过了一段时间你说我现在喜欢干啥？然后这俩发生冲突，一般就是要用新的记忆去把旧的记忆给盖掉，这样就保持保证这个冲突不会发生了，而且遗忘掉那些过时的错误的垃圾内容。
比如说你之前存了一个很过时的内容，然后过了多少时间它已经没有用了，对吧？就要把它给去除掉，检索阶段的时候你不仅要去智能体，不仅要判断它什么时候去翻看记忆，而且要在这个用户说的不太详尽的时候，比如说，哎，我喜欢他这个，他到底代指谁呢？就要进行查询重写。
而且还要对所有的记忆碎片进行重排序，他的记忆有的有用，有的没有用，那怎么是最有用的呢？还要，所以要进行重排序，而这种从静态存储走向形成演进检索闭态的这种动态循环的特征，正是现代记忆智能体记忆系统与传统数据库之间最根本的差异，而这种更新与遗忘机制又是记忆系统在面对真实的长周期任务时能否长期稳定运行，保能否长期运行的这种关键。如果没有行之有效的这种遗忘策略，长期运行的智能体就会被海量的无关噪声和冗余的那种数据给拖垮。就让他推理的成本，推理成本飙升，而且检测的精度就会比较低。
这个更新机制主要是来解决新信息与旧记忆的冲突问题，就导比如说有些矛盾的记忆他就会进行更改之类的，这种遗忘的策略他主要是解决的是留下来什么的问题，他就是他把什更什么更有用的留下来，什么更有用的他放在第一位，有三种主流的遗忘形式吧。第一种是基于时间的，就是模拟这种，主要是模拟人类生物学的这种记忆衰减曲线，就是那种越久的东西，越不重要的是东。东西它就赋予的权重就越低，越新的东西就越新，越重要的东西它给的权重越高。再想的时候搜，在搜索的时候就搜索到这种权重更高的东西，基于频率的，像类似于那种计算机调度的 LRU 的那种机制，频繁被任务调度的记忆，它就会留在核心区，长期不用它就会放到边缘部分价值和语义的就是这种，就是大模型时代特有的，直接就让这个大模型去充当裁判，对每一条的记忆含金量进行一个主观型记忆打分，它让这个把这个东西交给 LM 去做清洗，去把这种低分的给筛选掉。这是这种更新和遗忘机制智能体的 LM 走向更强智能体。
关键就在于迈向第一类公民范式的转化，主要是主要就讲的是记忆现在已经不是 agent 可有可无的外置挂件，而是必须逐渐演变并如何成为 agent 核心认知架构中密不可分的一部分，我们就需要让这个 agent 去有更多的形态、更丰富的功能，而且具备自我演进，把它从那种单纯的那种单次对话补全，而升级为更长周期、更复杂环境生存与自进化能力的这种智能体。
接下来我主要讲的是两个部分，一个是 Token 级别，就是这种。这种静态数据库使用最多的一个工程化的库，就是 GitHub 上开源的一个，是 GitHub 上开源使用最多的一个 Mem0 的库，我针对它的开源代码，然后进行了一个流程的解析，我主要汇报聚焦在三件事，一个是它的add，它是如何存记忆的， search 它如何去查记忆的，它的底层，比如说它的更新删除功能，还有它是怎么存储这些记忆的？首先我先讲一下这个 add 部分，然后再。然后分层讲，一个是add，一个是search，然后它的最后它的update、 delete 和底层存储的分工。
首先它的add，它，它的记忆的 add 是怎么实现的呢？就是它，不是，就是它这个它的add，它不是收到聊天就存，而是它是先进行整理再落库，像我们之前讲的，它并不是直接就存原始型的记忆，而是会对这个记忆进行语义层的整理，把，比如说把这个聊天它变成了标准的事实句，然后这个它，这个它是怎么做到呢？它就是它首先是靠它，首先是它这个是依靠这个大语言模型去，它去把这个零散的聊天变成了独立无歧义的规范记忆。
他的代码是依靠三件事，比如第一个是读上下文，拉取最近 10 条的原始上下文对话，然后并且他再去召回，他再通过这个语义相似度，用向量的余弦相似度去做，他再去召回 10 条旧的记忆，相关性最高的旧的记忆，然后他把这个上下文和这个旧的记忆，再加上自己新的这条东西都传给了大语言模型。
大语言模型就把他的这些，他的。他的大语言模型就把他的传进去，这些东西变成一个规范的句子，把这些零散的聊天上下文、旧记忆去压缩成一个能单独看懂的事实，这个上下文和旧记忆的为什么要这个呢？为什么要用上下文和旧记忆呢？主要是因为比如说用户对话的时候可能会说我喜欢他，但是这个他是代指的什么呢？我们这大这模型不知道对不对？那我就需要去用，比如说我要去搜索我最近的 10 条对话，对吧？最近的 10 条可能告诉我这个他代指什么东西，哎？再比如我召回旧的记忆，比如说我之前在聊到这个大概的事情的时候，我都说了什么呢？哎，这个我就要靠语义相似。向量的一些相似度去做，让这个大语言模型知道了我的前后语境。通过这。对对对，对的，它每一轮都会调一次。
诶，那它这个 add memory 是每一轮对话结束之后它都会调用一次吗？就是大模型生成完一次回答之后就会调用一下 add memory，这样OK。
对的，就是其实它这部分还做了一个去重工作，就是比如说你之你库里已经存了，就是你之前召回进帆已经存了这个东西，对不对？你大于你模型生成了一个类似一样的东西的时候，它其实在这还会做个语义层的去重，它并不会就是把这个也存下去，就是如果发现它已经有个概念相同，它就不会存这个行。
对对对的。可以讲得精简一些。没事，你讲吧？就是讲得有点太仔细了，就这个部分， add memory 这个流程，这。
在系统层落库的时候，它已经把大语言模型已经抽换成规范句，之后的时候它又会进行生成向量，存到向量的数据库里，并且它还会精简这个关键词，还原这个词形。比如说它把我正在吃什么东西改成我吃，就改成我吃什么东西，就把什么某些繁杂冗余的句子，什么有什么各种时态的变成关，变成一个无时态的东西。这个就是专门用来给之后的 BM 25 关键词匹配的时候用的，所以他会把这个也存起来。
第四，然后他把这些向量这些原文关键词文本，还有什么创建时间、更新时间一起存入了向量库中。接下来一步他要进行的是实体绑定，他这什么东西呢？他就是曼姆林，原先是以是双重召回，他还会建立知识图谱，但他现在做的比他现在把这知识图谱的部分改更改了，而是他进行了实体绑定，那从这个规范句子中他去。挖取了人名、物品名这些实体的东西，它存进实体库里，把这个实体库它去，再去和记忆的这些 ID 绑定，它就存到了实体库中，它然后它还会以维护一个 history 的数据库，它还会把这些赠 add 记录都会扔进去，这是它的 add 的详细流程，就大概就是通过我，大概就通过我刚才讲的这几个部分去实现的，它有个重点就进行了。
核心有以这个三道收缩会让记忆越来越稳，它在收缩，比如说它在记它的作用域收缩，它就只在 user 或者 agent 或者 run 这种专属空间里去处理，它不会进跨用户存在，它还会把这些聊天压缩成规范事实，消除歧义。它在去重方面，对吧？比如说刚才我们说它的演进的时候，它还会进行去重，就像它大语言模型在处理的时候它会，它不是调了过去的这些记忆，它还会，它首先会对这个语义去重，比如说它我发现我生成这些东西和之前都完全一样，那我就不会存之前的东西。语义去重完之后，它为了保证它进行了保。
进行了哈希去重，就避免了重复这种写库，这是他主要艾特的这个东西，就比如他这个他原始的电视，我还是喜欢那个，那他整理以后的具体就是这种用户仍然喜欢之前谈到的方案，底下是底下，这是他在那个源代码中是写的这各种prompt，去帮助他更好地处理这些用户的这些问题，去帮助的就是艾特这些库，艾特进这些库里，就是刚才讲的那些他会先进，他会先使用大语言模型语义去重，然后语义去把它去重。之后再用哈希进行去重。
就让他一个意思，不要重复写，也让他的文本不要去重复的写，这是他是这些去重的部分，他的源数据库入库的部分，就是我们刚才所说的，他先会进行一个词形的还原，把这些什么动词、复数、比较级全部用这个统一的词形停掉，那些什么没有用的词，什么标点符号去帮助我们更好地进行关键词匹配。
就 search 的时候这个实体入库也是为了帮他的，他抽实体去记录这个实体有关的关系，也是为有关的这些记忆，也是为了更之后搜索的时候更好去使用它。这是这些数据入库部分在它的记忆的添加之后，到了记忆的这个搜索，它的搜索是怎么做的呢？比如说这个 query 的话，它碰到一个query，它查询它是会先做，就是先会预处理，先把这个问题向量化，再刚关键词化，就是刚才用到那个 BM25 关键词匹配的用到的实体抽取，就是去抽取实体，去和刚才那些实体的那些记忆进行比对，会得到三类的这种查询表示。然后它分别通过这三个这种余弦相似度的召回这 BM 25 的关键字匹配，再通过这个实体的增强，然后这三个部分进行一个加权的融合，就大概就是它这个计算这个记忆得分的一个一个步骤，然后它之后这个重排部分是它可选的，如果你配置了这个重排，它跑遍重排，比如说你可以让大语言模型再做一遍。
重排，他自己对这个关键词记忆进行一个排序，对吧？像刚才所说，用户查询会通过这三种部分综合计算得分。余弦相似度是主召回时这个 BM25 和这个实体更多是给它进行加分，这是那个官方的库里进给他的一个流程图，他是并行去计算这三种得分的。他先查询向量库召回意思相近，用词不同的这种记忆，再通过关键词匹配到关键词匹配。比如说他要去查 bug 1234，他会专门查 bug 1234。然后得到这个分数，再去从这个查询里面挖这个实体，去找这个实体关联的记忆，再给它进行加分，最后得到一个总的分数。
这个抽实体的记忆，它是比如说抽到一个实体，我发现它是会把这个实体进行一个规范化的命名，它如果它之前发现这个实体不存在，它会建立这个实体，在这个实体的库里建立这个实体，如果存在，它就会直接在这个实体后面的记忆里加上这一条，这是它的检索的部分，它使用多信号综合检索的之后再过选择，可以选择重排，进行一个 RE rank 的重排它的。
更新 upside 操作和 update 操作和 DELETE 操作，它这个部就是也它这个记忆更新就是重写记忆文本，重新计算这个embedding，然后把这个更新的记忆它放到它那个历史操作库里，这是它更新记忆的操作，它删除也是这样的，从向量库里删除，然后写入 DELETE 历史中，然后清理实体索引中它这些关联，这是它的这两个部分。它的这个记忆库里总共有三个库，一个是向量库，一个是circulate，去存它的这些 circulate 库。一个是一个实体库，这个向量库就是存它这种向量记忆。这个 SQLite 就是存它的添加更新这种各种历史去保，还有它的原始的上下文信息，这个实体就是它会存它实体库，以及实体中存实体库对应的记忆这个漫名它还它最近还进行了两次变更，第一个是它把它之前是什么样的？它之前是，哎，如果我调用这个 add 的话，比如说之前我调用这个记忆添加的话，其实我最我底层可能会调用 addupdate 和 delete 三种形式，就是我有，我可能在这个记忆库里添加记忆，比如说我遇到那种什么需要更新的，或者删除记忆，我还会进行。我还会找完记忆之后比对之后进行更新或者删除，但是这个曼明他们官方发现这个很慢，而且还很浪，耗费TOKEN，而且还很，这个由 LM 判断它很容易删除，误删很重要的历史。
然后他现在已经改成了只增不删部分，他任何新信息都像写日记一样，直接就追加写入他，并且附带着最新的时间戳，这样写的操作延迟直接缩减了一半。但他现在因为他原先可能还要去靠大语言模型去判断要不要更新或者删除，所以很慢，他现在不用了就会很快。他至于什么，至于这个更新或者删除部分，这个怎么去判断这些旧的没有用的信息呢？他这个只。
把这些东西都交给了检索的部分，这个检索的时候他会让你大模型去选择是不是通过时间戳过滤，比如说你要用最新的，最新时间的东西，就可以通过时间戳过滤去找这个新时间的东西，或者你可以自己开启大模型的重排，让大模型去判断哪个是最新的、最准确的记忆。这是他的一个变更，第二个变更就是他把之前，就像我刚才说的，他把之前他双重召回，还会走一个知识图谱进行一个，比如说进行一个知识图谱的那种遍历召回。还得再用个图数据库去存里去构建节点和关系，他发现这样导致提取得很慢，而且极易出错，查询延迟极高，他发现其实用这个知识图在 99% 在百大部分情况下用知识图谱都是会导致它速率更慢，那个效果也没有提升，很好，所以他现在已经把图数据库已经彻底废除了，而是改用实体连接，就像我们刚才说的，他把这些专有名词提取出来当做标签，他挂在向量上。这些实体。他这个实体对应的这种各种记忆，他检索的时候直接做这种底层数学加分。就把这个有关这个实体的记忆进行加分，这样就可以提升了更多的速度。
之前的图数据库模式下，它每次 add 需要先提起事实，再提起实体和关系，提取这个三元组，然后还得两次写入，写一个向量库，再写个图数据库，这样就很慢。现在实体的话只需要提取，是只需要提起之后一次性的写入一个向量库就可以了，就一次数据库写入一次 LM 调用它的延时就会减半，这是这个 Mem 0 的核心部分，基本上就讲完了，接下来讲的是。
接下来讲的这篇论文是 Iclear 2026 年 2026 的一个论文，它这个主要是针对于就像。之前是就像刚才介绍那个前记忆所做的一个论文，他发现我们在目前在构建大规模自进化 agent 的时候，他的核心障碍往往集中在，就是集中在这个记忆系统的建设上，但是这个虽然在单次交互中表现经验，但在那种长周期跨领域的复杂任务里，他发现表现的就有点匮乏了，他缺乏那种持续经验的工作手感。而这篇论文他提出了一种机制，就是让大模型通过这个生成机器原生的这个潜在记忆，在生成过程中实时去植入这些他的这些思维流，让他去更好的去帮助我们去。更好地去完善这个agent。
就是。这就是这个它的，它解决的问题就是现在的 agent memory，它不是，就像刚才不是分成三种，一种是 TOKEN 级别的，一种是参数级别的。现在的这个 agent memory 它不是改参数，就是把经验硬塞进上下文，它缺少与推理同步演化的内生记忆。这什么意思？就是改参数它就会导致那个参数就可它直接改参数，就是就可能导致那种参数性的遗忘，然后而如果用 TOKEN 级别的，它一般现在不都是先检索，然后再去推理它，就在推理之前进行一个检索，就这种一次性的东西，而不能像我们人类一样，比如说我们人类做着做着题，比如说我们做一道题，哎，我们读着读着题就发现读到这了，发现，诶？可以用这种做法去做，对不对？它缺乏这种。比如说思考到某个过程的时候，他就调用一次 memory 去进行检索的东西，这是他的一个现有问题，他做了，他就做了一些，他又做了相对应的创新。
其实现在那些 rag 那一套，他们也开始做这种，就是边 think think 到一半，然后进调用search，就是调用检索来做，他们也可以，就比如 search 2万这种。
对，对，但这用前，但是他这个用潜在记忆，比如那种如果用静态的去编 search 边那个检索的话，他的效速，他的速率会比较的慢，这也是一个部分，就是你看你用前记忆，然后用这种前记忆的话，他这个论文实现主要用那个前记忆去训练一个 Lora 的参数矩阵，然后他的他这个 Lora 参数矩阵，他就不需要像这那种检索一样再去用 LM 去进行各种检索，然后这样就会这样速率就会很低。他这样也，他这个也是一个为了相对配合着提升速率的一个东西。
我觉得这样它可以提升效率。OK。
对对对，然后他这个东西还在跨领域的，在这种跨领域的复杂推理测试中，他的准确率也非常的高，这是他这个大概的模式，他的，他主要是就是为了解决这两条路线，他都不像人类记忆的这种问题，一种参数化记忆有这些的劣势就会破坏之前的参数检索性记忆。也有这种不好的，就游离于认知加工的工程之外，他觉得真正有用的应该是和推理相联动的检索过去，还能对过去进行重构和压缩。
他的核心思想就是他所说的 wave 是什么意思呢？就是他把当前的 hidden states 认，就是比如说他在解题或者执行过冲突中，他可能会遭遇关键推理的节点，比如说他遇到了知识盲区、逻辑卡壳，或者有一些遇到问题的时候，他是想他会训练一个模块去介入，这个时候他就会暂停这个继续的生成，去捕捉他当前的内部特征，他生出一，他就会是他，并且还会生出一连串的高维连续空间的。
latent tokens 就是潜在的表征向量，然后这把这个向量再前插到当前的这个隐藏状态序列中，用于后续的这个推理。这什么意思？就是比如说我们这个机器它想着想着它生成部分，它发现它有点卡壳，它不知道接下来该生成什么，它开始疑惑了，它这个东西就是它这个Memgen，它就是在这个疑惑的时候它会去出手，它去判断。我现在是不是疑惑了？如果发现我疑惑了，我就通过我，我之前训练的 Lora 矩阵去，我，我去介入，我去介入。你比如说我就会告诉你这个 Lora 矩阵就会给生成一个潜在向，潜在特潜在的tokens。然后去拼到你现在中间层的输出里，然后告就相当于告诉你接下来该怎么做，然后你就拿现在中间层输出，就继续接下来的去生成 KKV 矩阵，后续用来后续的推理。
诶，这是他的，这是他大概的时候，大概的步骤，他就有点像人类在解题卡壳的时候出现突然出现的直觉，我们不，我们就有时候我们脑海里会突然灵光闪现，就告诉我们，诶，这道题，诶？可能会这么做，不需，因为你看他用这种潜在的，他就不需要，他不需要像那种静态，像就不需要像那种静态的数据库，诶？用生直接生成一个语言，比如说。
这个推理流语言告诉我们，就会像人类生成语言告诉我们，诶？这步应该怎么做？而是它就直接生成这种机器用的语言，然后它直接就告诉机器了，然后这样就会很节省它的速度。然后这是它的整体框架，它是一个三模组设计的框架，这是它的方法主图，它中间就是它，冻结的推理器就是它的，就是它主要用的大模型，它的参数是全程保持冻结的，也不会发生任何的东西，这就保证它不是那种参，训练参数化记忆不会发生那种模型，那种那些知识的遗忘，知识的那些覆盖之类的。然后第二块就是它的triggertrigger，就是它的记忆触发器，这个是一个轻量的 Lora 适配器，它会进行原认知状态的监控，就来输它的输出是一个激活的概率，就是决定当前是继续正常生成还是强行暂停。强行暂停就是它发现我的生成遇到了困，遇到了一些困难，我生成我现在问题我比较困惑，我需要外部的帮助。
这个第三个 Waver 它就是它是一个记忆的编织器，它同样也是用的轻量的 Lora 识别器训练的，它一旦收到 trigger 的强行暂停的信号，它会把当前的内部特征与可选的外部经验重构和与可选的外部经验重构成一个高维的机器原始信号，就像刚才一样，重构成一个，重构成了一个 latent TOKEN，然后去和现在的当前和当前我遇到困难的 latent tokens 去拼到前边，然后让我就相当于给我提示，让我继续去生成。然后这是它的总体。
总体设计，接下来讲一下它的每个组件是怎么进行的这个设计，这个 trigger 它学的是什么？它学的是何时去记忆，何时去触发回忆，而不是去回忆什么。这个 trigger 它就监测，监控的是模型的中间层输出的高维隐藏状态去感知。我是不是卡壳了？就通过这个 trigger 去感知我是不是现在生成卡壳了？它是怎么做的呢？它并不是在每个 TOKEN 处都做这种网络的前向计算，这样就很麻烦，就每个 TOKEN 都太慢了。他是怎么做呢？他是在句子分隔符处进行了激活检查，比如说逗号、句号每这种符号部分他就检查，因为他觉得这个，他觉得这种句号，这个符号就代表着一个句子的转折，而且这些符号可以直接用那种正规表达，正规那种表达式去匹配就很快，他不用去，如果那其他的那种去专门找哪个 TOKEN 适合去触发，然后他就需要用专门的东西去做，就做起来就比较有点费，有点消耗过大。
它不仅能识别的知识盲区，还能敏锐地捕捉到跨上下文的断裂或者什么结论，它前导之间那种结论，前导前的那种关键推节点的隐状态畸变，它这个是通过强化学习的训练去引入调用频率的惩罚项，它让它这个 trigger 会学会在什么时候它是需要经验辅助的这种决策点，它就会执行暂停它、告它，它接下来就会调用我们的那个Waver，这是它那个面对不同的 Benchmark 的时候，它就这，它那个选择。这个。选择这个 trigger 调用的概率，它这个 Waven 就是当 trigger 我发现，哎，我当前的时候，我发现我当前我这个大模型学习输入生成出来有点困难了，我 triger 我就拉起手刹，我就不干了，我说我接下来就要把控制权交给专门用于经验承载和内化的模块 memory Waven 部分，它就是它的输入，就是以它的输入就只有一样东西，就是大模型当前的隐藏状态序列，它凭借它之前的。
训练，他说他有两种训练方法，一种是通过强化学习，一种是通过大模型微调，这都是可以的。他通过这两种部分他就训，他把自己训练好了，他就可以通过自己的参数，对，他就通过自己的参数，对这个大模型，这个他我现在当前卡壳遇到什么问题、什么困难？比如说我解方程，我遇到，我不知道接下来该怎么做了，他用我之前训练过的这些知识，他进行生成式的重构，他会合成一个固定长度的潜在记忆矩阵，然后他把这个潜在记忆矩阵就会拼到我现在这个大模型当前的隐藏序列里，然后接下来叫让我继续去做，相当于给我，就相当于。拼一个大模型机器才能看懂的这种proof，对吧？相当于这是我 Waver 所做的东西，这也是我训练一个 Lora 矩阵的，也不改变就是它的设计，它的它具体为什么它也没有讲它，它就可能它就写的它是一个固定长度的。
诶，我没有声音。你好，他的潜在记忆矩阵为什么是固定长度的？
我觉得变长的吧。诶，对了，我感觉这个文章的机制是不是有点类似投机解码？
他论文里写的固定长度，我等我可以接下来再去看一下他。这个我在这个分享之后，我会再去看一下他为什么是固定长度的？我看完了可以再去和你分享一下，怎么样？
对，如果你跑这个实验的话，也可以设置看看不同长度的这个长度影响啥的。我感觉他这篇论文思想有点像投机解码，他不是生成的，是潜在记忆的 TOKEN 拼接到后面吗？在推理中投机解码其实他就是在你 coden 阶段每次生成只能生成一个TOKEN，那么投机解码的话他就是先生成一些，先用那个草图去生成一系列的TOKEN，然后再用大模型去验证。生草图的 TOKEN 既可以用小模型去生成，也可以用一些检索或启发式的方式去生成。我感觉这两个有点异曲同工之妙。
OK，OK，我。行，我接下来我现在再看，再去看一下这一块，行，谢谢你。
就举个例子去更好地去解释它，就比如说我们在解方程，比如说在解 2X 减 3 的绝对值等于 5 的这个真实案例的话，它会先它的，比如说它这个大模型它会正常启，正常去启动，它先去生成前半部分的文，比如说它先，首先我们要去掉这个绝对值符号，我们需要，诶，我们需要之后它要讲怎么做。但是比如它现在要开始分类讨论了，但它在这个逻辑分叉点的时候，它就会在这个逗号这个语义边界，我们需要逗号这个语义边界的时候，它的 trigger 会检测到它模型中间层的隐藏状态，它发生了一个极高的不确定性，它就去验它，去验证它这个触发的概率，然后它发现它的这个概率通过了。然后它会暂停它现在的生成，就去靠这个 Waver 传给Waver，它去捕捉当前卡壳的这个隐藏状态的向量，让它去结合自己的参数，它去重构成一个固定长度的潜在机矩阵去拼，然后向量就是它生成个向量就潜在的去表示了分类讨论的这种东西，然后它拼到了前现在的前端之后它就会计算接下来的过程。
它主要就实现了这几点，这三点的这种创新，一种是这种时间维度的东西，就是它之前就是这种先检索再推理，在任务开始之前做一次静态检索，这种局面它现在是在生成文本中边推理边回忆。第二种是空间部分的，它那种摒弃了这种效率低下，而且这种充满其这种自然语言的promote，而是实现了在这种潜在的空间传递这种高密度的认知特征的这种跨越。第三种是架构的一个突破，它不是用参数化记忆，它不是用这种直接参数化记忆，也不是用这种静态的数据库，而是靠这种 trigger 和Waver，两个两 Lora 矩阵，两个 Lora 来去解决何时回忆而回忆什么的这种解耦的这种形式。这是他的一个实验结果，他在这种跨领域的这种 9 个主流数据上都进行了一些验证。他的基线，他的对比方案从基线到那个到强的强化学习算法之类的都进行了一个验证，他发现他的效果就很好。
他好像没有在 Memory 领域，这比较最常用的两个数据集上测，没有在那个Longmem、 Evo 和 Longcom 上测这数据集里面好像没测这两个。
他主要进行都是推理的这些东西。
好像是之前做 RAG 常用的这些数据集，测的这些，还有代码相关的数据集测，做推理用的。OK。
确实，他发现他这个效果很好，他这个贡献他都很好，他的对，他发现他这些效果都很好，这个亮点是他在训练，在单一领域中仍能向其他领域进行迁移，比如他在这个奔驰方法上训练，他在其他的效果上也比他那些传统的算法更好，他发现他的跨领域泛化能力很强。然后他发现他的这种跨领域泛化能力就很强，他在数学领域，比如他在数学领域训练的这个模块，他在完全零样本的情况下，他能直接去平移解决其他领域的问题，准确率没有下降，反而还进行了提升。然后因为他自己，他说是因为他这个权，他的这个 Waver 权权重参数内部没有存具体的数学公式或者死记硬背的模块，而在参数中固化沉淀出来这种通用的抽象的这种认知策略，比如说就遇到什么事，遇到什么多步困难事，我们进行目标重评，什么步骤拆解什么是，比如说什么生成代码要进行对齐格式或者合规检查这种高级性的策略，然后。
这个是准确率，回答日问题的准确率。OK。
他在面对全然陌生的跨领域的时候，他会自动这种降低触发回忆的频率，避免胡乱调用不成熟的记忆，他会展现这种高阶的抽象型、策略型经验，就导致他在跨领域泛化能力也很强。
诶，那他这个 memory 模块主要用的就是你刚提到那个 Waver 模块，是吧？他是训练完一次之后就不会再变了，是吧？那如果用户随着用户问题这个时候需要更新一些记忆的时候，他这个系统怎么办？就是他这个。
对，对，他，对，就没有继续更新，他没有继续更新。对对对，他没有。
这，这不是他主要想干的事，但是他没有讲的对，他就直接就静态的。
不是这个事情，这个我也感到有些疑惑，你最开始介绍的时候大概是分了三类，其中有一个是参数化记忆，还有这个应该算是潜在记忆，参数化记忆它修改的其实是模型 backbone 里边的一些参数，那么它提出的这个方法其实它并不是直接存了隐藏层的数据，是吧？它其实也是做了一个模型去生成一些 HIDDEN state，对吧？所以说它自己本身其实也是一个模型，它也有自己的参数矩阵，那相比这个参数化的记忆的方法。它怎么去避免那些比如说参数被覆盖导致的灾难性遗忘，还有泛化的问题。
对对对。
就真的，他。
就是因为他，就是因为他这个参数化，他这个之前说的参数化就是对那个主要的推理大模型进行的进行这个参数改变，而他这个训练一个轻量的 Lora 模型，他在训练的时候都只是对这个 Lora 参数进行改变，并没有对主要的那个推理大模型进行参数的改变，所以他不会。
但如果你场景变化了，你的记忆出现了变化，那么你这种不同的记忆对它这个模型本身参数的影响就是不同的记忆它应该也有不同配套的参数吧。那你新的记忆来了，也有可能会覆盖这个 Waver 这个模型的参数啊。
他这个 Waver 这个东西，他训练，他在这个论文里训练就直接是个静态的，他用那种高阶的策略，比如说我们遇到什么问题该怎么样去解决？他并不是去，并不是存的是那种事实性记忆，而是存的是那种比如说认知型、那种策略型的经验。就比如说我们遇到这个问，遇到什么要分类讨论的问题，我们该大概选用什么方法去解决？而不是存在的那种事实性记忆，就是他大概存的是解决的方略，就那种长期型的记忆就大概是这样的东西，所以他不会去影响主推理模型的参数。你遇到跨领域的问题，对吧？你跨领域主要还是靠主那个主推理的模型，你这个 Waver 在。 Waver 提供是一种高级的认知策略，然后在跨领域的模型上他这个高级认知策略也同样可以发挥一定的作用，这是他这个论文想讲的东西。
好的好的，谢谢。
嗯，好的，谢谢。
他这个论文，他然后他发现他的在持续学习和延时方面也有一定的进步，他发现他在学习完新任务的时候，就像刚才学姐提出的，他传统的S，传统的这种微调可能会改了模型的权重，导致旧任务的准确率发生了，这种准确率发生了跌，但他靠这种用这种存在着外部的这种 Lora 矩阵，他没有改变，他这没有改变，他主推理模型，然后他会去展示。他就会展现一部分稳定性，因为他这个 waverLora 矩阵， Lora 矩阵训练的更是这种。
策略型的知识，对吧？它不会影响这种常识性的东西，所以它能展现出强大的常识稳定性。而在推理延迟开销上，如果正常情况下来想这种大模型中途频繁停下来的话，那它的端到端延迟一定会提，进行了飙升。但是它发现它的实验发现它的耗时不但没有增加，反而还进行了下降。
它发现传统的大模型在面对难题时，由于缺乏解题策略，它会在推理上下文中吐出它一堆海什么胡思乱想的东西，因为它不知道该怎么做。它会随机找一个路径去做，它发现它，这带来了庞大的 TOKEN 开销，而它靠这个 manage 这些 waver 注入的这几个潜在的TOKEN，它反而会让模型。反而会让模型知道该怎么做，它能更好地找到正确的道路，它反而会减少了试错和减少了试错，让它的答案让它正确，答案更准确，它反而让它整体推理延时实现了下降。这是它实验发现的，发现它这个持续学习能力和推理开销都进行了优化。然后他还，就其实他这个Waver，他还发现他的，他这个记忆，他发他就是他没完全就是他进行了一个探测实验，他发现在没有给予任何人工指令，去告诉 Waver 应该怎么分类记忆的前提下，他仅依靠高维潜空间的端到映射，他的内部网络竟然自发地分化成了三种记忆的层次。
他发现他这个记，他就是那个Waver，是这种 Lora 记忆，他竟然划分成三类记忆，一个是规划型记忆，一个是程序型记忆，一个是工作记忆，就是他发现他这种训练好了就自分类，就在他内部去分化成这三种记忆类型去指导这个。遇到困难时候知道这个策略该怎么做，这是他论文里所写到的 Waver 内部自发演化的类人记忆的层级，他还进行了一个消融实验，就比如说，哎，我训这个 trigger 的时候去训练这个trigger，就是到我遇到什么标点符号的时候，我去判断它到底要不要用，到底要不要触发Waver？我要不要去训练这个东西到底有没有用啊？如果我让模型每一个标点符号都去回忆一次，或者给一个 50% 的概率，对吧？我这个模型百有 50% 概率， 20% 概率去触发，会不会也差不多？他就做这个消融实验，他发现。他发现这个 trigger 是训练是必要的，他发现这个 Memgen 的这种机制，他和这种传统的检索融合的时候，也是有很大的优化的时候，他想说 Memgen 他不是要替代检索，是去当一个认知的编译器。
所以他这里，他这个工作和用外部知识库检索，他俩其实是可以一块协同工作的。就是我他这篇文章这个 memory 其实和我们之前关注的那些 memory 其实好像不是在解决同一个事情，他这里好像更关注的是解决一些特定问题的时候，能够通过他这个 viewer 能够具备一些经验之类的。但是我们关注那些记忆其实是更会随着用户的问题要不断的变化不断更新的，但是他这个文章的话好像其实不太能解决这一点。
对对对。
确实，但他那个 Waver 也是可以，再加上他可以和外部数据库配合的。我其实想的是这种用 Lora 去训练这种矩阵的， Lora 就去训练 Lora 去固化这个记忆的方式，我感觉可以用在那种长期记忆或者用户画像上，比如这种策略型的认知，这种高级型的，这种策略型的认知是完全可以使用这种训练 Lora 的这种方式的，毕竟他不需要长，不需要频繁的更新的话，我觉得完全可以用这种方式去存。
是的。
然后就基本上就分享结束了，就大概是，就我觉得大概是这种，我觉得这种间接的这种参数记忆就训练个Lora，然后还有这种潜在的这个我感觉都是可以用来优化在这种静态的静态数据库上的。就比如说这种像我说这种长期记忆，这种什么这种用户记忆，比如说是我觉得也可以是，甚至可以这种分 Lora 有训练很多不同的Lora，对吧？然后再训练一个路由器去选择去，比如说在某个部分。在你大模型需要的时候去进行选择，对吧？给他去训练这种动态权重，去进行相应的选择，去看他哪个 Lora 里存的更是你现在需要的，因为这种 Lo 这种训练 Lora 它的优势很明显，推理性能比较高，而且还它推理性能很高，而且还不会更改那个主推理模型的参数，所以我觉得就这部分对我感觉我们借鉴还是有一步有一定作用的。然后我大概就讲完了。
不好。就是直接把今天。
OK，行，讲了挺久的，讲了一个小时，然后感觉我看中间这篇文章是主要面向推理任务的，我看中间那个 map 0，它现在代码库改了，是吧？它现在变成 append only 了。我看它你前面也提到它好像可以支持用时间戳做检索，感觉可以看一下它现在这个可能也是间接地支持了一些时序上的召回，感觉可以大概看看它是咋做的。OK，那别的同学还有什么问题吗？
对对对对。对对对。
行。
可以，你要是没什么问题的话，咱们今天要不就到这？嗯，OK。
我就结束共享了。
你早点看。

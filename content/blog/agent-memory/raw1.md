然后我这次的分享就是关于 agent memory 这个比一个比较新兴的领域，是它是一个现在大模型，由原来的这个对话框里面它无法对这个用户的画像有一个了解，它每次回答都是基于当前窗口内的上下文和用户当前输入的问题来进行回复，那么随着这个大模型。的应用越来越广泛，它就比如说可，比如说它现在可以充当一个个人的助手，或者代码写帮助写代码的助手，这样子它可以，如果它能了解用户的这个代码风格，或者说是一些别的行为习惯，可能会对回这个大模型回复的质量有更好的帮助。
然后除了 agent memory 之外，然后这我这次分享的会额外提到一个部分，就是时序上的。一个记忆管理，这是一个当前可能是，就是之后打算要是继续了解的一个点，我个人，然后首先就是背景了，这个大模型从单纯的聊天机器人已经逐渐走向能够独立规划执行任务的智能体，所以说这个我起的名字也是 agent memory，不是大模型memory，然后一个具备长期记忆的 agent 应该能够在复杂的任务中保持状态。能够持续地跟进用户的这个问题，积累经验，并且进行个性化的进化，就是这个 self involving，那么一个好的 agent memory 可以由三个下面这三个方向方面进行概括。首先就是存这方面，它要精准地存下来，就是要精准地存下来，应该存的东西可能包括交互、历史任务行执行轨迹，然后环境反馈，尤其是用户偏好这方面。然后就是第二个方面就是查，要查得准。这个当我们需要从记忆库中进行召回的时候，要精准地召回到相关的记忆。然后除此之外，现在一些现在的一些文章还关注这个记忆驱动决策，就是我不仅这个召回，这个评估质量不只是根据这个召回率这样的指标来评判，而且还要根据召当前召回的记忆是否能够真正帮助到大模型或者 agent 对于后续进行决策，或只或者只进行action。
提升 action 的质量来提升这个决策的连贯性，然后最后就是这个演进就是记忆的，我这么想的，就是这个记忆的周生命周期的管理，就是我这个 agent 自我演进也依赖记忆的演进，说白了就是这个存这里就是增查就是查，然后演进就是删和改。我大概是这么想的，然后。
记忆的话，按照现在的这个整个研究领域的话，大概把它分了两个种类，两个方面，就是这个记忆的载体是什么？第一个就是第一种就是 TOKEN level 的记忆，就是把它以明文的方式进行保存，就像之前做 RAG 那样，用一个搭建一个外部的知识库，通过 RAG 进行那一套东西来检索，然后把，然后记忆检索到了记忆，然后以文本的方式输入到上下文窗口中，帮助大模型做回答。然后第二种就是参数化记忆。参数化记忆这里就比较笼统，包括预训练或者微调，或者是 Lora 等信息编码进大模型的权重中。然后还有一个比较新兴的，这个 DeepSeek 的还提出了一个 NN gram，好像也是有点像参数化记忆，但是又。有点像。不太像。也不太像，就是他那个其实也有点像明文，因为他是 n gram 做的。但是他只是，但他确实是改了模型内部的结构。对对对，然后参数化之前你还少一个，就是kvcache。对对对， kvcache 也是，对。就是Kvcache，他们就是也类似于参数化，但是它并不是固定到那个模型里面。
是的，然后按重要性分类。为什么按重要性分类？因为这个现在的一些记忆系 memory 系统通常都会因为一，按照重要性来进行层次化的划分把，然后根据这个来设计一些这个记忆的这个结构，然后比如说比较核心的就是用户的核心。核心偏好这样一些核心内容和当前会话内容，这肯定是对于当前这轮会话是非常重要的，需要非常快速地添加进来或者召回，然后其他的就是一些可能会用到的，但是不一定会在当前对话内用到的，就是之前任某个任务的记忆，或者是之前哪一天提到了什么事情的记忆，还有一些工具调用的经验，然后按时间分类，就可以分成短期记忆和长期记忆这样。不过我感觉这个短期记忆和长期记忆的话。现在其实记忆系统不太用这个，就短期记忆其实已经有了。就是 key CACHE，说白了。是，反正就是这个短期、长期它有就不同，文章对于这个的理解也不太一样。然后我感觉这个可能说起来也不太直观，也可能也可以通过这个重要性划分这分类来指导这个 agent memory 的设计，好像更多一些这样。
然后右边这个图就是从一个综述里面拿过来的，大概就是就看这三 12 三这三个部分，第一个就是从左上角这个一个 raw information 就是原文本进来原data， raw data，然后把它转换成 memory unit，可以同既可以是语义上的总结，也可以是知识蒸馏或者是结构化的构建，比如说知识图谱或者是 latent re， re representation，比如说有一些是存这个大模型某。
后里面某几层的那个 logits 还是权重什么之类的，也当能也能当记忆或者 param 这个参数化记忆，这样然后参模记忆形成了之后，就会随着这个时间的演进，这记忆是需要进化的，就是比如说记忆需要更新或者需要遗忘，还有不同用户之间的记忆是需要进行隔离。然后第三个部分就是查怎么查？然后怎么查？什么时候查？这样子现在的这些记。记忆系统，他们对于这个检索什么时候检索？什么时候检索什么时候写、什么时候查，一般都是由大模型自己去通过写一个prompt，然后大模型自己去决定在这一轮对话完了之后，我是不是要去写、去增加、去删，这样，然后参数化记忆，这个刚刚提到参数化记忆它不太透明，而且更新成本比较高。然后 TOKEN level 的记忆它是一种外部存储的方式，支持显示。
的检索，然后也是当前比较主流的研究方向，或者研究比较多的一个方向。因为之前很多做 rag 的那帮人，很多都来做 agent memory，他们就可以很方便地把那之前那一套东西给迁移到 agent memory 这个领域来做。然后还有就是这个记忆的演进也是非常重要的一点，刚刚说了，然后基于 agent memory 的话，大模型的回答就是既包括了上下原本的上下文，会话的上下文还包括了一些记忆的提取，基于这个来。进行回答，然后下面的这我会介绍就是几个现在大模型 agent memory 领域里面一个几个比较主要主流或者是有奠基性的工作，然后重要的关注点是在于如何设计记忆的载体或者搭配，还有搭配怎么样的检索算法。然后接下来这几个 agent memory 系统通，他们都是 TOKEN level 这个流派的这个参数化记忆，这个流派的我没有进行总结。可能还是主要选择的还是 TOKEN level 这种这样的这个记忆的载体比较多。在这个介绍之前，先介绍一下graphrag，因为这 graphrag 它是一个比较，它是 rag 领域一个比较有重要性的工作，然后这 graphrag 它这个论文出现了，也使得后面的大多数这个 RAG 研究都采用了这个向量加知识图谱这两，这个，这两个向量检索和知识图谱这两个东西结合这条。
技术路线，然后也很大程度影响了 memory 系统的设计，那 graphrag 它主要就是在此之前主要都是用这个 BM 25，或者就是那种 TF IDF 那样的词频匹配的那样检索方式，或者是向量检索，那 graphrag 就是把知识图谱给引进来了，提升这 graphrag 引进。
把知识图谱拿进来的话，知识图谱它主要是可以通过一个结构化的实体表示，使得在检索的时候能够增强推理的这个推理正。据比如说某些事，小明想查小明今天做了什么事，那这个知识图里面小明就有一个小明这样的实体，然后直接去扩展一跳，他的邻居就知道了他小明相关的信息，他相比于语义检索的话，能够降低一些语义检索带来的不确定性。然后他 graphrag 他的这个索引构建流程就是上面这个，这一条先从原文本开始，然后进行切分成文本段，然后用大模型来提取这个文本段的这个实体的关系，然后构建出来个知识图谱。构建出来知识图谱之后，知识图谱里面就是实体和实体之间的关系，然后他再通过一个这个 Ladder 算法，把这个知识图谱里面的一些相关的实体抽象成一个大的社区。然后就是那这个社区里面就是有点像聚类，就是把相关的实体给都给聚到一起，然后形成一个社区的摘要，然后这个社区摘要就可以帮助他，这个如果这个问题问的是一个比较笼统的话，他就可以通过社区摘要直接回答出来这一类问题的。这个回复，然后它还会对这个文本段和实体还有社区这三层进行这个向量化，形成三个这个向量检索索引。
然后下边就是这个 graphrag 怎么查？就是当一个查询来的时候，它先把查询变成查询向量， embedding 成查询向量，然后通过 ans 在这个实体这个向量索引中查找相关的实体，然后通过一个表，这个表。表也是提前构建好的，会知道这个实体是属于哪些文本段、哪些社区的，然后把相关的信息加进来，然后就生成一个回答这样子，然后。
那它其实还是用的向量检索，它其实没有什么，就是它图谱的话就是。它会检索NAS，它就在这个检索实体这里用到了一步，然后后面实体它会在实体这个知识图上扩展一两跳。跳一两下。跳一两跳，然后比如实体链接的那个。文本或。对对对，然后把那些拿过来。把那些他，那他走，那他排序吗？还都有 rank 吗？他这个 graphrag 提供了好几套这个搜索流程。我查就是 local search，就是说如果我查的是非常具体的一件事，他就走这个 local search 流程，然后如果他还有一个 global search 流程，就是他这些实体文本段他都不用了，他就直接拿社区这个东西来搜，来那个啥一下。就是，也是向量化，向量化，然后搜搜社区。然后社区拿到的东西。对，就是回答那些比较宽泛的问题。但是他还是没有办法解决一个TOPK，他怎么给他 TOPK 呢？就是你要排序123456，还是说就是按那个实体的那个排序？就按照实体，他把实相关的实体都拿过来，然后都塞到上下文里就行，应该是这个。就是说实，比如自己排TOP1，然后那他实体链接的社区和文本也一块上下文里边也是放TOP1。
是吧？这个我没有细。不过他 graphrag 他那个库也提供了一个，就是像是这样说的，双路召回就是一路是向量，一路是。就是知识图谱，对吧？对，就对。就是就是，还是我向量，是向量，然后那个知识图谱。对，最后Rerank。那知识图谱走的还是那个 local Server 那个路？还是说是直接就用那个知识图谱查？就直接用知识图谱应该。给他一个给，就是相当于也是抽一个实体，然后抽一个，抽一个实体，然后实体去就直接查知识图谱。对。这个也是类似，这个也不太。这个其实这里就是有点像那个蚂蚁那个基金里面那个。对。级联，级联，架构级联，先Ans，然后 Ans 到实体，再。因为传统知识库它应该不会用 ANS 去查，它应该直接丢一个实体进来。就是直接从块里边，然后拿一个实体出来，然后直接查里边插个图就好了。
对，嗯，可能就对，然后后面就是开始介绍这些现在比较主流的那些 agent memory 系统，首先是现在最火这个 open cloud 和cloud。
的code，这两个都是，这两个 agent 的话，他们都采用的是他们的设计哲学，就是 Markdown 文件级记忆，就是说以 Markdown 文件为主，我是需要记什么都自己写到这个 Markdown 文件里边。然后这俩 Openclaw 和 Claw 的 code 不同的是， Openclaw 它还会自己在还是会建一个索引的，建一个这个维护后台，维护一个 Sqlite 索引。然后那它这个 Markdown 主要就是两个部分，一个是大的 Mar memory 点。 MD 长期记忆就是保持这个，就是每轮会话它都会先加载这个东西，就是保持一些比较保存一些比较核心的记忆。然后第二个就是它会记录当某天某月、某年、某月、某天这一天，用户用它用 Openclaw 做了啥事，就是个每日的笔记，然后默认会加载最近两天的笔记，然后它检索的时候用SQL，它还是会用 Sqlite 的这个索引来检索，那检索方式就是。
结合 BM25 和向量检索这两路进行 re rank，然后它有一个带时间衰减旧的笔记，它会降低排序的顺序，就是它这个得最后 rank 那个得分就会用按照时间来降低一些，这样，然后 cloud code 就是完全采用纯文件记忆，就是没有任何索引，就是每次用户问题时候它就直接读这两个文件，就是用户级的这个。
cloud 点 MD 和项目下来，这个 cloud 点 MD 这两个文件读进来，然后进行回答。那这他这样做的话，实际上就没有任何的这个记忆的生命周期管理，因为他直接改 Markdown 文件，就是改了就是看不到之前的了，也不知道之前存的是啥东西。然后还有就是他直接写成一个文件的话，肯定会有很多臃肿的东西，可能不太需要进行，实际上可能用不到，所以就会导致这个上下。文就是这个 TOKEN 耗量非常大，不过它一个好处就是比较透明，因为用户也可以自己去编辑这个记忆。
然后第二个就是这个MEM0， MEM 0 是现在这个 agent memory 里面用的比较应该是最多的一个，一个一个一个算法，它就是向量减向量数据库加知识图谱。然后双路召回，但是就他是把记忆分成三类，然后分成用户记忆和会话记忆， agent 的记忆就是每一类，当比如说问用户，问这个用户相关的一些事的时候，他就会从用户记忆里面挑，但是他存的时候这三种记忆他全都存到一个 an 索引里面，然后知识图谱也全都存在一个知识图谱里面，就是只用一个 AN 索引和一个知识图谱去存这些所有的记忆，然后他会为每条。
记忆来维护一个 Meta data，这个 Meta data 里面会有时间戳，还有类别标签，比如说是这条这个记忆存的是用户记忆还是什么的，还有内容的这个文本体，那么检索的时候就可以按照这个 meta data 来进行过滤，我觉得它应该就是这么做的，主要依赖依靠的就是这个 an 的这个 filter 技术来进行过滤，然后它每轮，然后实际在用。
用的时候，在大模型每轮和用户每轮对话完之后，大模型它会阻塞式的，就自己判断一下是不是这轮对话完了，用户刚输进来的一些东西，我是不是需要添加记忆、删除记忆或者更新记忆？然后如果记忆产生冲突了，就是和当前要新加进来记忆，和之前某个记忆冲突了，就会直接替换掉，或者把旧的记忆给删掉，它也没有任何的生命周期管理的机制。
那它这个 an 索引删除怎么删呢？它这个 map 里。零一般底层就接的那种开源的向量数据库。但是这个。向量数据库咋删它就咋删它不管那下边的逻辑。其实很低效，它，因为它逻辑啥。是，那是它，是的，它都，它不管这个 map 0。一般没有真实删的，你真实删的话重复索引了。
对，所以它就是知一路知识图谱，一路向量检索，然后做这个双路的召回，然后在它基础上后面这个。那它 memgpt 它做了一些优化，它就，但是它是不用知识图谱的，它只用数普传统的数据库和加向量数据库，不过它把记忆给分成三层，就按我刚刚说的那个重要性来分类，分三层，最上面最重要是 core memory 保存高价值的这个信息，它这个怎么分呢？具体这 core memory 也是大模型，自己来总结，就是它觉得哪个重要，它就会记到这个 core memory 里面，然后 recall memory 就是保存，是之前。
和用话用户对话的对话，历史 ARCHIVE memory 就是长期的记忆库，就是我们不能没那么重要就存到这个里面，分成三层，然后每次回答的时候，大模型回答的时候就会它这个上下文窗口就长这样子。首先是系统的提示词是黑的这块，然后它会把核心这个核心这 core memory 给拿进来，全都拿进来放到上下文窗口里面，然后 recall memory 和 archival memory。是不是需要从这个知识库里，从像数据库里面拿它是由还是由大模型自己决定提取出来相关的，就再插到这个上下文窗口里面，然后再回答。然后其中这个 core memory 和 recall memory 都是用一个数据库，普通的数据库来存，其实这个 core memory 用普通文本存也行，因为它每次反正都要每次全都拿进来，但是它用数据库存的话，就是它可以相当于数据库，它可以存一段一段的，就是它方便的每对每。
一段进行编辑和修改，我觉得可能是有这样会方便一点，然后，但是然后 archive 这个 member 是用了向量数据库来存有，通过这个向量检索的方式来召回，然后这个也是没有任何的生命周期管理的。就一直增，对吧？就一直往后增。有增有删。有删。对，有增有删，就是我说没有生命周期管理，指的就是说比如说两个记忆冲突了。我怎么办？就是。明白。默认的方式就直接删掉更新了，就是旧那条记就直接没有了就不记，记不住了就没了。明白。
生命周期管理良好，生命后边就会说就是它应该保有个历史版本，就是记一下之前的之前这个相关记忆是啥，然后现在的又是啥？类似于Git。对对对，就是这样的东西，然后 zip 这个他就是提出了这个，就是通过一个时序知识图谱来。来实现了对记忆的生命周期管理，它这个对于生命周期管理主要就是依赖于它底层这个graphity，一个动态的时序时间感知的这个知识图谱引擎来做。然后核心架构就是它搭建一个三层知识图，就把 Rag graphrag 那套给搬过来，文本实体社区搬过来之后，然后三个图也是互相引用，就是通过实体能找到对应的文本，能找到对应的社区，这样，然后它在它的核心是这个，它这个实。
据的管理主要就是依赖他提出这个双双时态机制，就是他每一实体这个像他这个知识图谱上的每一条边，他会记录下来这个边的事件时间和系统时间，就也可以理解为是客观时间和主观时间。比如说问的时候，问他现在在哪工作，就会按事件时间来查。比如说他现在是 2026 年，他就会查这个事件是在这个，是不是在二六二零二六年这里他就会查这个，然后如果问的是一个比较主观的记忆，就是。
我当时认为他是在哪天工作？比如说我当时认为我去年认为他在哪工作，他就会用系统时间来查，就是我查那个用这个系统时间来做filter，就是看一下哪条记忆是从 2 在 2025 年这是有效的，然后来提取出来那条记忆就可以，因他为啥要双时态？主要就是这个记忆的输入是有主观性的，就是用户通过用户那边接受的不断地输入是具有主观性的。
就是用户很有可能会改，今天喜欢这个，明天又喜欢那个，所以通过这样的双时态机制，它可以实现了对记忆的追溯，然后他还提出了个边失效机制，就是新信息出现的时候，比如新记忆进来了，旧的边不会直接给硬删除掉，而是以事件失效的形式，就比如说这个系统时间这里它就是会就过期了，就过期之后就表示这条记已经过时了，但是还能不能召回？回到也能召回，到这个时候就给大模型，就知道这条记忆是一个过时的记忆，我应该会采取用那条更新的那个那条记忆来回答用户问题，这样当用户问这个，比如说问一条，因为用户有很有可能会问一个已过期的记忆，比如说 10 年前的事，他这个时候就可以记忆，然后就可以回答出来。
但前面那几个 letter 和 MEM 0 都是回答不出来的，然后这后面还，这还有一个，这个 MEM one。 S 它是个非常完备而且复杂的这个 memory 操作系统，它刚刚说的这些它都做了，是个非常庞大的一个系统级别的工程吧。然后就不细说了，就它具体的这些实现的技术，我估计和前面的这些也都差不多，它只不过都揉到一块了，除此之外，它还支持了这种 KV CACHE，这个记忆还有参数记忆它都给，还有这个明文记它都给统。统一管理起来，它都会封装成一个memcube，然后所有的增删改查都是针对这个 memcube 来做。然后前面就是把那些比较主流的这个 MEM agent memory 的系统都介绍了一下，然后下面就是从介绍两个，从向量角，向量检索这个层面是如何支持这个支持时序上的这个问题的。
然后第一个就是这个 split code，它。通过这个它主要的方法就是修改向量表示，就是它会把一条向本来向量检索，直接拿向量原始向量就进行这个检索，然后它现在把原始向量扩充成了一个时间的子向量和一个空间的子向量，然后进行距离计算的时候，就对这三个部分进行加权的距离计算，这样时空相近的这个向量距离就会更小一些，然后空间上它就直接拿这个经度、纬度。
就是拿一个球坐标系直接建模出来这个时，这个空间角度，这个空间位置，那时间位置它也是通过一个这个一个单位圆上的这个距离来表示，就是它维护一个，就是这个东西，它是在 0 到派内，是这个相位里面它是单调的，从 0 到派，如果这两个隔得越远的话，它距离就越长。但是随着时间增加是因为时间是一直往前推进的，可能会无限大，所以它就这里会提出一个。
更新机制就是如果一直往前，时间一直往前走，有一些旧的记忆，就是有一些就是比如说相位超出了这个派了怎么办？它就会维护一个这个相位偏移量，随着这个偏移量也是随时间变大的，当每一条新的记忆进来的时候，它算一下这个新的这个相位，然后如果这个相位不在 0 到派了，那这个向量就失效了。
就是说随着这个系统在一直进行的话，随时间进行那些旧的记忆它就会，它会就淘汰掉它。就不在这个 0 到派这个范围内了，但这条这个记忆就失效了，然后这也是我觉得他这个做的不好的一个点，因为他这个，他就这样就直接把那个旧的记忆给删掉，向量给删，也不是删掉，他就认为这个向量给失效了，就不召回了。然后第二个就是这个 feed MEM，它是提出了一个生物学的启发的遗忘机制，它也是在可以说理解在为，在向量检索层面来支持时序感知。就是它原本计算两个向量距离的时候，直接做算向量距离，现在它是算一个这个重要性得分，就是它有三个部分，第一个就是向量本身的距离，就是CC，就是这个记某条记忆的向量，然后 q 就是query，算一下它俩的向量距离，然后乘这个后面这中间这一项是FF，就是记忆的访问频率。如果。访问频率越高，它是因就越重要。
然后第三个部分就是recency，就是说它这个时间是不是越近越重要？对，然后它。对，感觉是线性的，是线性。然后他也做了一些不同层的划分，比如说这个记忆如果是长期记忆的话，他这个 recency 他就会降得比较慢。如果是远期、短期记忆的话，他就会降得非常快，然后他会也会设计一个这个。就是记忆的这个遗忘机制，就是随时间的话，就是它这个重要性也会越来越低。这样他大概就是做了这个这样的事，然后提了很多公式，不过核心的就是这个公式。对。我感觉是包装一下。就原来是算向量，直接算向量的距离，现在是算一个这样的得分。
然后就看完这些之后，然后我就想了一下，就是哪些记忆应该被删除。然后老的记忆是否应该被删除？冲突的记忆是否应该被删除？然后那些那个 MEM 0 和 MEM GPT 的话，它都是旧的冲突的记忆就直接删除掉了，但是感觉现在看起来只有那些错误的事实记忆是应该被删掉的，比如说生日记错了，学号记错了，那确实应该删掉，但是如果一个记忆是老的，就是它太旧了，但是我觉得是，我感觉是不能删的。就是因为人的遗忘的话是因为脑容量不足，但是良好的。
涉及agent，它可以应该提供一个追溯机制，就是记得再就时间再久，我应该记住它。如果万一我又问了一个相关的你，你不能删掉，那我就真的记不住了。就是得记住说我当时我是怎么想的？对，还有冲突的主观记忆应该保留下来，比如说，就是，比如说。今天喜欢。今天。吃苹果，明天喜欢吃香蕉。对，这个应该保留着历史版本，提供追溯机。
机制，然后具体实现方法也可，就可以通过那个时间戳 metadata 上的时间戳来做一个，用 an 来做 filter 就可以。然后目前现在大多数 memory 主要是依赖这个时序知识图谱来进行时序记忆的管理，然后在向量检索层面，目前除了我刚刚说的这两个工作，除了这个 feedmem 和 speed code 之外，就我觉得就是其实大部分还是通过这个 filter 技术来筛选符合日期的要求的记忆，所以这个向量检索这个 FILTER 技术在这个memory。 query 的时序管理上应该是有比较重要的应用的，然后第三个就是时序上的相关的记忆在向量表示上是不是会相似，然后进而影响 an 索引的质量。
就比如说今天喜欢那个，明天喜欢另一个，然后这两个如果它相似性又比较高，那在这个 an 索引上这个向量，这个因为如果有很多个这样的相似的记忆，它都会存到它这个在向量空间中就会非常近。非常近的话，它会不会影响这个采编之类？类的，进而影响索引的质量，就类似于阿里的那个大，很多向量都是在空间上是聚集的这种情况。然后其实我觉得你最后可能还是 filter 得还得用，因为你 filter 是个完全精准的，然后。但是你还不能把之前刚刚说那种带公式的，就是带那种就是权重的时间权重的那种，你完全不用它也不好，因为你全用 filter 的话，有时候因为大模糊、大模型它。回答本身可能又带一点模糊性了。对吧？所以可能最后是两个都得用。就最后又可能变成另一种双重召回的东西，就是你精准的 filter 也要用，然后你用那个时间去作为权值，做一个，去做计算的那个也要用。
刚刚才说那两个feedmap。还有就是前两天那天师兄在群里发了之后，黎清兵师兄说他自己那个使用场景就是 Web coding 的时候，两个版本代码像素非常高，然后他。上下文特别长的时候，就他们就记不住他现在用的是哪一版代码了，就是这个也不知道是不是因为他，这个也可可能是，也可能是别的因素影响，不一定是 an 索引质量影响的。然后就是介绍这个，今天主要介绍了这个 Hippo campus 这个文章，它是发表在 MLC 42026 上，它是一个，它也没，它没有做这个记忆的时序管理它，它主要的创新点在于它认为当。当前基于向量数据库、向量检索和知识图谱这个记忆架构都存在这个记忆插入和检索效率低的问题，因为插入的时候如果是用向量检索话，它是因为这个，因为这个是记忆的插入，它不像 RAG 那个，它是离线都做完的记忆的插入，它是每一轮都要做，有可能是每一轮都要触发记忆的和，就和在用户对话的时候就要进行记忆的更新，或者记忆的记，就记忆的插入，那这个时候实时的就要做。
做 embedding 生成和大模，用大模型来识别实体现在那些知识图谱，它怎么从一个文本段识别出来相关的实体呢？那些实体主要以前是用一些启发式算法，现在都用大模型来做了，所以这又多一步，大模型调用插入的时候效率又比较低，然后检索的时候依赖向量检索，其实我觉得向量检索还好，主要是这个知识图谱查询的时候，它有的时候也要依赖这个大模型来做，就是我怎么知道我该。
张，那天实体有很多边，我怎么知道该往往哪一条跳？他就会大模型，先看一下这些边，然后把相关，然后选一下，我往这边跳，也可能要用大模型，然后他画了一个这个图，就是说现在的这些 agent memory 模块，这个端到端检索的时候，它这个端到端生成说这个检索的这个时间是占比非常长的。然后他的这研究动机就是说不用向量处理。
库也不用知识图谱，他自己创新点，提出了一个把记忆压成一个，一个 010101 那个那种那个二进制串，然后对这个二进制串做检索，然后用这个来检索的时候，他就可以显著地降低写入成本，就是他也不用大模型来识别实体了，然后检索延迟他也很低，然后检如果他这个记忆效果好的话，也可能降低很多大模型在回答问题的时候这个 TOKEN 的消耗。然后他这个文章就是他会对数据做一个这样的处理流程，就是原本的原文本进来之后，他进先进行把，比如说这一段话，他把这个每一他这段话里面进行每个单词进行 TOKEN 的话，就是他是每个都是一个TOKEN，然后他把 TOKEN ID 给他，把这一个原文本表示成一个 TOKEN ID 的序列。
就通过查一个这个 TOKEN 词表转换成 TOKEN ID 序列，然后每一个 TOKEN 它还会生成一个叫2，其实这写的不对，不是 word embedding，它是一个二进制签名，就后面会提到这样，然后这个二进制签名是它这个的，它这个方法里面核心要用的东西就是一个 01010101 串，然后这个 01 串它有个特性，就是说。
在这个就是这个序列上，这个 text 中离得近的它这个相关性，这个 0101 串的，它这个汉明距离要更近一点，汉明距离就是每一位这个零 1 串的一个，一个距离就看有几位不同它的，像它这个汉明距离就是几，比如说一一一和零零零，它的汉明距离就是3，我印象中是这样，就是反正就是零一串这个汉明距离，然后 2 进。
他怎么做二进制签名？他就是首先他这个记忆，他这个源文本输入进来，他会全都排到一个序列里面去，然后把这个序列全部都转成 TOKEN ID，然后他会对每一个 TOKEN 都生成一个随机向量，生成一个，随机生成一个固定维度的向量，然后对每个 TOKEN 都使用一个基于窗口的求和操作，就比如说把这个。当前 TOKEN 前几个和后几个向量这个随机项都加到自己这个向量上边，然后做一个随机投影。
随机投影就是就意思就是如果加完之后，我这一遍历有自己的这个 d 维，一个一共一个d维的向量，遍历每一维，然后如果这一维它是大于 0 的，那这一维就设置成一小于0，就设置成2，就这样一个随机投影，就是大于0，大于 0 就是一小于0，就是0，然后这样就会把这个序列中每一个 TOKEN 都表。
表示成一个很长的01，表示成一个 01 串，然后他说他这样做的话，通过他这个基于窗口的求和操作，就会使得在序列中相近的这个 TOKEN 语义相似度就高。然后所以接下来主要就是接下来说白了就是要高效地检索这个二进制签名，就是当一个 query 进来之后，先生成一个二进制签名，然后去他这个，然后。有高效的检索出来这个和这个二进制签名最相近的那几个记忆的相关的记忆。它是 TOKEN 级别的检索，对吧？因为每个 TOKEN 有一个这种。对对对，向量是，它也是这种外部记忆。我说不是，我说它甚至它不是那个按句子求embedding，它甚至按 token 求embedding。对，它每一个 TOKEN 都是都弄成一个embedding。对吧？对。它是提出。
出了一个叫动态小波矩阵的东西，就是他通过这个数据结构的话，他把他那个二进制签名给索引起来，然后就是二进制签名，他对，拆成每一个二进制签名，就是一个 bit Vector，然后比如说我这个，我二进制签名长度是有，有5，有五位，那他就是五层，然后这个索引的具体里面的这个原理的话。
这就不详细介绍，它就是通过这个矩阵的话，它提这个数据结构，它可以提供三个原语，就是access，就是能够快速地回答出来这个序列中第 i 个数是什么，然后 rank CI 就是在前 i 个位置里面数字 c 出现了几次和 select 操作数字 c 在第 j 次出现在序列的哪一个位置？然后。不是，这个很像那个什么，你知道有一个东西叫做那个 scanit structure data structure，那种叫紧凑数据结构。简洁数据结构。简简对，简明数据结构。它就是一个这样的数，一个数据结构。就是那个东西，那个简明数据结构。对。是，这个不是新东西。然后它主要就是提出了一个双索引结构，这个聚类是它的这个记忆系统中就不存任何文本了，因为它全转成TOKEN，然后 TOKEN 它会每一个 TOKEN 都生成一个这个二进制签名。然后它会构造两个矩阵，第一个叫做签名矩阵，这个签名矩阵里面存的就是二进制签名，然后支持高效的这个签名的检索，就是我拿，我输入进来一个签名，我能快速地找到和它，快速地找到这个相和它相同的签名是在哪个位置。在矩阵的哪一个位置，通过 rank 和 select 语义来实现高效的查找就能够快速地查出来。我比如说我这个签名是在矩阵的第。
第十、第 10 个或者是第10、第 20 个这样子，然后它 Signature 这个矩阵和 content 矩阵它是每一行都对应的，所以它在查出来在 Signature 这个矩阵里面的位置了之后，它又能够对应地找到 content 矩阵。 content 矩阵这个里面存的是也是二进制，用二进制的表示这个 TOKEN ID，然后我现在拿到了这个记这个相关的二进制签名，然后的位置，然后我直接在 code content 矩阵里面。用 access 源语找到那个位置上，它是它的这个 TOKEN ID 是多少？然后再通过 TOKEN ID，那 TOKEN ID 再通过词表再能，就能转换成原本的那个文本了。稍等，我看一下，就说先是把它全部变成TOKEN，就是那中间那一部分就是i。
I win my first video game last.
对吧？对。然后这个地方它会，就是你就是往，再往那边移一下，就是它转成。成了一串，对吧？对，上面这个 content 就是这里，这这个 content 矩阵里面存的就是 TOKEN ID。就是 TOKEN ID。 TOKEN ID，对，然后 TOKEN ID，然后再去找那边的。对，他找的时候不用看上面，就看下边这个矩阵就行，就他通过这个刚我说这个随机索引生，给每一个 TOKEN 都生成一个二进制签名，比如这就是三位的。三位的二进制签名。然后主要是通。通过下面这个矩阵来查。
对，就比如说我这个时候查 II 这个单词，然后我先，我转成101，然后我就通过 access 和 select 的原因，就马上就能查到在这个矩阵里面出现过，在哪个位置上都出现 i 了。这样子，然后再通过一一对应的方式，它这个 content 的矩阵和 Signature 矩阵是每一列都对应的。对。然后它找到 content 的矩阵，说第一个就是101101，它就只要知道。在第一个矩阵里面， i 也在第一列，然后他再通过那个 access 元语就恢复出来这一列。
嗯，他存的是哪个token？是 tokenid 是多少？多少？然后再通过这个词表，然后转换成这个tokenid，它原本的文本是啥？就行了。那他怎么求相近呢？就是相似的话。怎么求相似的？它是这样的，在这个看到这的话，其实完全不支持任何的相似操作，它就只能就如果用户问的用户。
会问题，那个 TOKEN 必须得严格出现在当前矩阵里面。对。如果不出现在矩阵里面，那就查不了了，所以他又实现他做了一个，我这没写他，就他是这样的，他是比如说我这个用问题，比如说这个签，比如说问题是一个没有出现在矩阵里面的，他也会先通过这个 random indexing 先生成一个二进制。质签名假如说生成出来是101，在这个矩阵里面没有，就看这五个没有。然后它会比较汉明距离就是它先，它会对矩阵里面全部元素都进行一个扫描，就是每一个矩阵最近的汉不汉明距离，比如说是它会设置个阈值，比如说小于 2 的，我就接受类似于一个 range KN，就是范围。对对对对，但是还是要把整个矩阵全扫一遍，所以我就觉得这个。
效率不是很高，因为他要把所有矩阵全扫一遍，然后找出来汉明距离。但是未运算。小，对，是未运算，所以这个就，他就说这个是非常高效的，就先通过这个就相当于通过一个异或操作，比如说101，就是给矩阵里面拿当前这个二进制签名和矩阵里面所有的二进制签名都进行一个异或运算。然后再通过一个 pop count，然后看一下这个异或完了之后这个结果里面有几个一，然后看 pop count 就算出来，比如说有3，那这个汉明距就是3。但是这有问题，就是说他最后算出来之后，他找最近的还是找的是TOKEN，就是比如说上面是i。
I was my first video game.
我如果找什么？找什么优。
When the first video game.
那个you。它里边没有，对吧？但是它找相似的话，它可能只能找到几个单词，比如说you，它可能和 i 或者i。对，我忘了说了，就是它这还有一个维护了一个，这个 start index 和 end index。就是这一句话，就这一句话。话，我是在这个矩阵的第几行的第几列到第几列，那假如说我现在而通过他这个查询流程，我查询出来了那个召回到了那个是在假如说第二列，他会查一下这个，我这个第二列是属于，属，属于是哪句话的？然后就把那句话全召回过来。那这样的话感觉更不准了，就比如说 you win the first from first video game 这个就。这可能不太好吧？就是比如说可能只有举，可能举例不太合适，就比如说可能里边你的 query 里面，可能这句里面可能只有一个TOKEN，和这个句子，和这个里，这个句子的某一个 TOKEN 是相近的。但是你就要，你要把这一句话的都给拎出来了，它其实没有，它其实丧失了语义的这个连贯，它那语义连贯就只能是说通过那个 tokenizer 的时候，它转换的那个 TOKEN 的时候，算算那个。对。
window 那个窗口。来决定喽。是的。好吧，我感觉也不是很高效，它只是感觉它是换一种 embedding 的方式。对，实际上它还是用了embedding，对，查询流程，就是刚刚说这样就是先通过大模型先提取用户查询的关键词，然后对关键词每个 TOKEN 进行二进制的二进制签名。然后在那个 Signature 矩阵里面找到签，对矩阵里面所有元素进行都进行一个主位的异或，然后 popcount 选出来符合汉明。应距离阈值的那些候选的。候选的那个就是那些列吧。然后对这些候选使用 select 语言语，找出来他们在序列中的位置是在哪。然后在序列中的位置，然后再通过 Meta data 记录找到他在序列这个位置属于是属于原来哪一段。然后再恢复在那个 content 的矩阵里面，通过 access 语言语恢复出来那个段落对应的每一个TOKEN。但是我有。问题如果说咱们把 embedding model，咱们现在 embedding model 就是出来一个，比如说 float 的，或者说什么 int 的这种一个Vector，对吧？高维的。我直接把它量化成零一，不行吗？我觉得是完全可以。对，我直接给它做一位量化。那不就这东西吗？然后你说精度不够，那我就把那个 embedding model 那个输入变短，以前你是比如说数据优化出来的embedding。我现在输 3 个单词出一个embedding，那不就变个东西吗？ 3 个单词，然后。
So in grammar, sign grammar, sign grammar. So sign grammar from English room, I, I win the first video game, which I win the.
第一个 when the first，第二个，对吧？然后依次做一个 embedding model，然后做成零一的量化，好像也好像和这是一模一样的东西。对，是。对，但是可能比这慢，因为你还是要做一个model。 embedding model。对。它其实我感觉它这里其实就是本质上就是一个非常轻量的 embedding model。对。对，是。对，这个什么举序列中前后。后后前后 w 个 TOKEN 加到自己头，这个项 TOKEN 的向量加到自己向量上。就很像那个，就比较原始那些深度学习模型，那个什么上就是前。对对对。加到自己身上好像就相近了这样。
不是，这个很像NGRAM， ngram 也有点像ngram，但是就是 NGRAM 在编码。对。对，然后实验结果就是他实验，实验做得也比较少，首先就是他做了一个大模型，回答质量的得分就是自己是。自己是最高的，然后除此之外，就 Memos 是比较高的，就是刚刚说的那个比较做得非常完备且复杂的那个系统，得分是比较高的。那 Webj 反正是很差，就那个 Webj 不是那个什么拉。leta。 LETA 对。对。对吧？也没有很差，但是实际它是除了 Memos 和 Memoryos 之外，已经是很高的那个了。但是其他的 a 万我知道它都一般了，所以现在都不有人过来。就是大家其实没比 M0 可能还行， M0 其实可能真不差。就但是 MEM 0 被 MEM GPT 的作者说 MEM 0 很差，就是。他说。 MEM 0 简单嘛。
然后他两个实验结果就是平均的检索延迟，就是，嗯，左边是检索延迟，他是这个红框里的，就是他是检索时间，在整个的时间占比里面，他自己检索所占的时间是非常短的，就检索检，他自己检这个检索非常快，其他时间都是others。others。发这么高的感觉。感觉还是有点问题的感觉。然后就是右边就是问题的平均 TOKEN 消耗量，就是这个就反，也能反映那个记忆的质量吧。就是如果我记忆的做得越好，我们回答每个问题的头号的 TOKEN 量是要少的。对，但是你会发现说你可以看这个东西和漫步 OS 其实拉不开差距，就是从时间上。
左边这个图吗？对。就是你看它上面的放大的小图，它也看，你能看出来。 MO MOS 一点1.2，这1.1，对吧？就这样。然后 MOS 是，它就是个纯向量检索，你看它比它高的都是用的知识图谱，像 Web GPT。对吧？诶，这个 read agent 我不知道是啥。不知道啥，反正，但是我觉得就是知识图谱，那很可能就是说只是那个大模型推理的延迟导致了它升高，就是因为大模型要决定实体往哪条边去跳。你。如果用向量检索的话，感觉其实挺清亮的，就是当然那个 embedding model 肯定会有点时间消耗的，但是他这个也做了这么一通，也不见得说就很省，因为整个其实他做的这个用这种简明数据结构去查询其实也挺慢的。ok。
然后就是一些实验结果汇总了，主要就是看第三行这个 memory 构建时间，它是构建时间非常非常短，别的都最快，这个 Amem 都比较。 0 因为它不需要构建。对。向量检索索引也不需要构建知识图谱的问题。是。然后还有它还有一个就是 memory 构建时的大模型 TOKEN 消耗是0，然后其他都构建知识图谱的，它都需要大模型来。所以它没有比那个增删的场景，对吧？比如说增加记忆删除。对，这就说了，就是它的记忆是 append only 的，就是它只能加它，不能它这个数据结构，可能因为数据结构里面它不能。数据结构不能删。它不能删、不能改，好像。那个不能删，不能改，对。对。这是他一个缺陷，然后第二个就是说他文章，虽然说他没有不用了向量检索，做语义检索，但他实际上用签，用随机顺序生成签名，这一步他还是向量。而且这个他签名的这个质量，我觉得就从他看这个生成签名的这个过程，我也觉得他质量难，很难说咋样。
他那个语义的语义联系应该是和他那个 window 的大小强相关。就那个窗口越长肯定是语义的，那个就是能够覆盖更长的语义。但是有问题，就是你窗口越长，你最后算出来那个语义，那个相似度准不准？因为它就直接往下加一嘛。你，你比如说我弄一个窗口，可能假设 1, 000。这么大一个窗口，那和 100 或者10，你最后说那 1, 000 它能反映到，你能反映就是这个 1, 000 和下一个 1, 000 它俩相似，你说能不能反映它俩语义相似，我觉得很难反映。确实。因为这个里面的这个额外的因素太多了。对，是。然后还有就是他查询的时候，他需要对签名矩阵所有全部签名进行逐位异或和POPCOUNT，不过这个是线复杂度，不过可能因为他这个是按位的操作，可能还是确实比较快。而且他们现在是不是都用那个？就是有一个做 memory 的Benchmark，就那个叫什么low。 lowmo low omo。对，那个东西好像不是很大。他就是在 Lo OMO 上做的。那个不是很大。那个东西就是绝对比不上咱们现在用向量复杂很小的。
对，然后向量检索，它就都是对数级别的实验差检索时间复杂度。而且我忘了它这个里面比的向量检索是啥？向量检索好像是，我忘了它用的是什么索引了，没细看。然后还有就是它既然已经做了那个主谓异或和 pop count 这步已经拿到哪些签名是。符合汉明距离要求的他为啥不直接顺便记录一下这个签名是在哪个位置序列的哪个位置上？我不知道是不是因为这个索引结构不支持这样做还是咋的？如果直接找到这个位置了，那我直接就恢复 TOKEN ID 就行了。那为啥就是也不用 rank 和 select 语再去查一遍了？这个是一个疑惑。然后主要就是这样，就是后面就没有了。行，大家还有。问线上大家同有问题吗？线上同学有问题吗？
志远有问题吗？志远。
诶，你问谁？志远说没有。没有。诶，王珂，我看你也在，王珂是专门做时序的，然后你们那边我因为我知道时序的数据库。和这个时序可能还不是很一样，但是可能有点相似吧。
我感觉关系不是很大。
对，因为传统的时序数据库一般是写，然后但 member 也有写了，就是这个，但是 member 可能更多是读传统时序数据库读是比较轻的，读一般是扫，对吧？
我感觉石旭那边更主要是在 LSM 区域上改，其他的可能不是特别。
因为你们那是写秘籍啊。
但是查的话，时序的查和传统数据库的查感觉没有特别大的区别，就是还是那一套东西。
如果你们是读密集的话。对，是的，因为做记忆的，满记忆的时序查的话，它可能还多一套语义的匹配，就是它的记忆的匹配，它是两套匹配，就是不要兼容这个时间和语义。
对，对，是的，在实际数据库里查一般。因为时间是单独的一列在数据库里，然后它直接一个 where 或者一个什么什么之类的就直接过滤掉了，就不需要说再算向量、算距离，这样，对，是的。
就类似我们这边做filter。是。对，OK，好的，其他同学还有问题吗？大家可以随便说。
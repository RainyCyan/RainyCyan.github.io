好，能看到哈。好，今天我分享的主要是南京大学发布在 SIGMOD 2026 上的一篇论文。这篇工作主要关注的就是带范围过滤的近似近邻检索，就是我们通常所说的rangeANN。目前主流的效果比较好的rangeANN。方法基本上都是为范围过滤设计专门的图索引。比如说先讲两类比较经典的静态索引代表。静态索引方法就是 WST 和IRangeGraph。 WST 它核心是利用线段树来管理属性范围，它给树的每一个节点都构建了一个索引，在查询的时候就是会对涉及到的树节点进行并行的搜索，再合并。但是它有一个明。显的局限，就是说它在搜索的过程中，肯定会不可避免的搜计算到很多范围外的节点。而且它的结构首先是静态索引，所以它其实是定死的，它完全不支持数据插入。
接着是ArrayGraph，它其实也同样使用了线段树，但是改进点在于它试图在图中只访问范围内的节点。它这个做法就是在每一跳导航时都进行一次 logN 的树遍历，来获取候选。这样虽然解决了范围外节点干扰的问题，但是在高选择性，或者是一些合法点比较少的数据集下，这种频繁的树遍历开销也会变得非常显著，所以 QPS 也会被降低。而且跟 WST 一样，它也是不支持增量插入，所以如果有新数据进入的话，肯定要重新建图。
还有一些半增量索引，就是支持后插入的，这种。能有，比如说 rangePQ 和DIGRA。 rangePQ 其实是目前比较少见的基于量化的 rangeAN 索引，它是通过加权平衡树 WBT 来对聚类中心进行，整个进行一个范围分组。它的问题主要在于，它的聚类中心其实是静态训练出来的，所以如果插入数据导致它整个的分布发生偏移的时候，就要重新进行训练，所以说它这个查询精度也会迅速降低。
还有就是， DIGRA 这个静态树，它也是通过静态先构建多路树结构，在每个树节点的内部构建HNSW，所以对于后续的插入，它是借鉴了 B 树的思路，通过节点分裂来吸收新数据。但它这个做法也是有一个问题，就是在节点分裂的过程中，原本的图连接关系就会被强行切断。
WOW 这篇论文里面，它也提到了，就是 DIGRA 也是它的一个基线方法。就是说在经过大量插入之后， DIGRE 它的召回率也会发生非常明显的下降。然后就是两个支持完全增量的索引，一个是 SERF 和HSSIG。 SERF 它虽然支持，它虽然支持增量索引，但是它其实是通过压缩所有可能范围下的 HNSW 来进行的，所以它的问题就是说它图压缩是有损的，它为了减小体积删掉了。
很多遍，这种就容易在高召回场景下陷入了局部最优，搜不到一个合适的结果。而且它也是只能支持按序插入，就是不支持无序的插入，它必须要求数据排好序。 HSIG 它倒是支持完全无序的增量插入，但是它的问题就是说图导航它并没有真正感知到这个范围的限制，这种情况下它也是会计算大量的范围外的节点，所以目前总。
接下来，主流的 range index 方法的一些难点就是它很难同时维持比较高的图质量，而且还在高选择性下有个高效搜索，并且支持完全无序的动态插入。所以说为了解决这个问题，就引入了今天的这篇论文。在这篇论文里面，它提到了一个比较关键的概念，就是 oracle graph，它可以简单理解成，就是我们如果提前知道查询范围的话，只对这个范围里面。的合法点单独建图，那这样建出来的这张图，它就是当前查询范围下最理想的邻近图。就比如说右上角的图，它就是只对范围 5 ~ 15 的点里面的点建图，这样的话，它这个搜索路径里面就不会经过任何的非法点，每一步搜索都是真实有效的。这样的话合法邻居比例最高，导航效率也最好。但是现实的问题就是。查询范围都是动态变化的，我们不可能真的为了每一个查询单独去建一张图。所以现在刚刚介绍那些很多润指 ANN 方法，其实本质上都是在尝试，用一张通用的图去尽可能地去逼近这个 Oracle graph。也是针对刚才提出的这些问题，所以这篇论文它提出了一个层次化窗口图的概念，它的出发点其实也比较直观，为了逼近这个 Oracle graph。
Oracle graph 它的特性。就是在这个图里面，如果两个点相连，那么它们不仅是在向量上接近的，在属性值通常情况下也应该比较接近。所以为了模仿这个点， W O W 它在建图的时候，它不是让一个点在全局范围里面去自由的选择连哪条边，而是会给这个图定义一个固定的属性窗口大小。就比如说这个图里面，它定义的窗口大小就是4。那像这个 11 节点，它如果想连边的话，它就只能在 5 到15， 5 到 15 这。 4 个点里面去选择。这样的话，它只能在这个局部属性范围里去选择向量上真正的邻居，所以这样窗口里的邻居关系会更符合当前范围下真实应该存在的关系。
但是如果只用一种固定窗口大小也是不太方便的，比如说这个图，这个是 Oracle Graph 里面有一条 15 到 14 的边，这个 15 到 14 的边在。在窗口大小为 4 的，为 4 的窗口图里面就是没有的。因为 15 节点它如果窗口大小为 4 的话，它就会包含16， 16 就会比 14 更近，它就会抢占它的边。所以如果想让 15 到 14 这条边重新出现的话，就必须给 15 节点建一个窗口大小为 2 的图，窗口图。所以说这篇论文它后面又又进一步提出了多层次化窗口图的概念，就是它不是只建一张窗口图。
图，而是构建多个不同窗口大小的图层。就比如说这张图里面，它就是举了一个三层窗口图的例子。每层窗口图的大小是 2 乘 O 的 N 次 L 次方， L 是层号， O 是它的层级扩张系数，论文里面它普遍选取的层让层级扩张系数就是4，这个是比较平衡的。这样的话，我们就可以看到它整个图，它每一层的窗口大小都是不一样的。每个图每个点可以连接的。边也不一样，这样我们在后续进行检索的时候，我们就可以把不同选择率的查询对应到不同窗口上，将高选择率对应到小窗口上，低选择率对应到大窗口上。这样的话，查询实际访问到的图也会更接近当前范围下对应的 Oracle graph。这个 WOW 它所创建的多层图化窗口图，对 Oracle graph 的逼近程度也是比较高的，这个在后。据他的实验部分里面也会有证明。另外就是他他设计的这种结构本身也比较适合动态插入。
接下来就是讲一下他具体的实现部分。首先就是搜索部分，先讲搜索是因为他这个底层搜索函数在整个系统里比较重要，后面的动态插入和查询底层都会调用这个函数。像这个函数就是跟我们比较熟悉的 HNSW 搜索函数有一定的相似性。但今天主要就是想讲一下它的区别。 HNSW 它其实只是在最后一层，L、 L0 层进行多领域的搜索。它上面的每一层其实都只是为了快速地去找到接近的范围，每一层只找一个最优点。所以 HNSW 里面它就是上层节点比较稀疏，下层比较密集嘛。但是在 WOW 它它构建的这个图里面，它每一个层级其实都包含了全量的节点，它只不过它只不过。是每一层的窗口大小尺度不一样，所以说它每层连的边不一样。 WOW 逻辑它其实更像是，每层其实更像是一个 NSW 图，它在搜索的过程中，会从上到下去搜多层的 NSW 图。也就是说，它的搜索其实是在每一跳里同时去翻这个点，在多个不同层级下的邻居表。
之所以要这么设计，主要是因为在固定大小的窗口图里面，高层图它虽然说能够跳得更远，但是它噪声比较大，非法点也比较多。低层的窗口，虽然说它连接的点比较少，但是它基本上都是符合它的属性范围的点，所以说它的非法点比较少。这样做法它本质上其实就是想让每一跳都能够利用低层一些比较干净的点去给高层去补位，避免陷入一个局部最优的情。
然后我们可以看，它这个跨层搜索也是有一个层级的区间的，这个层级的区间是用参数传进来的。这个层级区间，如果说我们一直搜到最底层，它计算量也会非常大，所以说为了省资源， W O W 它也是引入了一套早停策略。这个策略其实也很直观，它在每一跳的时候都会检查这个点在各该层的邻居，如果这一层里面的。邻居里面还有非法点，就说明这个窗口还是比较大，不是很适合他的查询范围。他就继续往下探索，去看他在更低层级的邻居。但是一旦算法发现某一层的邻居全部都符合他的属性要求，这个时候他就会立刻触发早停，不会再往更低层、更细碎的层级去翻。他之所以会在这一层停下，是因为他认为这一层的邻居全部都合规合法，代表这个当前窗口的尺度已经比较适配当。前的查询范围，那这一层的路径已经比较干净，没有一些非法点。这个时候当前层它就已经能提供比较高质量合法导航，像再往下搜那些更短的边，就可能比较有点浪费资源了。所以总结下来，它的搜索算法其实就是在每一跳里，在，在它邻居每一跳里，它都会观察各层的邻居合不合法，然后动态的去决定它的搜索深度。
介绍完这个搜索算法之后。那下一步就是它的插入算法，因为它其实是无序插入的，所以说它的构建算法和插入算法其实是一样的。在插入的时候，因为它真正的WOW，它的窗口，它的多层化窗口图里面，最高层是要，最高层是要涵盖它完全的属性窗口的，所以说在插入之前，首先要保证最高层的窗口能够大小能够完整的覆盖它的全部属性集合。那如果不能的话，它就会在这里。先把最高层先复制一遍，然后先提升一个层级，然后接下来再开始进行插入。那整个插入的过程中，它也是分成了三个阶段。
首先是因为我们是窗口图，所以要肯定要计算这个窗口的大小，它是通过 WBPT 数来进行计算的。就是在插入新节点的时候，会先通过这个 WBPT 数确定它在每一层对应的窗口。然后这篇论文的一个创新点也就是它并没有像之前的。按照 PQ 那样，把树结构跟向量索引完全的进行紧密的耦合，而是把 WPT 作为一个轻量级的插件来用的，主要是用来替代一些低效的有序数组。因为这个 WBT 的每个节点都记录了子树大小，所以它可以在 logN 的时间内计算出属性的排名。就比如说这里它即将要把 73 号节点插入，它就会用这个 WBT 数快速的算出 73 这个节点它在每一层对应。
的窗口。第二个阶段就是他要去找他的候选邻居。在插入的时候，他是会按从上到下的顺序，把这个节点插入到每一层中。在每层插入的时候，在他就检索这一层的邻居，他其实是会先检查一下上一层已经找好的候选邻居。如果这些邻居当中，落在当前层。窗口内的数量已经够多了，它就会直接复用这些结果。复用的原理其实也是因为它证明了它证明了一个定理，就是高层次的窗口平均上来说会比低层次的窗口节点邻居的距离更近。而且也是因为高层次窗口里面的邻居，如果说它是符合低层次窗口的范围的话，说明它肯定存在于低层次窗口里面。这样的话，通过这种复用机制，它也能。
能保证节点插入的质量，并且缓解来说，接下来调用搜索函数带来计算压力。只有当上一层留下的合规邻居数量不够的时候，才会进行下一次的完整搜索。那它如果真的不够进行完整搜索的情况下，它就是从最顶层，从 top 层，一直检索到当前的插入层。这里面就是它必须要从 top 层开始检索，这个主要是因为它第一层的窗口通常比较窄，然。然后导航连通性也比较弱，如果直接在低层搜寻邻居的话，它路径就很容易受限，导致搜索就是陷入一个局部的状态。
因为插入的时候需要保证因为插入的时候需要保持图质量比较高，所以说会通过高层次窗口提供一个全局导航的能力，帮助新节点在向量空间中先快速定位，再逐步深入到当前层。然后他利用这个搜索功能找到 M 个邻候选邻居之后，接下来就是正式的建边。首先他这个层次化窗口图，他并不会把刚刚找到的 M 个候选邻居全部都连上，而是通过 RNG 剪枝的方式，只保留二分之 M 条边。这个主要是因为它是一个动态图，在插入的过程中，如果一开始就把邻居表全部都塞满的话，那后续。每次插入就需要频繁的重构和删边，代价会比较高，所以他就只插 M/ 2 个，剩下的话就给邻居表留一些空位，就方便后续的邻居插入。在这一部分，他选好边之后，他就要去维护邻居的表。维护邻居表的时候，他这一步的处理流程就是，如果在这里，如果他邻居的节点表还没有满的话，就直接加入。如果满了的话。就会触发论文里面提到的两阶段剪枝。
第一阶段就是做窗口检查，就是加入一个新节点之后，每个邻居的窗口都会随之变化，然后它会重新计算邻居节点的窗口，然后加了新节点，窗口就可能会把那些已经明显调出窗口的邻居删掉。就比如说这里面98、 98 号节点，它在加入 73 之后， 60 其实它就已经不属于它的窗。窗口了，这个时候它就，因为它的窗口左边界变成72，它就需要把 60 删掉。
第二阶段就是做 RNG 剪枝，就是在剩余的邻域里继续删除冗余边。在这个两阶段剪枝的过程中，其实它有一个延迟剪枝策略，就是正常来说，新节点插入的时候，肯定会有一些旧边不满足当前的窗口范围，这个时候原则上应该是立即删除的，但实际上它也没有。这么做，他选择就是暂时保留一些这些边，因为它本身 W O W 就是一个动态图，它本身范围就不是固定不变的。如果后面插入新的属性值的话，再按照属性排序动态划分窗口，这样的话新点插入，可能原本调出窗口的邻居也会再重新进入到 window 里面。如果说他每一次插入一个新节点，都要重新检查一遍窗口范围，然后去删边的话，那他就会不停。
的删边，重新再连边，整个图一直修改，这样的话维护代价也很高，它更重要的是它也会破坏图导航的稳定性。所以它目前选择的策略就是，虽然很多边暂时，不合法，但是只要它们在搜索的时候还是能够提供有效导航，所以就暂时不删掉它们。因为如果删得太激进的话，图越来越稀疏，它整体的连通性也会变差。所以它选择的就是，如果这个邻居表里面还有空间的话。那就让这些暂时越界或者暂时不合法的边先继续存在，只有当邻居表它真的满了，真的需要放一个更好的邻居，它才会真正的把这些边都剪掉。
这样做的话，插入所带来的影响基本上就会限制在局部窗口的附近，它不需要频繁的重构。这也就是它 WOW 架构之所以能够支持动态增量插入的原因，核心就是在于它。整个索引的维护操作，其实就是有很强的局部性，它不需要去涉及到一些整体全局的，全局的操作。像传统的很多范围过滤索引，它本质上都是比较依赖全局结构的，比如说基于树划分或者是分桶的方法。这种方法一旦有新节点插入，它就会引起节点的频繁分裂、重排之类的。这种情况在动态更新的过程中，原本的图连接关系就容易被破。破坏导致召回率下降。
那在 W O W 它的窗口逻辑下，每个节点的插入以及候选邻居的获取，包括后续邻居列表调整，它整体都被限制在 W B T 划定的属性窗口范围内。这样的话，新节点加入就只会影响到图中局部的连接关系，不需要像那些传统的树形结构索引那样，为了维持性能，进行一个代价特别大的全局重构。这样 W O W 就能在持续的无序插入过程中动态修补，从而维持图的连通性。
这一部分他论文里面主要还验证了一下他的，插入过程中的时间复杂度。他认为插入过程中的时间复杂度主要出现在 beam search，就是他在每一层搜索候选邻居的过程中。根据他。根据它每一层的窗口大小，还有它最高层一定要包含所有属性的定义中，可以，我们可以确定它总层数的话，总层数大概是在 logN 左右。单层的搜索开销，每一层它的节点个数都是 2 × O 的L， 2 × O 的 L 次方，这样的话它就是单层搜索开销就是L。把 top 层都累积在一块，它整体的总体的时间复杂度。
复杂度就是 logN 方，像它这个时间复杂度其实比 HNSW 它的插入时间复杂度其实是要高一点的，但是在实际运行过程中，它这个时间复杂度计算的是最坏情况，就是每一层都没有办法跳过搜索，必须要都搜一遍，没有办法复用。但实际运行过程中还是有很大机会能进行邻居复用的，所以它整体的构建效率也是比较高效。这一点可以看，就是后面实验结果里面，它也测试了。就是索引构建的开销，然后就是检索的部分。在检索的部分中， WOW 它并没有沿用 HNSW 那种固定的从最高层开始搜的逻辑，因为它本身就是建立了不同大小的窗口图，所以说它处理这种阈值查询的时候，它就会直接选到一个最合适的起始层，它论文里。们管它叫着陆层。这个着陆层的概念其实就是他想给这个 range 范围找一个最合适的 oracle graph。他具体选层的时候，就会利用这个 WBT 插件，在这个数里面统计出他当前查询范围内到底包含多少个点，他把这个点记为 N 撇，这个算法就会去匹配一个最接近这个规模的层。它这个匹配就是让这个层的窗口尺度，就是算出来的 2 乘 OL 的 L 次方，尽可能去贴近当前的合法点数 N 撇。
选到的这一层，它实际上就是找到了一个导航连通性和属性，就是点的干净程度的一个平衡点。从逻辑上来看，这一层也是它目前针对当前查询来说，最接近 Oracle graph 的一层，所以它可以直接从这一层开始。去搜，就能够确保他这一层在搜索过程中看到的邻居，大多数其实都是合法的，就可以跳过高层上的一些非法点的干扰。确定从这一层开始搜索之后，他其实也是会，他并不是局限于这一层，他也会继续向下搜，一直，他的他的算法里面是写的是继续搜索到第0层，他继续往下搜也是因为想要利用这些低层图去做一个路。
近的收敛，去除一些非法点。因为虽然说这个着陆层的尺度，它对于范围来说已经很匹配了，但是毕竟还是属于高层，它还是可能会存在一些属性跨度比较大的点，就容易出现偏差。随着层级的不断向下，再继续探索，它整个的窗口也会不断收缩。像最底层下面一些层级的边，它连的都是一些属性比较近的点，这种向下的探索就可以。利用底层属性接近的点，再进行最后的路径修正，最后确保这个搜索路径它可以收敛在最核心的区域上。当然，它在向下探索的这个过程中，也是会用到早停策略，也是不会一直探索到第0层。
在这个过程中，他认为检索，他检索整体的时间复杂度，其实是包含他用 WBT 数去算窗口的。这个时间复杂度是OlogN，但是他论文里面提到，他认为这个不是很耗时，他认为最主要的耗时的部分还是在于在图里面进行探索，所以他把整体的时间复杂度定义成logN，这个 N' 这个 N' 就是他给定的查询范围里面的合法点数，它并不是那个全量的数据规模。像这样做完之后，即使遇到一些规。
规模比较大的数据集，只要它的过滤条件比较严苛，它过滤出来的合法点数比较少的话，它的整个查询效率还是比较高的。就是它的实验部分，它的实验设置，它的硬件环境是英特尔的CPU，和 240G 的处理器。为了全面的评估性能，它主要设置的是两种。也不完全是两种数据集，它设置的是从 2 的- 1 次方到 2 的10， 2 的- 10 次方不同梯度的过滤率。还有一种数据集是把这些不同过滤率的数据都，均匀的混合在一块，构成一个混合数据集。
在实验的部分中，它主要用到一些基线方法，有专用的 RFANS 的索引，就是我们刚刚前面介绍到的那些图索引，还有一些通用的位词过滤，以及它的基础机。建就是前过滤和后过滤，它的基建还是比较全面的。首先第一部分就是它关于索引构建的部分。这个图里面展示的是各个基建方法的构建出来的索引大小，这个也能看到，就是 Acon 它的索引体积其实是最大的，这个也它其实达到了其他索引的 10 ~ 50 倍，这个主要就是因为它构建的是比较密集的HNSW，而且它没有进行。
简直，通过这个图也能看出来， WALL 这一行，它其实它本质上来说并不是最轻量的，就像 SERF 和 RangePQ 它其实都有比它比它更小的索引内存，而且它甚至比单层的 HNSW 都要大一点，这个其实也是由它的结构特性去决定的。因为像平时我们用的 HNSW 分层结构，它是层数越高，节点越少，所以上层的空。间开销就会比较低。但是刚刚也说了， WOW 它的逻辑完全就是不同的，它是每个层级都包含了全量的节点，就相当于它其实每一层都是一个 HNSWL 0 层，所以这样的话，它整体的体积也会比普通的 HNSW 大好几倍。但是如果说就是拿它横向的去跟其他的这些专用索引比，那这样的话，它的空间控制其实也还算合理，它每一层虽然说点比较多。但是他利用这个两阶段，刚刚建图的时候，两阶段减质这个策略。
这个不是，这个是它的点，虽然是每一层都有点，但是它其实只是个标号，点应该是只有一，只有一份，但是它因为每一层都是为点建了很多边，所以它的边很多，导致它空间很大。对，他测的这个 icon 好像是，他的参数设置有点问题，他给那个 icon 选了一个， icon 那个参数是可以调的。就是我的边数有多少，它这里面设了一个特别特别大的一个参，一个边数，所以就看起来 icon 是一个，体积很大的一个索引。就是可以调，可以调，可以把它调得小一些，对。对，他，我觉得他设了一个很，我记得是他设了一个很大的值。
嗯，对。
就是icon，它实际上的，它实际上的，其实索引内存没有那么大，是。嗯，好。他设的是 M 的，到64。对。
那监控它除了。 M 还有一个伽马参数，那伽马参数我记得他论文里 SIFT 是设置是12，所以就会，边数就会乘个12，就非常非常非常非常大。
就 WW 里，他那个论文里面说， Chrom 他设置的是1024，因为他说他应该设置为 range 的选择度，所以就导致他的体积特别大，而且他的效果很差。对，他应该是故意把那个参数设置得比较差吧，他，他实际上就一般来说应该。是 32 左右。
它后面好像 icon 的性能也不是很好，就是它好像参数可能设得不是很合理。
对，它过于稠密其实也不是很好，它过于稠密的话，那个图就是你像它如果是个稠密图的话，它就跳不动了。对。
嗯，好，那我继续，然后就是索引构建效率这。这块，构建效率的部分，就是SERP，它是，它的测试过程中， SERP 其实是在这种简单数据集上，比如说SIFT， ARCV 这种数据集上，它的是最快的构建速度。但是因为 SERP 它采用的是有损压缩，所以后面的实实验部分中，它在它的召回率其实不是很稳定。就是RangePQ，它的表现就是它对维度会比较敏感，它在低维数据集上的构建速度。是比较快的，但是在高维数据集上就差很多了。嗯，不对，应该不是。他是。对，他标签都是自己生成的。
这里的，这里的 archive 数据应该是不是咱们用那个吧？咱们用那个是 label 的，它是，它应该是还是随机取的点，然后做的就是合成数据。对，OK。
iCap 数据集，它 range 是真实的，它有 range 也有标签。对，他用的是真实的。是的。
但他用的。
对，我知道那个，但是他用的那个是那个吗？我也想，特别想知道。嗯，OK。
然后就是在 GIST 和 Wikidata4M 上面，这类比较困难的数据集上，那 WOW 它的构建速度其实是比较快的。主要是两个原因，第一个就是说刚刚我们算出来的它那个时间构建的时间复杂度，其实是最坏情况的时间复杂度，它的这个时间复杂度和主流方法的一些平均复时间复杂度其实是在同一水平的。
第二个就。就是说构建过程中，它候选节点的选取，候选节点探索，其实效率也比较取决于图质量。 WOW 它前期构建出来的图质量是比较接近 Oracle 图的，所以它构建过程中的搜索操作会更加的高效。另外就是它在 WOW 它在它在构建的过程中使用了顶点，使用了顶点级的细粒度锁，所以说它在 16 线程的加速比可以达到 5 到 6.9 倍，它的并行扩展能力还是比较好。这个就是它的查询性能分析。
哦，首先先主要看第一列吧。因为第一列是它的混合负载测试，就是刚刚做的不同选择性的混合数据集。在混合数据，在混合数据集上测试，它的查询性能其实是远超其他的对比索引的。在达到 90% 召回率下。它的查询速度会比静态的索引 Irange graph 会快 1.4 倍，比无序的增量索引 HSIG 快了大概 4 倍。像它这种动态索引，如果能达到这种静态索引的性能水平，说明它整体的动态插入策略是比较有效的。它还做了一个有序插入的实验，就是 order 的版本。这个 order 有序插入的实验可以看到，这个有序版本，它在 sift 数据集。上的表现会稍微好一点，整体的会比无序版本会更高一点。这个也是因为有序插入下顶点的属性窗口会更加稳定，就可以减少一些剪枝操作。无序插入的话，窗口频繁变动，它就会触发剪枝操作，有可能会损失一部一些高质量的邻居。
右面这些介绍的就是不同选择性从 2 的- 1 到 2 的- 10 次方的表现。当过滤。条件比较宽松，就像 2 的- 1 次方的时候，它各索引其实差距也不是很大。但是在过滤条件比较严苛的高选择性场景下，比如说这种 2 的- 10 次方，它这个IRangeGraph，它因为每一跳它都要经历线段树遍历，所以开销会整体会比较大。像ICRF，它它因为有损压缩，会容易丢失一部分范围内的邻居，所以它们整体的效果都不是很。很好。然后就是在在 GIST 和 WikiData4M 这种向量分布比较稠密的困难数据集上，它图质量会对性能影响很大，所以 WOW 和 IrangeGraph 这种依靠高质量，依靠高质量的图，依靠高质量的图的效果会优于其他的方法。就比如说 GIST 可以很明显的看到， WOW 和 IrangeGraph 它们两个的。它们整体的效果会比其他都要高很多。
在 SIFT 这种分布比较稀疏的数据集上，其实它整体的效果就是差的不是很大。它各个机制的一个深度的分析，首先是这个图质量的分析，它是用这个 DC 指标来，来测量的，它主要代表的是向量距离计算的次数，因为在向量检索里面最耗时的就是算距离。所以这个 DC 就可以直接反映出它的算法的效率。像这个图里面就是，它这个红色的实线就是 WOW 它的，我们可以看到，这个黑色的虚线就是，黑色的这个线就是，理想的 Oracle 图，可以看到，在这个图里面基本上它们两个都是几乎重合的，说明它这个导航也是比较精准的，没有产生什么多余的计算开销。
这个也可以说明，虽然说它 W O W 是用无序增量的方式去构建出来的图，但是因为它采用了两阶段剪枝和延时淘汰策略，就可以使得它最后形成的图结构，已经达到了已经达到了他们大家都想要逼近的 Oracle graph 的理想水平。而且在召回率，就是已经达到了 95% 这种高水平区间下，它也没有产生太多的距离计算。
后，这个是对于早停策略的分析，我们可以看到上面这个图，它下面有早停策，上面是有早停策略，下面是没有早停策略的，它整体的就是把早停加入早停策略之后，它整体的 QPS 都有显著提升， DC 就是它向量计算次数也会有大幅的下降，比如说 SIFT 数据集，它整体的提升还是相对来说比较明显的。
这个就是这张图片，就是。进一步证明为什么它的 QPS 会提升。图它主要是，它主要是证明在每一跳当中它的探索的层级。像有早停策略的情况下，它基本上就是在 6 到 8 附近，它向下的话，也就是向下探索 1 到 2 个层级就可以了。像没有早停策略的话，它基本上就会向下探索到 4 到 5 个更深的层级。像如果没有早停的话，它的算法整体上就会。会为了更进一步的提升，去底层窗口去进行一些属性检查和计算。但是你开了早停之后，就可以发现它当前层级的邻居其实已经合规了，这种的话再向下计算其实是没有必要的。最终早停策略可以减少一部分的冗余计算，也可以降低一部分的检索延迟。
就是，这张图片它就是，算是为了验证它，刚刚我们说的。检索探索他选择着陆层的准确性，我们可以看到在不同的选择性情况下，他都找到了他自己比较合适的选着陆层。像图里面 2 的负 7 次方的话，它对应的着陆层就是 9 号层，也可以看到实际上 9 号层它的 QPS 和 recall 相对于来说也是比较高的，这个就说明他这种通过WP。 BT 预估过滤率来决定这个搜索起点的方法，是可以准确找到保证图连通性和又能最大限度规避高层噪声的平衡层，说明他找这个着陆层的策略还是比较有效的。
最后就是总结一下这篇论文，它的主要贡献其实就是在构建机制上，它可以真正做到无序的增量插入，它可以。可以从一个空的索引开始，拿一个点插一个点，它不需要像 DIGRA 那种，得先拿一批数据建一个底层索引。它可以真正规避一些无效节点计算开销，就像刚刚判， DC 的测试，它其实是基本上没有一些无效节点计算开销。它论文也提到，它论文里面也提到了，它其实是目前比较少的同时优化过滤。
检查和距离计算的一篇论文。它其实是支持任意范围的大小过滤的，它其实不需要像那些树索引一样去进行距离的合并，它只需要给它找到比较合适的出入层就可以了。而且它整体的图质量是比较接近于 Oracle 标杆的。我觉得这篇论文它其实可以，它其实比较设计的逻辑其实比较适合 agent memory 场景，它智能。体的记忆，随着对话会不断产生，这种情况就比较需要随时进行动态的增量插入。而且像智能 agent 检索记忆的时候，它比较常用的过滤条件也是时间范围。那像 W o W，它就可以通过划分窗口的功能去处理这种时间戳这种属性，这种会比传统的层级结构要快一些。然后它其实也是有一些问题的，比如说它现在还。还是只能做到一种属性，就是它不支持多个范围属性同时过滤。而且因为它现在占的内存还是比较大，所以后续可以，其实可以把它放到 GPU 或者是 SSD 上去做，这样的话它底层的一些窗口图就可以放到磁盘里面，然后像内存里面就只放 WBT 和一些高层的图，这样它的检索也会快一点。
好。诶，琪琪，你要不说说，你不是最近，咱最近看 agent memory 的东西，你给延嵩说一说你的看法。
这个WW，它范围过滤的话，它查询是一个会携带一个时间范围，会携带一个 range 范围，是吧？每个向量是一个标签。对 agent memory 来说，可能是反过来，查询是一个只写。携带一个标量，但是库里面的每一个向量，它是要携带一个时间范围的，这两个正好，正好颠倒过来。
我们在 agent 里面， memory 里面查询的时候，是想要查一个命中当前时间戳的那些向量，命中值就是每一个向量它有一个时间范围嘛，它如果这个时间范围正好覆盖了查询的这个时间戳，那就是命中了。所以然后。也有，去年 ICD 也有有一篇时间戳图像量检索的文章，它的核心思想跟这个 WW 这些 range filter 是 range filter 索引的思想是类似的，也是会为你这个 WW 就是为每一个，每一个点它会构建多层，每一层都是不同窗口大小的一个边，相当于。
查询时会动态组件这个边，是吧？去年 ICDE 那篇时间戳向的检索文章，它也是会为每一个点去存储在不同时间戳下，它的历史版本的边是怎样的？当一个查询携带一个时间戳过来的时候，这个索引，它会动态的恢复出来图上的，在该时间戳下哪些点是有效的。它的哪些边是有效的？想法，构建思想也是比较类似的。
在 agent memory 里面，现在比较关注的是一个双时态的查询，这是 agent memory 里面的一个特色，它既要关注事务时间，也要关注事实时间。查询相当反映到向量检索里面，就是每一个项查询向量，它会携带两个时间量。两个时间戳，库里面的每个向量要是携带两个时间范围，在这方面的话，目前还没有相关的研究，可能就是需要优化一下，也可以用用 fi- filter 这个技术来做。差不多就是这些。
对，其实如果是做单个时间戳的话，也不是不行，就是刚刚琪琪说的那个。是稍微复杂一些的，它有个双时代查询的问题。如果是只是你对这个记忆做一个单个，它有一个单独的，就一条记录，就是时间，这个时间可能就是这个记忆发生的这个时间。你召回的时候，你肯定是说我既要语义相关，又要时间接近嘛。你用这个WAV，那肯定是可以的，但是可能就是时态设计上会稍，对时态设计方面就会相对简单一些。同时你也不能给它完全规定一个。
硬性的过滤的一个范围，比如说我就给你规定说我就要检索最近一天之内的，这个肯定是不太好的，就万一说，因为 agent memory 这个是一个相对智能一点的场景，它肯定是不能有一个硬性过滤条件。如果用 WW 的话，如果你折中一下，用这个 WW 这种 range 的filter，我可能一个 query 它要，它这个查询的这个条件会变，会变一变，就是它可能不只是说是，它可能就是假设说我最近一天这个返回的。
TOP 可以是查TOP10，假设，我再往后，范围扩大一些，最近 5 天查TOP5，最近 10 天查TOP1，比如说这三个 query 下去之后，综合上，综合的我找到了比如十几条相关的记忆，我对这十几条可能再做一个重排，返回给用户一个综合的时间和语义的一个结果。这个就是我大概，我刚。想一下哈，就是如果是 agent memory 处理的话，可能就这么处理，跟 WW 就这么处理的话。就是稍会相对来说，这个会符合它的一个，确实会符合一些它的查询的一个模式，会会有一些相似。我其实更感兴趣说，如果我们把这个东西放到 GPU 上加速会怎么样？我不知道思桐你之前想过吗？
嗯，对，因为它现在底层都是放在 CPU 上去做的，然后它的加速就是用 SIM 的加速，就可能建图的话有。建图也会快一点，它现在只是用 16 线程加速。
对，我说如果 GPU 我们还在查询，你想我们之前做 GPUAN 的话，其实没有支持 range filter 的，比如说 webflow 它也支持 label filter。因为没有说对，因为在 GPU 上没有单独的说做一个 filter 的图， filter 索引，就单独设计一种新的索引放到上面去去做加速，所以我在想，把这种 CPU 上的图搜引搬到 GPU 上，可能一开始的效果就是它基线搬上去肯定不会太好。
对，它那个查询的时候，里面会有一个多层的查询，可能可以放到 GPU 上，就是每一层去做。
对，可能放 GPU 上，我觉得是有一些可以做并行化的空间的。对，因为我之前做的都是 label filter，其实可能 range filter 可能还值得去看，也可以去看一下，可能会是一个比较有意思的工作。诶，我看源辉刚。源辉，你在吗？你觉得呢？
哦，我在这。我看这个内存膨胀是不是有点重啊？可能需要优化一下这个跟显存容量相关。
它怎么说呢？你看相对的膨胀的比例，看你的点的维度有多高，它边就只有这些了，对，如果点维度很大，点的维度很低的话，很高的话，那其实。可能还好，就比例，膨胀的比例可能还好，对。对，您可以做一些。
对。它的它的膨，膨胀的比例是不是跟你数据本身它的就是属性的跨度也有关系啊？如果属性跨度很大的话，它的膨胀会出很多层呀。
嗯，对，它整体，它最上面那一层就是要覆盖整体的属性跨度，所以如果说是跨。
哦，OK。诶，那目前他测出来就是概是在哪个级别？是 10 层，还是还是说可能会出现一个上百层这种？ 1000 个。这块。
嗯，他自己设定的是 1000 个属性。
我们是我们之前测试的时候就 14 层，对。
就是它的层数就是 log N，因为就假设 N 个点每个属性都不同嘛，所以它最多就是 log N。假如说 11 条的话，也就顶多就二三十层，不可能再多了。
哦，那其实还好。对，膨胀个二三十倍的话，现在我想一下啊， 50 GB 之内的估计能装，能装。
十十这个量级吧，对，嗯。
就它最多一般大概两三百条边吧，假如说原始向量是 1000 维的话，那其实根本没加多少内存。
嗯，对，他这可以考虑做 GPU 上这个。
对，在这这样可能检索的算法就可能得改改，重重点就。改变形算法呗。对，定流就可以换，可以给它换掉。
是他。它的很多并行逻辑可以给它换掉。另外这个数据是不是还有什么跨层啊之间的一些共享？比如说你可能有一些数据跟，就是这种非过滤的逻辑不太一样的，可能还需要改一改。
嗯，对，它跟 HNSM 的搜索其实不太一样，它是跨层去找邻居的。
嗯，对，这可能要改。
对，但是。
一般是找。连续的三层，一般连续的，两到四层吧，这样。
对对。
是这样的，所以我觉得可能比较值得去做 GPU 上的， GPU 上优化，因为 GPU 上感觉做 label 过滤的很少，工作非常少。对，就是唯一找了一篇那个外 flow 还是做 label 的，他 label 也做得不咋地。
嗯，这个是。
直接把上面那几层放到 GPU 里面，那下面那几层放内存，是不是就是就有比较好的加速效果了？因为我看刚刚那个实验图里面，大部分都是访问 6 到 8 层，就是把这几层都放到 GPU 里面，不是只有边吧？每一层只有。
它这个每一层都是全量数据吗？
嗯，对，每一层节点是，编，每。
每一层只有边，点肯定只有一份了。
哦，比如。说，如果我相邻的两层它它都算到了某个点，那我能知道，比如说我在第二层，第一层可能已经算过它了，我这里可以跳过吗？
我记得他应该是，嗯。
他那个不是这个样子的，他那个是，比如他是把两三层的边凑起来，一起算点点，就是根据边的编号找，就是他，对对对。
哦，他在算的时候，把那个边都合在一起。哦，我懂我懂。
那这个并行可能还得考虑一下，可能跟我刚开始想的这个算的逻辑不太一样。对对。
嗯，但是我觉得可以去看一看。嗯，应该会比现在的 baseline 都好得多，因为现在做label， GPU 的 label 那还是前过滤后过滤，还没有其他东西。做range。
对，我觉得延嵩可以后边看看，其实。可以想想，大家还有其他问题吗？
嗯，好。
行，我们就是WW，我们其实做了很多，包括志远，志远对他很熟，我们都志远、卓然他们做过这个方面的东西，但是我们一般是做的，我们把这东西改到了 disk 上，去去测了很多，包括志远，还有德嵘，他们还做。做个插入的东西，也是针对这个东西做的。如果说咱们可以把这个东西放在 GPU 上的话，可能也可以搞一下，具体的一些算法细节，他们也都比较熟，我反正到时候我可以再商量。行，如果大家没问题的话，我们就先到这吧。
好。

---
draft: true
---

我感觉就是陆陆老师想把他们那个判分做成一个很丰富的。然后所有人都往上加。反正思路可能也跟，对，其他的，反正 filter 的思路不太一样。但是哎呀，这实验看不出来啥。东西可能得自己跑。嗯，他是只比了比较基础的baseline。嗯，他那个 baseline 都是自己写的，所有都是自己写的，也没跟跟这个内存的比，所以不知道具体什么情况，我感觉实验写的反正问题很多，这个实验。应该就是半成品的原因。
主要他们的出发点就是说你类目太多了，只能放 400 个。嗯嗯，后面一切都围绕这个来开，行，咱要，咱们开始吧。因为其他本科生他们都考试去了。哦，对。对，首先是一些相关度，就是目前的 SSD 的这个最近邻最近邻解锁。一般都是在构建索引的时候，把它分为内存跟 SSD 上两部分。 SSD 上大多就是存每一个 record 大致就是存向量，还有 neighborhood 两部分。除此之外，如果带lab，当然 SSD 目前没有，带 label 的比较少，就可能带一些其他的信息。目前 SSEANS 大概就是 discn 拍。
为 n starling bamg，还有这个今年的 G 开头的他们大多数都是在内存中放的是量化，不能大多数，全是在内存中放的量化的向量。有一些像 starling 之后的，有一些块内有排序，方便提高cache，还有读取的一些东西。搜索阶段有一些也把搜索分为。
两阶段，一开始的扩展候选集，还有后面的重排阶段。既开，有一些 SSD 的 ANS 就在扩展候选集阶段只使用，直接就不读取 Vector 了， Vector 的信息了，因为它有一些可能写的点面分离啊，或者写了一些，对这块内的 record 进行了一些重新编排。
诶，咱没有人说话吧？我简单说，诶，卓然，因为周四要开题报告了，咱们开题那课件，你和志远那还有更新和补充吗？
老师，我们具。边应该暂时没有补充了。
嗯，好的好的。咱们今天晚上报告会开始了吗？哦哦哦，对对，我觉得我看前面是有那德嵘，德嵘掉线，是吧？好的，嗯。
开始了，他应该是掉线了，我现在打。
啊，对，刚刚听到哪了？不知道啊，我问一下。刚才。 Ppt 讲.
Set on the open.
不好意思，我也是刚听，我不太确定你刚才讲到哪了，应该是这章是没有讲。
这边。
Oh, sure. Yeah, for really easier, never approach the.
对对对，就是常规的，内存版的。过滤它都是直接确定性的，你这个向量在范围内你就有效，不在范围内你就无效。或者你有标签有这些个标签就有效，没这些个标签就无效。他这个论文就是用一种推测性过滤的方法，他用用这个函数，他直接就用了一种推测性的方法，如果这个。函数返回的返回值是False，那这个向量它一定是无效的，一定不在范围内。但如果它是 True 的话，它可能有效，就属于我这个需要访问的集合，就 infiltering 跟 prefiltering 访问，需要访问的集合。所以这个就有有一个东西叫什么 no false negatives，就翻译叫什么假阳性。然后它的 tweeting 过滤是用于 prefiltering 跟 infiltering 两个阶。阶段的。 prefiltering 就是他用这个 is member approximate 之后，他对与操作进行优化，进行额外优化，进行一些额外优化，就只读取稀少的标签，对频繁访问的标签，最后再筛选出，最后再过滤出去。
inferring 就是，如果是近似，可能有效的向量，那么我就在图访问过程中，我就直接访问，然后到最后再筛选出去就行。然后他可能就是他提高了几率的严重性，但是可能会引入一小，就引入一些额外的 SCD 读取。诶，这块就问问延嵩，因为咱们毕设其实做的就很像这个东西，但是我们好像就完全都没有什么true，我们就是完，我我是认为所有的向量都是true，然后先进入开的list。对吧？然后最后再筛到那个结果集里。诶，你在吗？对吧？是吧？我，可以，OK。就是这个就很像我们之前毕设做的东西，但是咱们就没有做一个，没有做这种概率数据结构，当时是把所有的都认为是，先算距离嘛，最后距离算完之后，最后。
在，能听到吗？嗯，对。
满足那个具体条件了之后，进入结果集的时候才会真的去看一下标签是不是满足条件的。对对，其实很像。就是它的整个 master 的部分嘛，就分两个方面，一个是之前说，它是混合策略嘛， in filter、 post filter 跟 pre filter，它是分段处理的，所以它进行了一个定量分析，就分析我哪个部分这个开销大，开销。要小，我用开销最小的部分，我在一个查询到来前，我先判断是该使用还是 post 哪个方法，它对每个方法都会，每个查询都需要进行一些定量分析。
对。
第二个就是，我实际上是怎么使用这个推理型过滤进行进行图的访问的，对于 pre 跟 in filtering 进行优化的。整个流程就是这个图，首先是 index 结构，它这个 index 结构跟 pipe n 有点区别，它虽然是叫 pipe n filter，但是它没有导航图那个部分。它的，它所有的边信息，还有索引信息，所有的边信息都是直接从 SD 访问的，没有在内存里有catch。他论文是这么说的，但是我不知道，论实验里是。然后P，然后上面 DRAM 相当于只有只有量化后的weight。对，他论文中是这么写的，也没，他也没说，就没说有导航图，因为派班应该是导航图嘛。但是也无所谓，就是可能也无所谓，因为他是不是还是点面在一块？对，就，他他还有这个。
二跳。两两跳的邻居。对。对。就对 infiltering 采用这个两跳的，就其他的就不需要这个两跳的。其实也可以，直接存两倍，我一部分空间存这一部分，另一部分只存这一部分。就直接把两跳给抛了。它是分 VectorIndex 跟 AttributeIndex 两部分嘛， AttributeIndex 就是直接用来判断 filter 的。然后也是分 label 过滤跟 range 过滤两部分。然后 label 过滤就是它内存里就保留一个布隆过滤器，然后上面这个标签的技术是用来方便 cost estimation 的， estimation 的，就用来方便每个查询前判断选择策略的。然后它的 label 的 SSE 上存的就是倒排索引，直接就存倒排索引了。然后润值上这个这个润值的这个处理方法，我觉得不太好。他是放了一个直方图的东西。就是他开了 256 个桶，就相当于一币就能存下，开了 256 个桶，把每个 value 分到这几个桶里，然后这么粗略的筛选。那他不得准。对，就是很不准。他就很粗，粒度比较粗。
然后 inferring 阶段就是，之前就是我直接判断，我遇到不在范围内的，我就直接不走，在范围内的我才走。这个就变成了，我不在范围内的一定不走，但是在范围内的时候我再走，当然这个在范围是可能在范围内。然后这个候选集肯定也会有相关变化。那他就扩大。那他就 2 到 0 啥时候用的？就是 infer infer 的时候，就用2，就用2。对，只有 infer 用这个2。
二跳邻居。二跳。就是收集够 R 个点。就是 infer 的时候，就是不管是一跳还是二跳，所有的邻居都判断一下是不是在这个满足这个缩小。他这个说的好像是就这个点，我有个二跳邻居。我访问的时候，我访问到这个点了，我就访问所有的二跳邻居，把所有对所有的二跳邻居进行判断。那就是，就是一跳和二跳都得判断呗。对，一跳二跳都得判断。不会零跳。明白。那好吧，那他确实可能不把。把边放到内存里边是有道理的，他可能觉得这个东西太占地了。确实。然后就是之前说的开销估计方面，他给了这么一个表，让你区分什么时候该用什么策略，是 pre in 跟post。他是，因为我是有估测性的，这是推测性的一个策略嘛，所以他就有两个放缩，一个是选择性的放缩。
对于。选择率为 S 的时候，我需要探索 L 除以 S 这个向量，我才能找到 L 个有效的节点。另一个就是精确度的放缩，如果我的候选集大小是L，我需要我需要 L 比 P 个候选集，我才能搜索到原来 L 搜候选集的， L 个候选集。那 P 是啥？ P 是精确度。啊，精度。对。这个精确度就。
就是假阳性的里面，就假阳性的率吧，就应该这么说。对于第一选第一选择性的节点来说，它的假阳性节点都用于桥接，增加了图的连通性用了。如果选择率比较高的话，它返回池，候选池里是可，就插的假阳性点就比较多了。就把真的挤掉。对，就把真的挤掉，这个。所以他们，他这个计算的时候是把低选择率跟高选择率分开的。这个开销估计的时候，他总的开销是， L 时间加上距离计算时间嘛，他把这个，他把 L 计算时间， L 的时间，权值拉得比较大，他说是，他本地测的是一次 L 大概等于 10 次compute，所以就给了。
一个 10 跟 1 的权值。 LabelFiltering 的，然后就是 Label 的Filtering。 LabelFiltering 它怎么判断怎么判断一个节点它是近似有效的？它也是区分为这个，这个 Label 比较稀有的情况跟， Label 使用率比较低跟 Label 使用率比较。较高的两个方法，就 level 使用率比较低，那我就查询前，就对这一个查询，在查询前我把这些个这个 level 先直接从 SSD 的 attribute attribute 那 index 那一部分直接读取到内存中。然后对于这个访问率比较高的，那么直接用布隆过滤器就行了，用布隆过滤器筛选就不直接不读 SSD 了。
那个label， label count。这是为了算这是为了算那个什么的，为了算这个开销的，对，为了算开销的。然后是他，为什么这一部分可以用布隆过滤器？他的解释是，我记得他的解释是说这一部分，他这个假阳性节点，他占比比较小，他有这么一个，然后再加上这个布隆过滤。
器在上面这个阶段的时候，它那个哈希冲突可能比较多，在上，在这个少部分 label 的时候，就少选择的例子 label 的时候，它就直接不使用布隆过滤器了。嗯，就比较少，它就一下都读出来。所以这个整个 label fishing 就是，还是刚才这个图， prefiltering 就也是直接读取 SID 获取倒排索引，然后它。
它的另一个优化就是对于 and 进行优化， and 优化的时候我就直接忽略这个高选择率的节点，因为低选择率的节点它这个集合，因为选择率本身就比较低了，只有低选择率时候才使用这个prefilter，所以它整个集合就比较小。整个集集合比较小，我最后我筛，因为是 and and 方法嘛， and 位词嘛，所以我就只选其中一两个它，其中几个，那么它的这个。这个超级就也很小，那集合就也很小，就到最后很小的部分再单独单独处理那些这个没判断的 label 就行。然后infilter， infilter 跟之前的区别，跟这个严格过滤的区别只有这个推测性过滤，推测性的那个函数 is member approximate，那个区别。然后也是根据 real real label 跟这个 frequent label，两部分访问，就 frequent label 就 frequent label 就直接用布隆过滤器了，有 real label 就再从 SSD 读。
然后这个 range filter， range filter 就是它把整个数值直接压成1B，压成1B，为什么又压成1B？因为 1B 可以开 256 个桶，最后它把所有的范围，所有的数值都去开到这个 256 上，就变成一个直方图了。我判断是否近似有效，我判断它这个桶跟查询的范围是否有交集就行。OK。当然这个也，因为它就 256 个桶，它这个，反正就是误差可能很大，如果找个笔画一下。多开几个，变成两笔。笔就开开笔就行。它首先。这个 prefiltering 就不说了， infiltering 阶段它肯定是大概就是 1% 以上的时候才用这个infiltering，大概是这么个区分。那么是 256 个桶，这里是大概是 1% 的 1% 以上的选择率，我这个 L 到 2 是选取，这么选取。它这部分跟这一部。部分都是被误判了。其实都误判是，你桶越桶越，桶越粗，桶越桶越少，那误判率就越高呗。对。对吧？你除非你开得非常非常多，非常非常细，那个桶非常非常多，但是但是那又大了嘛。对。对啊。那 range filter 如果只是一个数值的话，是也不太用这样，因为直接放SMDV。其实也行啊，对啊。对啊，这个反正就是他提这个 range filter 可能就是为了通用性考虑。就它这一个系统能处理所有的filter。是，那有问题，就 range filter 的话，它不会那个刚才判，选择度判断函数不是又得变？对，所以，对吧？所以论文它它这个 level level filtering 的选择度跟精度估计跟 range filter 的选择度跟精度估计，它是有区别的。
这又两回事了。对，这又两回事了，它反正就有小部分数学公式吧。而且它是，它是完全依赖于这种概率数据结构去估计它的，它要做前过滤、后过滤还是中过滤？对。对吧？所以他，反正就是可能想的角度就可能比较单一，我觉得。我都觉得不如直接直接离线测个数，然后直接直接用那个阈值去做就完事。因为你反正是静态的。或者就直接批量。对啊，因为你反正是静态的，你为啥不测个数？你测个数，你看到底哪个哪个性能更好，哪个功耗高，哪个性能好。那可能是，就，说证明他这个模块兼具那个 profiling 的功能。你看，你看Unify， Unify 他就是，他就是说我，他就不回避，他说我就 hybrid factory，我就。直接测出来。就就之前。到这怎么测？之前那文章不就是低于 1% 的时候就直接这个pre，然后高于百分之几十来着，就直接post。对啊，你直接测。直接静态就行。你直接静态可能就完事了。
然后他这个可能说是这个计算的时间可能比较少，可能有也有这个考虑吧，就说计算时间比较少。但是离线也不费啥，反正就是他可能觉得比较麻烦吧。然后实验。咱就测了这三个数据集。我看的跟常规的数据，常规的 label filtering 跟 range filtering 数据集，这个用的都比较少啊。YT5M。它前两个是真实的 label filter 数据。我记得是它的la，它的 label 跟 range 都是真实的。 label 跟 value 都是真实的，但那个 query 好像后面 100 兆的不是真实的。 100 兆是不真实的，前两个它是它真的吗？YFC。 CC 还 YT5 啊，YT，他俩，我记得，他俩有开出来他的，开出来他的，没有。我不知道是他论文，他论文是这么写的，就是直接说是真实标签，然后还专门说了，说是 YT 五五 M 还是这个 YFCC 十十M，他有一个是这个标签特别多，内存占用特别大，你这当时就有这么一句话。然后他也没给链接，是吧？对。嗯，明白。然后它的 baseline 就是下面这几个。一个是pipeline，它用 pre 跟 post 写的，高于1%，低于 1% 就用prefiltering，高于，低于 1% 就pre，然后高于 1% 就post，但是没有in。没有in，所以它 base filter 的效果很差。
然后 muwas 我看了一下，是好几年前的。muwas，它也，它就走了一个 cost model，然后去选要是。是做 preheat 的post，他好像也没有印。没有印。对。然后他好像还是单标签的。对，反正他，因为他们是工业级的那个引擎，他们做的就是尽量的要简洁一些。
然后另一个就是这个filter。OK，那经典。就就就 in filter 了。经典没错。然后这个整体表现就就都说很好，但是他这个 notecy 上这个10。这个YFCC， YFCC 这个表现比较一般。 CC 是。然后它的解释是， YT5M 效果很好，是因为它后过滤的，是属于后过滤数据集，后过滤的比例比较高， 70% 多的 query 都直接走的后过滤， post filter。但这个就有个问题了，但是， post filter 它就不是，它不是自己写的，就post。filter，就他这个方法没有写 post filter 的修改。对啊，那他。这这，这个论文里写的就很奇怪。不是，那还有问题，就是他的 query 不都是他自己合成的吗？应该是。对啊，那他为啥要合成一个走 post 这么多的 query 呢？诶，你不是说那个 YTM 5M 是真实的吗？他 label 是真实的， query 不知道。 query 不真实。对， query 他没说。也也可能有吧，真实的。
10 兆。然后 10 兆， 10 兆这个效果跟这个 base filter 差不多，它可能感觉像是写反了一样，它说这个效果一般证明混合策略选择很有效，但感觉应该是写反了。就后面这个数据可能这个后过滤选的比较多。嗯，那个，那有可能。然后就是这。这个，就大规模的数据集。那大规模的label，它它自己是，它自己生成，它自己生成的那个 label 呗。应该是。那那它自己生成label，它得生成多少个了？它它它有说吗？哦，我好像有说，但我没记太清。它应该也很多那种。嗯，它的润值我记得了，这个润值它说是好像是图片宽度还是什么来着？像素宽度什么，反正是这个润值的那个 value 是真实的，但是 query 也是构造。就 0.1% 到50%。
真实的label，但是它和这个数据集搭吗？不知道。是不是也不是很搭？它是不是两个凑一块的？反正它这个润值，总的来看它这个润值，首先首先最明显的问题就是它这个没有，这数据集太少了。完全不知道什么情况，然后也没有对这个数据集的一些统计表。描述。明白，然后 label 就是 label or，还有，哎，我啥时候问问郭浩，我看能不能把数据要。他他，但是也可能他们项目的问题，到时候问问他们，他们有什么开源代码。然后就是别的分析，一个是因为他的是，他用了推测性的过滤条件嘛。然后他那个混合策略，他的这个选择。
策略选择上，它都是估计的估计的代价，估计的cost。它这里说，对这个 YT5M 这个估计的差不多，但是后面这个 Lion 100 兆，它的估计差的很多。它的解释是，它的派分 filter 用了一个早停机制，如果分布均匀的时候，它是差不多的，但是如果分布不均的时候，它是用 L 杠。 S 它近似L，所以就导致它这个误差很大。数据集是 OOD 的，估计不准。不是吧？那是。分布是。那是 label 啊，它的所有的早，它的所有的进估计不都是 label 的估计，那个 label 的那个准确性嘛？对，就估计 label 的准确性。就估计那个估计用那个 Bron filter，然后去筛那个标签的那个准确性的那个 false positive 嘛。对吧？那。
当然，你那个肯定有分布的问题了，应该是那个吧，我我不确定是哪个。就，我是这么想的，他用 L/ B/ S 近似 L 嘛，他推，他选择率是S，那么我需要搜 L/ B/ S 才能搜到原来 L 个有效的。那么如果我这个分布很均匀，分布很均匀，我这个推， level 的分布很均匀，查询那个 level 分布很均匀，呈现这个方块状，那么。那那对。对，那是，你一搜就搜着了。对，一搜就搜到了，就这个 L 比 S 就近似得很大了。
其实就是说 label 和，就是 label 那个重的标签和那个 Vector 的那个距离是不是，就是它俩是一块分布的，是不是有有一个成团的这种东西？是，说的对。但是这个，好吧，所以我觉得它还不如离线做个估计呢。你用这种东西过完之后，那你很。可能是。还不准，你对，你们数据还。不准，你还不如但是离线做估计的话，它其实，哎呀，离线做估计的话，反正你也得从，构造一些很不同的 query 去。去跑多次，你才能得到这个值嘛。对。就是这个估计的方法，我想想，应该是，按理说啊，做 pre filter、 post filter，还有那个 infer 那个那个阈值，应该是对每个 query 都不一样。对，对每个 query 都不一样，就每个query。到来前，我判断，先搜集一下每个 label 的信息。对。有那个每个 label 的信息，我才能估计。按理说是应该这样的，但是这个代价太高了，所以大家都是用一个存率。
然后第二个就是他说这个假阳性节点有多少？他在 100 兆那个数据集的时候，有 60% 的假阳性节点。很高，所以在普，其他数据集就是 20% 以下。那他还用的那个 Bronze filter 用的。太小了。他他们太省地了，他就是给他缩太小了。就后面是他这个使用的这个内存占比嘛，就下面这个表。 96 兆瑞宇。这个是只比，只相对于这个 attribute 的这个。对，他其实用的少，但是用多的话，他他可能就不好看了，这个数据。然后没了。OK，我改太少。
扩刊的。感觉，因为他可能挂在身上是个初稿，这实验差挺多的也。嗯，我感觉他想扩，他想做扩展版。就是他他把配分，配分拖一个拖一个期刊投入，应该是，应该是。我感觉我扩期刊我就只能这么做。很有可能是是是这么一个东西。前中后。对，是是的是的，但是他这个数据集其实挺有意义的，但是如果他能开人了，到时候问他，要问他他就开了。啊，我看看他这个论理怎么描述的，
就是在上面吧。这这这。就这。哎，他给了个链接，是吧？ 1Y Y YouTube 8M， YouTube 8M，1，那个角标 1 是啥？角标1，我看，等会你看，不是，往上一点。角标1，这个。哎，角标1，他他没有引吗？这个911，哦， 911 在这。shrink，它是自己爬的吗？不会是。
他没有引，他没有一个引用，哎，有，有那个，这个大一往下。大一。那个往下。这个。对对对，这个是。2016。168M，一个大规模的。视频的分类的benchmark，是这个。哦，就是他把，直接把这个缩，都放缩到五兆了呗。对，它是个 8 兆的，然后上面那个是，上面那个我看，上面那个是 YCFM 10 兆，是 image 的 embedding label，它是它是 B 贝格恩 benchmark 里边用的一个数据集。1500。就去年的数据。嗯，就是 NIX 的那个竞赛，就还在 NIX 里面办那个 AI 的竞赛。嗯，对，可以可以扒扒这俩。
对，到时候可以扒扒这两个，这两个数据集试试看，可以扒一个。反正我我们，我说我们分析过阿泰文的那个数据集，那真实的。啊对。然后我们分析过那个 label 和，当时清明分析过那个 label 和那。那个 Vector 之间的之间的一些。联合。联合关系。就是。 Label 近的。 Label 近的是不是 Vector 就近？ Vector 近是不是 Label 近？有有一些会有这个关联关系，就是。哦，你你去看看数据集来源。
对，看数据来源，比如 RCTL 里边，它那数据集就很近，为什么呢？比如说它有个标签就是文章的分类。哦，对你像，可能从这个点开始，他才提出了这个关键词。对，就比如说文章分类，比如说 CS 的文章，那他们属于一个 label 就是CS。然后比如 CS 点DB，对吧？那里面都都都都文章，然后 Vector 一 Vector 一，然后也卖点就是很相似。就是他们 label 相似，就是 Vector 就相似。就是 label 一定程度上代表了一个class。
对，然后这个是一个节点关系，另外就是一个节点关系，就是那个 label 的那个 label 的出现的频率，有些是极高频的，有些是极低频的。他们。是会出现一个非常明显的倾斜的效应。就是感觉这玩意分析分析还有有点意思啊。这大多数就是，看能不能就针对单独一两个数据集字母的高频低频关系。对，高频低频关系是可以做的，然后还有就是 label 本身和那个 vector 之间的级联关系。有些会有的，如果它会有的话，你建索引的时候就要就可以考虑这个东西。这个会对你的非常非常有帮助，对吧？你就是他他他是就成团了嘛现在。对。啊，对，这个这个这个其实很有用，感觉这个其实很有用，其他的就是我感觉是没有人分析过这个东西，就没有人分析过这种标签，或者说它的分布和向量之间的这种。
最近我今天下午还问，跟跟泽斌聊天，泽斌他在做 DCN 的filter，他。他做在做重排，我问他，我说你在做啥重排？他说是做既考虑向量距离本身的重排，考虑向量距离本身不就是做 starling 这种东西？然后又考虑label，就是标签属性的重排，把两边的两边的度量一块度量。
这个有问题啊，我 label 跟这个向量是可以分开的呀，分开存储的吗？那我探索这两个之间的相关性，对 SID 的存储是不是关系不大？是啊，所以，但是他他的做法应该就是还是把 label 和 watter 是，混合成的。就是连着存的。对，就一个一个点上相当于是都有。他这个也有好处，就是好处就是说我可能一次 IO 就够了，就是如果说是 label 比较少的话。意思就是一次 IO 把 label 相近。
label 的相， label 相近，然后距离相近，就是一块，就是label。还有点还有边一块，一个 L 就上来了。嗯，他在他在做这个东西，他基本上有个有出行了，因为因为德嵘那也做 WW 那个 range 的第四个第四个版本嘛，到时候到时候因为泽斌后边去香港理工读博，后面到时候还可以一块再弄，再掺和掺和，再玩玩。对，他他的他的工作我们也可以掺和，我们工作他们也可以，他也可以掺和掺和。对。我们后面要不要找个人去扒拉两个数据集看看？去分析分析。
就目前就带 label 的数据集不多，是吧？不多，就 archive 一个，然后这两个，三个呗。这个不， YFCC10M 好像我最近看到一些 AI 的过滤文章，确实经经常用。嗯，因为它的 label 数。不多，对吧？他好像也有个，他还有1万个query，还有 10 万个query，这个每个 query 还在流。
对，对吧？可以，我觉得我们可以分析分析这两个baseline，如果咱们以后想做 label 的。嗯，对。诶，我觉得这个正好卓然可以，他的工作要做，诶，卓然不知道。做 filter 版本的 FF 嘛，你其实可以用这俩。确实。对吧？你可以正好把这俩数据集看看有什么可以进步挖的，然后就直接做了。包括包括之前的 R 台文那个东西，但这个可能对于你来说可能会稍微小一点点，那个数据。
嗯，是的。
嗯，好。
确实，我这边是磁盘场景，我先扒一下看看吧。如果能像 archive 一样，对他们的 label 做一些分析规律的。的查找什么的也不错

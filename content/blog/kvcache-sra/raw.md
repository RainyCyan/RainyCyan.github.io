，好，很高兴呢，今天跟大家一起来探讨一下 kvcache 这个技术，然后我这边分享一下我们把 kvcache 这个技术在搜广推场景下的一些落地的一些经验。那第一点大家知道 Kvcache 其实是大语言模型推理的一项必备的技术了，那搜广推场景其实也应用了很多大模型推理相关的优化技术，同时搜广推这边有 Gr 把那些 model 也在做一些新的范式，然后随着模型的 scaling up 序列越来越长，参数越来越大了，其实这部分的整个的成本的开销也是越来越大的。
kvcache 其实本质上是一项成本优化的技术，用存储和网络来换 GPU 的算力，那另一方面是。搜广推场景其实比大语言模型的推理场景更加的复杂，我们这边会有很多的泛化的场景，我这边会列几个像用户长序列的 kvcache 的跨窗复用绑定什么。 model 到下游模型就 VERTICAL model 知识迁移以及搜索。我们这边在支持的 doc kvcache 场景有需求，会更加的复杂，规模更大，应用场景现在也是逐渐变多的一个状态。OK，然后我后面会具体地介绍这些场景的一些需求。
那第二部分是我们一起来回顾一下 kvcache 的基础知识，我们知道 kvcache 其实这个 key 跟 v 指的是 Transformer 中的 attention 机制里面的 QKV 中的 key 跟。 v 在大语言模型兴起之后，这个词就变得非常的火，那 q k v 的这个计算过程，我这边因为时间原因也不再详细解释，其实有几个点，就第一个点它的计算复杂度是跟序列长度的平方是成正比，所以随着序列长度越来越长，这个计算的开销肯定是上涨得非常的夸张的。
那第二个点就是我们其实只需要缓存 k 跟v，不需要缓存q，因为q。度只属于当前TOKEN，然后 k 跟 v 是会被后续所有的 TOKEN 共享，就在它自回归过程中计算下一步的时候，需要前面所有步的 k 跟 v 来做一个 attention 的一个计算，所以 k 跟 v 缓存下来可以大量的避免重复的这部分的计算。
那第二点就是有了 kvcache 之后，其实就很自然地可以做 cross request kvcache 的一个复用，那在大。模型推理的场景是在多轮对话历史前缀共享的场景下可以做复用。那在推荐场景我们有一个优势，就是我们其实用户的历史的行为序列是一定是固定，那我们其实在对这种长序列做 KV cache 场景，它每一次基本上都可以复用，因为前缀是一样。同时带来的一个挑战是我们随着用户序列长度的变长，其实这部分的展。
整个的 kvcache 的存储的规模是非常的大的，因为我们对每个用户都是存储一个很长的序列，所对应的 kvcache 的大小估算，我们可以通过 TOKEN 数乘上层数乘上维度，然后再考虑两个要存两个向量 k 跟v，然后每个向量的一个精度的一个字节数这样去计算，那典型场景下来这样算就是一个 40 兆b，其实在推荐场景下，因为我们的序列长度可能都做到是。
k 甚至说 100K 这样一个量级，所以其实总的存储的量会非常的大，就单个用户可能就会到 GB 级别，然后这里面当然也可以做很多压缩的技术，我这边列了一些，然后像现在其实比较常用的像量化可以做到，比如说INT4，甚至现在可以进一步做INT2，然后也有一些无损压缩，也是针对数据分布一些特点。大概可以有百分之七八十的一个压缩率，然后算法侧也可以做一些优化，像常用的 slice window 的attention， sparse 的这种attention，其实它可以把多层的 KV cache 中，其中只有少量的层是存储完整的序列的，其他的存层只存储一些较短的一些 sparse attention 其实也可以有很大的一个压缩，就是这方面也是一个很重要的一个技术。那下面呢？我介绍一下。
下搜广推场景下的一些各种 kvcache 的场景和这个场景的一个大概的一个流程。然后第一个典型的场景就是推理的跨刷复用，我们这里以 Gr 的抖音， Gr 的它的两阶段的这个模型为例，它本身 Gr 的这个两阶段模型它也是有一个防溺水model，然后这个防溺水 model 会先做一个训练，然后生产一份 for 的kvcache，然后在生产 for kvcache，它其实是先完成训。训练，然后把这个参数冻结，就打一个模型的checkpoint，然后再做一次离线推理。
这个 full KV CACHE 对应的是用户的历史的超长的行为序列，会有 10 万长度，然后在这个推理过程中它需要读这个 full KV cache，然后同时它也会有用户会有增量的序列，然后这部分增量的序列也会生产生成出来新的 KV CACHE，就是这部分的 inter KV CACHE，然后这两部分 KV CACHE。
其实都是存储在就我们提供的 index 的 KV cache 的一个存储的一个方案里面，然后在下一次请求过来之后，它其实也是同时读取这两部分 KV cache，然后再生产新的，然后新，再将新的再 open 的回来，就大概是这样一个模式，然后这里面也有很多推理层面的性能优化和一些流程上的优化一些点，比如说在那个 GPU 的机器上需要先做这个prefetch。来释放一些延迟空间，然后数据拿到之后先做prefill，然后再等那个 candidates 到达之后再做一些decode。然后从整个的体系结构方面也有很多的就是针对网络内存带宽相关的一些优化。对，下面可能会讲存储方案的时候，我们才会展开。
然后第二类场景是在 FM 到 VM 这里， FM 就指的仿 d 是model， VM 指的是下游的模型这个v。 VM 就是 VERTICAL model 意思，然后这里不同业务可能也有不同的名词叫法，就有的叫 task model 或者是叫 downstream 的model，它本质上就是一种两阶段训练的一种范式，然后它需要做 FM 到 VM 的一个知识迁移，这边有不同的这种知识迁移的方式，像蒸馏 user 组的共享或者是 FM 的生成一些类似 KV cache 的产物传递给下游 VM 模型去复用，那它整个的范式大概是这样的一阶段会训。
另一个 foundation model，然后这个 foundation model 会生产很多 kvcache 的产物。写入方式可以是整体的替换，就是通过 put 的方式去写入，或者是 append 的方式，因为本身它很多 kvcache 的数据，对应的它的 TOKEN 其实是跟序列是一一对应的。然后第二阶段训练下游的 VERTICAL model，然后 VERTICAL model，每个 VERTICAL model 都可以读取同一份 foundation model 生成的KV。
下载的数据，所以这部分会有对下游模型来说其实是一个一份数据，多份下游模型读这样的一个模式，所以读的整体的，读的带宽可能随着下游模型的增多会有一个放大，然后这部分数据也需要做训练机房到推理机房的一个跨机房同步，因为在推理阶段其实也是需要同样的这样的一份数据。
第三个场景就是我们支持的搜索的 doc kvcache 的场景，然后 doc kvcache 其实是做一个相关性的计算来降低推理的一个计算量， doc 侧的信息相对较长，然后 query 较短，其实它是相当于是把两个拼接起来，然后做一个相关性的一个计算，然后前面这部分其实就可以通过 kvcache 来做一个加速，然后它的一个流程是在线推理时读取kvcache。
可能会miss，因为这部分是kvcache，可能没有写入，然后 miss 之后生成 kvcache 之后再回写到我们的存储之中，这边会对总的 cache 大小会有一个控制，然后支持 TTL 和 FIFO 的淘汰策略，这个 FIFO 淘汰策略指的是当空间满了之后，自动地将最老的数据就是淘汰掉，这样释放出来空间，用来最新的数据写入。其实我们也在进一步探索这个方向，能不能做一些冷热。
分层的一些技术，然后我下一部分我们介，我简要介绍一下对kvcache，特别是存储侧这边的一些典型的一些需求。那在推理侧其实典型的需求就是要做到低延迟，我们目前看到的一些场景，像兆 b 级的 kvcache 大部分要求 P99 在 200 毫秒左右这样一个范围内， GB 级 P99 要在 400 毫秒内。然后第二点就是。要高吞吐，其实高吞吐对于这个低延迟这两个事情放在一起就带来很大一个挑战，高吞吐是我们需要尽量的打满客户端网卡的一个上限，才能充分利用算力。其实这里有一个比例关系，就是每份数据它对应的这个 GPU 的算力的一个消耗，当这个 GPU 算力的，当这个是算力瓶颈情况下，其实它那个对网卡的需求会变少，但我们现在看到很多场景可能因为做了 QA CACHE，它本身后续。的计算量会下降，所以其实对网卡的那个需求量会非常的大，我们需要尽可能地打满后端的一个网卡，就是 GPU 机器本身的网卡。
第三点就是因为推理也需要对新的序列做增量的append，就是 short term KV cache 这部分，所以这里面也会有一些数据连续性的校验，以及一致性相关的一些需求，同时也要考虑这个是。这个 Pen 就是会不会发生一些并发的冲突？一般采取的策略是对同一个 uid 会做一个一致性的哈希，让它尽量的录入到同一个机器上，还有一些短时效 cache 的场景，比如 TTL 是小时级别的CACHE，这样的一些长短时效 cache 的场景在训练侧典型的像 FM 到 VM 的场景，下游模型多读吞吐会非常的高，当下游 VM 模型。非常多之后，其实对我们这份存储的一个性能密度的要求会增加很多。然后第二个需求是在训练场景其实需要做防穿越，在训练时因为下游的每个模型训练的样本它其实进度是不一样，就是训练时它可能训练历史的样本，所以必须要通过它样本的一个时间来做过滤，然后避免使用到一些未来的可能发生穿越的数据。那这里其实会有两类穿越问题，我下面会。
讲同时训练也会有数据同步的一个需求，就我下面会再简要介绍一下在这个一致性和穿越上面的一些特殊的一些点，像首先第一点我们要避免模型参数导致的一些不一致问题，或者是穿越的问题，这里面有两种不同的业务上的场景和玩法，第一类场景是冻结参数的场景，就现。
边训一个仿真model，然后去将这个仿真 model 参数做冻结，就打一个checkpoint，然后在生产 KV cache 时是使用冻结的参数做批量的推理，那这种模式它不太会发生模型参数的不一致或者是穿越的问题，因为它的参数是一，参数都是一致的，是冻结后的，但它存在可能存在一个问题是随着时间其实效果有一定的衰减，因为它这个模型参数是冻结的，它并不感知最新的一些数据的分布。
那第二类场景是边训边写的场景模型参数就是一个仿真model，它一边训练一边去写出kvcache，它的模型参数会不断的变化，但这种时候它不同时间生产的，即使是对同一份序列生产的 Kvcache 也会有很大的一个变化，因为它的模型参数是不断变化的，所以它需要避免模型参数导致的不一致和穿越的问题，同时这种方式它一定程度上来说呢。
kvcache 就没办法重算了，因为它的参数变了之后，它再重算那个效果跟第一次计算的效果其实是不太一致的。那边训边写场景下的避免穿越问题，它需要根据请求的时间做两方面的过滤，一方面是序列的过滤，另一方面是要看这份 kvcache 所对应的模型参数的时间是不是更老的，避免模型参数导致的穿越问题，所以会有一些不同场景下会有一些不同的设计和。
注意的点，那我下面呢介绍一下我们当前的 kvcache 的一个大概的一个存储方案。首先我们 kvcache 的存储，我们我觉得会分这样的几个模块，我们会有一个 kvcache SDK，这个是好在用户的 GPU 的机器上，包括推理跟训练的 GPU 的机器上与用户的进程去连编，然后我们服务端也会有一些对应的模块，像我们的一些 Meta 配置。
管理以及我们的 Kvcache 的这部分Meta，其实数据的 value 的 meta 的一个存储会有一个正排，我们还会有一些控制节点，然后我们数据的部分是存储在一个 HDFS 或者 bytestore 的一个集群上，然后我们将数据打散，分布在不同的 DN 上，因为 kvcache 的总数据规模是很大的，可能到几 PB 或者是几十 PB 这样的一个规模，所以其实这部分数据还是。
一个需要一个很大的一个集群来去存储，那我们整体的一个架构的一个目前在主推的一个架构，其实是通过我们 KV CACHE SDK 做数据的直写跟直读，特别是针对一些比较 size 比较大的一些 KV CACHE 的一些场景，对吞吐的要求也很高的一些场景，就是做到最简的 IO 路径，只读只写HDFS，然后通过 RDMA 去访问，减少中间的链路，其实本质上是降低存。我们降低去，我们去降低存储和网络的一个成本，因为数据只存储在 HDFS 或者 bytestore 这边，只有一份就没有冗余存储，我们可以通过优化这边的机型副本数，然后通过冷热分层或者多集群混部等等各样的方，各种各样的方式，我们去尽可能降低这部分成本。
那中间链路的 TCE 节点这部分成本其实是非常低的，因为我们这部分的机器数非常的少，而且从网络链路上来讲只有一跳这样的RDMA，其实。在很多大吞吐的场景下，也对，网络是减少它中间的交换机的开销，不然在很多大流量场景下，再经过中间一跳会额外的增加很多的成本，而且网络上会有很大的压力。
我们从数据模型和接口设计上来讲，我们分两类模型，一类模型叫支持 append 的类似长序列，这种模型就是一个 uid 下的kvcacheitem，它可以去 append 去组合，也支持。 put 去覆盖，然后读取时支持过滤其中的某一些 item 返回来，这样减少一些网络带宽。可能也有一些其他的，比如说根据时间做过滤，避免这个一致性或者穿越问题等等多种过滤的需求。然后第二类是一种 KV 模型，就是一个 key 只对应一个 KV cache item 做一个整体的读写，这两种模型其实都会在不同场景中有应用，然后我们的元数据部分就数据的元数。据是存储，有一个单独的元数据存储，我们可以通过那个元数据存储做一些字段的过滤来按需读取，减少带宽，然后这个 KV cache item 是一个相对比较抽象的一个结构，它其实支持业务采用不同的打包方式来打包。然后我们现在看有整体打包，对，整体打包是对一段那个序列所对应的 KV cache 做一个。
整体的一个打包，然后分层，打包是按照模型中的不同的层来打包，然后分层分段是指的说它每一个层里面它还可以去进一步的去拆成多段，然后每一段是一个item，像分层分段打包，它可以按层去过滤，按每一层中也可以只读取某一段，对它的过滤更加的灵活，更进一步的减少网络的传输。
从我们整个的一个流程上。来看我们像推理里面的场景，以这个图为例，我们的数据的生产会有一个离线生产任务，这个这部分离线生产的任务会将那个模型推理产生的那个原始的 KV cache 直接的写入到我们的 HDFS 或者 bytestore 的存储之中，这部分也是只写，然后写入完成之后再将少量的一些 Meta 信息放到我们，通过我们正常的这种正排写入方。
方式存到我们的一个 Meta 集群里面，然后在推理的实时推理阶段，它通过 RDMA 只读HDFS，然后这部分如果做了 uid 路由，其实它会有很多的cache，其实是可以做在这个 GPU 机器的进程内的，可以做 uid 的cache， meta 的cache，然后这样它可以避免对这个正排进行的读取，然后直接就可以知道这个数据。
的位置，然后增量写也是通过直写的方式，直接写到HDFS，然后写成功之后再将我们的元数据更新到我们的集群，元数据的集群之中，这是比较典型的一种模式，因为 kvcache 刚才也讲到就是场景很多，所以现在不同的场景其实也有一些细微的差别，这也是我后面讲我们未来演进过程中，我们可能会把一些场景做一些统一，然后再。
读取时，我们可以支持全量的读取一个 uid 下的所有的 kvcache 数据，也支持做一些过滤和筛选，这部分可以通过元数据的过滤来实现，也可以只是用来判断通过元数据来判断这个 kvcache 是否存在，也有这样一些场景，就是要提前判断这个 kvcache 数据是否存在，用来以及或者说为了获取 kvcache 中的某些字段，用来判断是否需要读原始的序列，这样可以做一些。提前的判断和设计，然后筛选读是只读这个 uid 下的一些部分的KVCAP。
然后第二点是我们尽可能，我们，我们为了尽可能地打满客户端的网卡，其实我们会做内部，会做并发访问分桶的并行读取，然后我们分桶并行之后就需要避免一个问题，是当这个网络拥堵情况下，我们有很多的请求都在分了很多的桶，然后不能让每个。请求中的部分请求失败影响它整体的成功率，所以我们会做，内部会做一些智能的流控，然后避免说真的打满情况下的雪崩，而是说我们在流控在网络满的情况下只是控掉个别的请求，然后这样让能成功的那些请求尽量都去整体成功。然后这些其实更进一步的这些不同的子请求它可能也是有一些优先级的，比如它可以分层去入图，可能对于。下面的层或者是比较早的数据，它可以更高的优先级。然后第三个点就是我们现在是打通了 RDMA 的网络链路的，然后也是跟 HDFS 一起去合作，包括跟阿康一起去合作，然后把打通这个 RDMA 的链路，还有网络高网的一些团队。然后我们也是利用 RDMA 加多网卡的这个能力，充分的利用网络，我目前在推理的场景里面是使用了一个 200G 乘 4 的。的一个h，一个 L20 的一个机型，就是一个比较大网卡的一个机型。
在这个 RDMA 的条件下，其实它就是如果是纯粹的TCP，其实它有很大的一个瓶颈，包括内存带宽的瓶颈以及内核网络自身的一些瓶颈。在 RDMA 情况下就可以得到一个很大的一个缓解，有一个非常大的一个提升。然后在内存带宽优化方面，我们要尽量的避免拷贝，因为随着网卡不断地去。我们去提升，然后这部分的内存带宽的瓶颈是非常的凸显的，中间过程要尽量地避免拷贝，我们这里面也涉及一些压缩的工作，就是可以看到我们整个的读取，像这边并发读取，然后 RDMA 将数据拉回来，因为我们数据本身还是做了一些无损压缩的，这部分有 80% 左右的一个压缩率。如果我们在 CPU 端做 ZSTD 的。
压其实对 CPU 和内存带宽还是有一些损耗的，可能在高非常高的网卡的压力下，这部分的 CPU 和内存带宽的瓶颈也会非常凸显，所以其实我们也做了 GPU 解压的技术，通过 GPU 里面一个ans，一个优化过的一个解压算法，就是它可以在就是 GPU 内做一个很高效的一个解压，整体的可以达到一个更高的一个整体的一个吞吐。
从性能瓶颈角度来说，我们一般是做两方面的分析，一方面的分析是 GPU 机器本身的一个瓶颈带宽，或者是它的带宽瓶颈跟算力瓶颈的一个比例，然后内存带宽的一个消耗，以及是否要通过 GPU 解压来去进一步的优化等等这样的一些分析。然后从服务端来讲，我们可能会分析单个单位 TB 对应多大的带宽。
带宽对我们的存储机型来说，其实也分存储瓶颈跟网络瓶颈，如果它需要的带宽很大，可能会先达到一个网络的瓶颈，当然我们的存储机型它的盘数是很多的，可能网络瓶颈会比它的盘的瓶颈更先达到，所以大部分情况下是存储瓶颈跟网络瓶颈这样的一个两者之间的一个转化的一个关系。随着这个不同的pattern。然后在 cache 这方面，我们典型的一些场景，比如做了 uid 的路由，或者是其他 key 的一些路由，我们可以做本地的cache，通过先读本地cache，然后再读远端存储，然后这部分本地的 cache 我们一般是通过估计它能 cache top 百分之多少的热数据，然后来算计算它覆盖了多少QPS，然后来推算带宽的一个优化比例。然后我们再看的另一个场景是在训练侧。我们也可以做多任务的共享的cache，这样可以充分的利用机尾的 RDMA 的网络，因为这里训练的机器它分机头网络跟机尾网络，机头网络可以访问我们的远端存储，但机尾其实并不能访问。机尾是 GPU 机器单独组的一个 HPNN 的网络，所以这边还有一些共享 CACHE 的一些工作。
对，上面的是我们当前正在做的一些存储的方案，我下面也列了。下我们未来演进的一些方向，第一个方向就是在存储成本上我们会持续地进一步地优化。第一点就是我们会探索更高的压缩率，更激进的一些有损或者是无损的和压缩的一些方式。第二点是从存储的本身我们会做一些优化。第一个就是 EC 就是纠删码的技术，我们目前使用 HDFS 大部分是做两副本，然后。 EC 其实像常用的一些配置，比如 4 + 2 可以做到 1.58 带 8 带 3 可以做到1.375，其实会有一个很大的一个比例的优化。
第二点就是我们可以利用 QLC 机型，因为我们看到很多场景下它是容量瓶颈而非带宽瓶颈，在容量瓶颈下就非常适合用更高密的存储的机型，然后 QLC 可以做到单个机器是大概是 500T 的一个容量，而且成本也比，就单位的 TB 成本是比。普通的这种 s S80 的 SID 的成本是要低很多，然后以及冷热分层和多集群混部，就是把容量瓶颈跟带宽瓶颈或者是盘的 OPS 瓶颈等不同瓶颈的这个集群，我们去做一些业务上的混部，然后充分的利用一些其他的，就是利用一些之前无法利用的资源，然后我们也会做数据的快速淘汰。
那第二点我们会。针对多个场景的适配做方案的标准化，因为现在我们其实看到搜广推场景下 kvcache 有很多泛化的需求，我们可以做一些一定程度上的存储方案的统一，然后这部分我们也在与序列的整体的存储方案，就是 USDB 的方案来做一些协同设计。因为像有一些 kvcache 场景，它做了各种优化技术之后，其实它的 size 可能控，可以控制在单 uid 以兆b，类似这样的一个级别，其实跟序列长序列的一。一些 size 其实相对接近，我们会做一些协同的设计，能把一些方案统一掉。然后我们也在提供一些 Python 接口，方便更进一步地去更简化一些接入的一些方式，然后支持内置的一些量化压缩和编码的优化。然后在第三个方面就是我们做一些管控的优化，然后去完善一些生命周期管理的自动化和质量的监测，这个也是当前。很多业务的一些痛点，然后第二点就是我们在可算可观测方面做到更好的一些可观测的能力，以及对成本的一些分析的能力。OK，好，今天的分享就是这些，下面是一些相关的doc。
哈喽，林玲。
对，我看还有两个评论，你要不回答一下？
ok，第一个评论是说 Kvcache 的大小是不是跟那个序列长度线性相关？对，确实是的。当然这里从算法角度来说，它当它做了序列扩长之后，它可能就要看。探索更激进的一些 kvcache 的量化或者是压缩的手段来控制这个size。所以从最终结果上来看，可能我们序列扩 10 倍，但 Kvcache size 并不能同样地扩 10 倍，而是要进一步地去压缩。 contact 超过了之后，历史序列不淘汰，只淘汰增量序列。其实也不是历史序列不淘汰，是它它也会淘汰。
这里面有两种，一。一种是数据还在我们存储侧，然后在读取的时候做截断，因为他要控制读取的那个 KV cache 的大小，就是只读最新的，比如说1万长等等这样的。然后另一个方面是这个仿真式model，它其实是会定期的更新这个模型，然后它整体数据会重刷。对，这里可能我没有提漏掉这个事情了，就它定期的，比如周级的会数据会整体重刷一遍，重刷之后就是 for 部分跟 inter 部分就会整体都会。都会被淘汰掉，会被一份更新的 for kvcache 数据所覆盖。
OK。这个解不能prefix，不能匹配，这个可能是从模型角度，它是有一定的解法的，所以可能并不是一个完全一定要前缀匹配的事情，然后本地 cache 的命中率其实是不同场景差别挺大的，当那个 kvcache 很大。情况下，我们其实也有一些场景也算过，那本地 CACHE 的命中率是确实很低，是不是那么划算？然后另一个点就是要看它本地有多少可用的 cache 空间，主要是 SID 的盘有多大，像推理的一些机型，它本身的盘可能也只有一两块，所以就是整个 cache 的效果来说也做不到很高。另外就是盘的吞吐，如果只有一两块盘的话，其实单盘吞吐也就。三个 GB 左右相比，我们进一步扩充网络，因为网络现在是有 200G 乘4，我们把网络利用好，其实是有几十 g 大 b 的，所以它能带来的一个提升上限是比较有限，如果盘挂的比较多，可能会有一定的空间，OK。
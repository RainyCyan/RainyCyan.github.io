---
title: BM25 关键词匹配：朴素却仍未过时的检索基线
date: 2026-06-02T14:18:00+08:00
tags: [Search, BM25, IR, Retrieval]
series: []
featured: false
description: "从 TF-IDF 出发理解 BM25 的设计直觉、参数含义与工程实现，看看为什么向量检索流行多年之后，关键词匹配依然是召回链路上绕不开的一路。"
draft: true
ShowToc: true
TocOpen: true
---

## 前言

向量检索流行之后，BM25 一度被认为是"上一代"的关键词匹配方案。
但只要你真正做过召回，就会发现 BM25 几乎总会作为多路召回中的一路存在——它便宜、可解释、对长尾关键词鲁棒。

这篇文章想把 BM25 的来龙去脉讲清楚：它和 TF-IDF 是什么关系？两个参数 `k1` 和 `b` 在调什么？工程上又是怎么落地的？

<!--more-->

## 从 TF-IDF 说起

### 词频 TF
### 逆文档频率 IDF
### TF-IDF 的两个问题

## BM25 的设计直觉

### 对 TF 的饱和处理
### 对文档长度的归一化
### 完整公式

## 两个参数：k1 与 b

### k1：TF 饱和速度
### b：长度归一化强度
### 经验取值

## 工程实现

### 倒排索引
### 分词与词形还原
### 与向量检索的混合召回

## BM25 的变体

### BM25F：多字段加权
### BM25+：低频词修正

## 什么时候应该用 BM25

## 写在最后

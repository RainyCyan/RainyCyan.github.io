哎，你开始游戏。嗯，几点了？
好的，那我现在开始。今天讲这篇《 Creator 是采用低选择性过滤器的高效向量搜索。主要从下面五个部分展开，先看一下研究背景和研究方向。研究背景呢，就是现实世界呢，不仅会要求向量之间语义相似，可能还会带有一些像是时间或者是价格的标签，于是就涉及带过滤的向量搜索，就 filter ANS。比如说电商平台可能会搜索标签为红色，价格在 100 元以内的衣服。这种带过滤的向量搜索呢，不同的选择率呢，是一大的。
挑战，而现有的方法呢，在处理低选择率时都有局限性。比如说暴力搜索，比如说图索引算法和分区索引算法都有各自的局限性。本文的话主要是提出了双索引架构，推出了KWRITER，一种基于分区的索引方法。这种方案的话会与现有基于图的方法互补，应用于低选择性过滤的ANS。
可以先简单看一下这个图，这是后面结实验结果一个图，就 creator 是蓝色的这一条曲线，纵坐标是查询延迟，当然是越小越好，横坐标是选择率，都是比较低的一个选择率。可以看到 creator 相较当前比较先进的这个 a accurate，就是这样红色的这个线，绿色的，还有紫色。
的其他几个线是有一个 pop gap 的，这些定义的话，有些补充和定义，我一般会在左下角，如果不清楚的，可以看一下左下角。那么就直接来讲一讲他的解决的关键问题和技术手段。他先谈了一下为什么现在的方法会应对低选择率失败。比如说他下面一个图有四个子图，子图 A 的话是基础的图索引。子图 B 的话是是优化过的一个图索引，然后 C 的话是一个基础的分区索引， D 的话就是本文的实现creator。
我们一个一个来看，先来看就是像是图 A 的话，它是一个标准的图结构，然后呢，灰色节点代表的是不符合过滤条件，是不合格的节点，蓝色节点代表符合过滤条件的合格节点。在低选择率下，那合格节点是很少的。在符合过滤条件的蓝色节点之间呢，可以看到连接要么是很稀疏的，甚至有些是断开的。这种连通性的崩溃呢，就会迫使算法要么要访问所有的邻居，带来巨大的计算代价，要么就要完全跳过不合格的邻居，但是结果会不完整。然后第二个是优化过后的图索引的话，是通过增加大量的边，使得合格的节点，就是这些蓝色的节点连接在一。起，但是随着选择率的降低，确保连通性的总度数会爆发增长。文章中是给出了一个数据，说如果每个节点需要 m 个合格凝聚，且选择性为s，则总度数需要约 m 除以s，所以说这个也是不是代价很大的。
那么再来看子图 C 是基础的分区索引，就看到空间被划分成了多个细粒度的灰，且灰。黑色的椭圆形聚类，那么在低选择率下呢，每个聚类内部往往只有极少数，甚至只有一个合格节点，就是蓝色节点。那么与无过滤搜索相比呢，搜索就必须要访问更多的聚类，那么在与聚类质心进行距离计算时，就会产生巨大的额外开销。
那么 Curator 为什么可以应对低选择率的场景呢？因为它采用了一种层次化的分区方法，将稀疏区域中的合格向量缓存在树的更高级聚类的例。密度能够自适应的调整以匹配过滤选择率，后面会具体介绍它的一个系统实现。简单来说，其实就是越稀疏的向量呢，会在越高的层级，那么只需要访问几个高层，就可以得到想要的向量了。
我们来详细看一下它的系统设计和实现。它的核心哲学呢，是将特定标签的索引内嵌在一个共享的基础索引当中，看到就是。是这一大张图分成三部分，左上角的话是基础索引，右上角是美标签索引，下面的话是它一个编码，我们也是一个一个来看。先来看这个基础索引，基础索引呢，它是一个标准的层次化 kmeans 聚类树，它对数据集中所有向量都进行了空间的划分。优点是作为一种基于分区的索引方法，它不会像土基索引那样在低选择。性条件下有固有的连通性问题。有分区结构，无论标签稀疏程度如何，都是有清晰界定的。层次结构也可以自然的在同一棵树中容纳不同过滤条件下的多种分区粒度，使得每标签索引可以根据特定标签分布来调整聚类深度。就是如果不太理解这个什么叫根据特定标签分布调整聚类深度的话，也可以看一下左下角，我自己举的一个小。例子，或者是听一下后面讲算法的部分就能理解了。大概就是如果标签越稀疏，这个数可能就越浅，标签越稠密的话，这条数的话可以一直向下延伸，就会越深。
来看一下单标签索引。单标签索引的话是对于每个特定的标签，它不会单独的建一棵树，它会复用，刚刚提到的。基础索引的空间划分，构建出一个只包含该标签合格向量的一个子结构，就是右上角的子结构，它可以根据特定标签的向量密度，自适应地调整聚类的粒度和深度。这个也是后面的算法当中会提到它是如何实现自适应调整聚类的粒度和深度的。然后来看一下紧凑编码，为了极大的节省。
审内存的单标签索引其实不会存储真实的向量数据，它主要是存两种关键的数据结构。图中标注的一个是 buffers 缓冲区，缓冲区的话，可以看到是我鼠标标注的这里，同时呢，它也是左边这些图里面的小矩形，比如说深黄色或者深蓝色的小矩形，它存放在之前的虚拟的树的叶子节点。本值中，里面是排好序的一个向量ID。然后第二结构的话是 Bloom Filter 布隆过滤器，它是存放在每个基础数的基础数的每一个节点当中，它会近似记录该节点包含哪些标签的向量。然后这个布隆过滤器它是假阴性的，什么意思呢？就是如果它说有这个标签，那么不一定有，但如果它说没有这个标签，就下面没有这个标签，那就一定没有。
接下来我们来看一下，第一个算法，第一个算法是针对单一标签的。单一标签的话，第一步的话是会进行一个冰色数搜索，会从根节点开始搜索，它会维持一个宽度为 b 的候选队列。这样做呢，是为了克服高层节点置信之间不精确的问题，防止检索，陷入局部最优。可以看我画的 mini 的小图。
来大概理解一下，如果中间这个点呢，是我们想搜的点，左边是一个聚类，右边是个聚类，蓝色的节点是它的中心，单从距离来看，可能这个紫色节点离蓝色节点，离小的聚类的志远会更近一点，实际上是属于这个大一点的聚类的，所以说这样的而我们采用树搜索的话，它不会直搜，只搜最近，也就是最好的一条路，它会挑出前第一个最。
有希望的节点，把它们放进候选队列。那么这样子的话，就是增加了容错率，更容易找到优质的答案。来看第二阶段，第二阶段是优先级，就是这 best first search，它会利用初始化好的一个队列Q，开始执行最佳优先遍历。比较关键的一步是布隆过滤器，就是每到一个节点，它都会问一下布隆过滤器。去问说有没有这个标签，如果说没有的话，可以直接跳过，因为就是布隆过滤器说没有的话，就是一定没有的，虽然他说有的话不一定会有。然后一个是缓冲区的处理，如果遇到带 buffer 的一个叶子节点的话，就会计算 buffer 内所有合格向量的精确距离，来更新我们的结果集。最后什么时候终止呢？就是当结果集经过一轮 buffer 扫描之后，不再发生变化的话，那么搜索就停止了，也就是它收敛了。
可以注意一下它的启发式评分函数，这个S，左边的我列出来了，它原文中给出了公式，那么它的第一项的话是查询点到簇质心的一个距离，第二项的话，其实是一个该簇的平均半径乘一个调节的系数阿尔法。那么这个启发式函数它有什么用呢？它其实它不仅仅是看谁离自己最近，它会给地盘。大的这个簇，就是后面它会有一个该簇平均半径，就会给地盘大的簇一些额外的优待。就是有一个簇，它如果它很大，但是它离中心有点远的话，那它边缘的向量也有可能会离自己更近，这就鼓励算法去搜索那些面积大但是有潜力的区域，而不是死磕在一个小的区域。
刚刚这个算法可以拿这个图，我们来看一下，这是这些图都是原文当中涉及的，这是一个空间。切分图，我们看到这个红色的 X 的话是查询请求，不管是左图还是右图当中的黄域、高亮区域都是被算法访问的空间区域。而有些深灰色的节点的话，是属于不合格的方块，直接被跳过了。之前算法一中提到的，如果布隆过滤器说没有，它就一定没有，就跳过了。然后单标签索引树的话，就。是像之前提到的，它是依附于基础索引的，它只是一个逻辑子拓扑。浅灰色的节点的话，代表的是虽然它在物理空间上存在，基础索引中有，但是因为不含特定的标签，所以说在我们的视图，单标签视图当中，其实是被隐申了。
从这个算法和这个图来看呢，可以看到结合了布隆过滤器的快速否定能力，还有启发式函数的贪婪搜索能。力，在只访问少数、极少数节点的情况下，就能获得极高的一个召回率。刚刚讲的是单标签索引，那么现在我们来讲一下，就是复杂谓词的查询，它是怎么处理的呢？它提出了，如果我提前构建的话，无法涵盖所有的情况。为了能够应对各种各样的组合呢，它是构建了一个临时的索引。我们也会，分区域的来看一下这张图。
现在看左上角，左上角这个图的话，它定义和它是说在 quadtree 的基础索引树中，每个节点都会被分配一个NID， node id，它会被编码，从根节点出发到该节点所经过的分支索引序列。就是说它能完整记录和复现该节点在整颗层次聚类树。中的绝对空间拓扑位置。而VID，也就是向量标识符的话，它会有一个，每个向量有一个唯一的VID，它是等于 Leaf 的NID，就是叶子节点的NodeID，加上它在叶子节点当中的一个索引，会按位打包，用 0 填充来保持固定的长度。这意味着什么呢？我们通过一个VID，它的高位部分其实就能看到它的叶子节点是哪个，因为它的高位部分就是叶子。节点的NID。在右图的举例当中，可以来详细的看看，它是一个极度简化的设定，树的分支因子设置为2，只有两个孩子，容量阈值，这个 buffer 能存储的向量个数，设置为2， VID 这也是用三位二进制数表示的，所有向量都在 00 到 11 之间。像是基础索引的话，根节点的话就涵盖了全局的取值空间。根节点的左孩子就涵盖了 0 开头的 0 开头的那些十进制，右孩子就包含了 1 开头的十进制。这个也是后面算法二中会利用到的它的一个空间特性，就是这些节点自带的物理区间。可以通过这个节点的向量标识，就判断出它物理区间。
然后再来看下面的图，下面这个图会有一个排好序的合格向量队列，都会直接传递到算法二当中。它的话本质上其实是就是排好序之后的一个向量队列，只要把它从小到大一排队，那么属于同一个空间分支的向量在物理数组当中天然就是连续紧挨着的。这里。就可以来看算法二了，算法二针对的就是复杂谓词的一个过滤，它的输入条件里面就有排好序的向量标识符。
第一步骤呢，是启动和初始化。它会首先先创建一个空的临时索引，接着呢，就会调用核心的递归函数，这个buildsubtree。初始状态下呢，它的视野会是整棵基础大树的根节点。然后以及那份包含所有项目 ID 的完整名单。第二步的话，它会判定叶子节点，也是一个递归终止的条件。进入这个函数之后，它会考虑先前，就当前传来的这批向量，它如果它小于或者等于是之前提到的缓冲区容量上限这个 Bmax 的话，就说明当前的数据已经足够稀疏了，这个数就不用继续，这个分支不用。继续往下涨了，就会立刻把这批向量打包塞进一个 buffer 里面，会创建一个临时的叶子节点，并且终止这一分支向下探索。这是就是为什么说虚拟树能够在稀疏处会变浅。
如果前面的判定并不符合，它是超过了这个 dmax 怎么办？说明数据太稠密了，就是还得继续往下分岔。它这里的比较绝妙的一点就是，因为输入的向量 ID 是排好序的，而且基础的索引的这颗物理大树，它每个子节点都有一个明确的管辖范围。比如说之前的从 000 到111，然后 0 开头之类的。它就是可以直接通过二分查找，在有序列表中快速定位，会找子节点管辖的左边界在列表中的位置和。子节点管辖右边界在列表中的位置，也就是一个像 Cmini 和Cmax，其实就是节点归应的一个 ID 值范围，像是 LC 和RC，它就可以直接查找出来这批数据在内存数组中的起始索引和结束索引。
最后一步就是递归构建和向上组装了，它如果切完蛋糕之后呢，发现 LC 小于于RC，就。说明这个子节点下面确实有我们想要的合格向量，那么它就会继续深入递归，对这个子节点再次调用这一个函数，把刚才切出来的专属的那一个小名单又传进去，让它继续去下一层进行上述的过程。然后它在深层构建完毕并返回之后呢，当前节点 A 会把返回的子树 C 挂在自己的名下，当做自己的孩子。遍历完所有的物理子节点之后呢，当。当前节点就构建完成了，向上返回。他的算法优点是什么呢？我总结了一下，首先他代价很小，他临时索引构建的开销是极小的，后面也会有实验验证，他的效果是很好的。他未预先建立索引的creator，就是他对于这些复杂的位置条件的是，来了之后才临时构建一颗虚拟树，他其实是达到了与预先建立索引版本。相似的性能，并且在两种过滤器 and 和 or 上面都显著优于， ACRON 是一个比较先进的一个实现，又展示了它实现的高效性。
还有一个它在复杂谓词下，其实是有一个降维打击的，相较于其他方法，它在复杂谓词下其实表现得尤为出色。其他的大多数方法要么在复杂谓词场景下会完全失败，要么就是需要很大的内存开销来进行索引。接下来就来看他的实验。硬件的话，这是处理器的硬件，内存是 256GB RAM，操作系统是 Linux 与邦图，编译器是 GCC 杠O3，基础库的话是基于 FAS 库进行 C 加加实现。计算精度他说均采用 Float 32 单浮点浮点数，为此使用任何标量量化和成绩量化，所以排除了量化损失对。对检索准确率的影响。
数据集的话主要是下面这两个，一个是YFCC， 10 兆的大小，一个是Active， Active 的话是 2 兆的大小。向量维度一个是 192 维，一个是 384 维。独特的标签数一个是 1000 个，一个是 100 个。平均每向量的标签数是 5 个和 9 个。对比基线简单看看就可以了，大概就是他会和工业界和学术界的各流派标杆进行对比，比如经典的向量索引变体，还有先进的过滤专用图索引，比如说 filter disk aaccurate，之前提到的，还有关系型数据库的扩展，比如这 page vector，还有退化基线，比如这个prefiltering，直接来看这个实验结果吧。
这是单标签下的一个性能压测，它是不同选择性单标签过滤器的召回率与查询吞吐量之间的关系，我们一个个来看。上面的图是针对 YFCC 杠 10 兆数据集，下面这个图是 RSA 数据集。每个子图的话代表是不同的选择率，都是比较低的，像0.0013、 0.0016 这样。纵坐标是 QPS 吞吐率，横坐标的话是re。recall，召回率，我们肯定是希望它的吞吐率越来越高， recall 越来越高，希望这个曲线是在右上角的。可以看到 creator 是黄色的标签，它黄色标签的话，在这些子当中基本都是在右上角的，说明它的 QPS 和 recall 都挺不错。可能会有人问了，诶，它好像不是最高的，为什么还能证明这个方法很好呢？像是绿色的 Poly I V I V F 是在右上角的，它的这个 BS 会更高。像是下面这个图的蓝色的曲线， per label HN, SW 也是在上面。原文是这样解释的，说 Poly I V F 通过在各标签索引间共享向量数据，始终能实现最佳效果。
per label HN, SW 及 I V F 在 active 上性能因内存太。消耗过大，把处理 1000 万规模的 YCC 杠 10 兆数据集，就是有一个方法它是在这个大规模数据集上首先是用不了的。这些方法都存在过高的内存消耗和构建成本，在后面的一些实验结果当中也会体现。所以说虽然有的方法性能更好，但是它开销，就 QPS 虽然更高，但是它的开销太大了。
我们再来看一下这个复杂谓词的检索性能吧，可以。重点关注的是蓝色曲线，就是creator，它的对比是紫色的accurate，可以在这里看到， creator 也是在比较右上角的一个位置。它采用with，他说他相较，他用这个是想证明采用 with 索引的 creator 系统，相较主 with 方法实现了接近最优的性能表现。
会看到一个是 creator crea- creator ids，就是主索引的，它是为了。证明就是它每次复杂位词来了之后，它才临时构建索引是完全 OK 的，是高质量的。而且这两种过滤类型呢，都显著优于Accuracy，就是它的先进的对比基线，也就充分展示了查询时临时索引构建的高效性。然后，上面有两条就是比它看起来要好的曲线，这两个曲线的话，它不是临时构建索引，它是提前构建好的。但是提前构建好。的话，它很难涵盖所有的组合，就是它的内存开销和构建成本是很大的。综上呢，就可以表明， creator 为单标签和复杂位词搜索都提供了一个统一的解决方案。虽然它在所有选择率水平的单标签过滤器上都提供了具有竞争力的性能，但它在复杂位词上表现得还更为出色。而现有的大多数方法要么完全失败，要么内存开销就是令人望而却步。
接下来看一下它那么高性能背后的代价，它资源消耗，看到纵坐标的话是索引构建时间和内存净开销，横坐标代表是不同方法，我用红色圈出来的最右边的话是creator，就是本文的实现，看到 creator 的两个柱子都是比较低的，说明它的索引构建时间和内存净开销都是比较小的。之前提到的两个 QPS 比它更高的方法。一个是 Pale ADF，一个是PHNSW，它的开销都是显著大于 creator 的。
我们可能会注意到，有一个柱子，右下角这里，它的 PG IBF，它的内存及开销是小于去 creator 的。原文中就说这是得益于紧凑的反向列表编码技术，但是它的搜索性能是。逊色于内存处理方案的，虽然内存静态消小，但是搜索性能没有那么好。上面这个图可以验证它搜索性能不好，PGI、 VIF 就是棕色的曲线，然后棕色曲线是明显在黄色的曲线下面的。
接下来的话会有一个综合的图，它会权衡索引开销。构建时间和搜索延迟之间的关系。我们先来看纵坐标是索引的开销，当然希望它越小越好。横坐标是构建的时间，也是希望它越小越好。靠近左下角的话是优质的节点。它用圆圈的大小代表 record 等于 0.9 时的延迟，也是越，圆圈越小越好。我们来看一下 creator 在哪？ creator 的话，它是在一个接近左下角的位置，这个蓝。色的节点也被我圈出来了，它在处理低选择性查询时是具有竞争力的搜索性能，同时在所有评估方法中保持最低的内存开销和构建时间。
可能会注意，如果只看单项指标，只看这个索引开销的话，会有一个节点， PGIBF 是低于 creator 的，它更靠近下面。它也解释了，它虽然说。说它的磁盘占用空间极低，但是搜索性能是逊色于内存方案的。又说， creator 只需要少量额外资源，就可以实现与最佳方案相当的性能表现，非常适合对内存容量和构建时间有严格要求的应用场景。
我们再来看一下动态更新和扩展性，主要是标签向量的更新。性和随标签数量增长的扩展性。现在看左边，左边是一个动态更新延迟，可以看到纵坐标的话是延迟，横坐标的话也是不同的方法。像我们标注的creator，它不管是在标签的插入、向量的插入，还是删除方面，它都是在一个比较低的延迟的位置。他说， creator 的向量差。速度比共共享索引会快 4.4 到 58.7 倍。同时呢，它又是全场唯一一个能够完整支持向量删除、标签删除、向量插入、标签插入的索引。很多图索引甚至没有实现动态删除的功能。
接着我们来看一下右边的这个图，右边它是随标签数量增长的扩展性。像是creator， creator 是黄色的曲线，也标注出来了。浅绿色的这个线呢，代表的是仅向。向量存储容量大小，无论是黄色还是浅绿色的线呢，都展现出卓越的记忆效率。它随着向量数的提升，它的 memory footprint 是没有显著的没有显著的增长的。
最后是消融实验，消融实验它主要探讨了三个图，一个三个方向，一个是结构约束。结构约束什么意思呢？就是，它在实践当中把。个单标签的数呢，其实是挂在了逻辑挂在了基础索引的那棵树上面的，对，它其实是有一个结构约束，也就是说单标签小数不能自己去计算最适合自己的聚类中心，就要强行套用寄生在那个全聚大树的物理骨架上。那么它的这个对比就 creator 是蓝色的曲线， unconstrained 就是没有采用结构约束这条曲线，可以看到它的两个版本是基本。重合的，这表明它引入的结构约束对这种嵌入式标签索引的质量影响微乎其微，所以它的实现是没问题的。
图 B 的话则是把原始数据集与随机打乱的数据集之间看了一下吞吐量和它的召回率之间的关系，看到几乎存在相同的一个权衡关系，由此验证， creator 在不同数据分布下依然具有稳健的性能。那么像是图 C 的话。展示的是随着数据规模的增长，遍历开销的一个变化。看到数据规模从 200 万到 1000 万增长，遍历开销却从 26.9% 降至18.9%，日开销在较大规模下仍然处于中等水平，且持续的降低。
讲得可能稍微快，差不多就讲完了，大概这边我。文章的话，它首先是提出了这个双索引，主要是做了这个 creator 的实现。高选择率下呢，图索已经做得很好了，低选择率下就由它 creator 来保证。它这这个思想呢又有具有工程的可行性，它通过对内存构建开销等各种这些结果的衡量，发现开销很小，而且结构约束的思想呢也对检索性能和准确率几乎没有任何的负面影响。
还有。能，它能应对所有的复杂位置场景，它会抽出一颗临时的索引树来实现极高的查询灵活性。同时从它低选择率下的实验来看呢，它确实表现极佳，代价很低，同时效果很好。我在想，它如果增加工作的话，就可以考虑就在多并发场景下呢，可以考虑建一个缓存，就对一些复杂的索引，能可以不临时构建，而是采用一个缓存。就直接读取。左下角的话是他记在总结里面写的一段话，就是可以看到他的源码是开源的。大概就是这些了。
OK，大家先看一下有什么问题吗？泽斌你们提个问题吧。
我现在问一下这个，这个只做了，就是往前翻一下。
听不见，这个有点。
你说话听不见，应该。
啊，你好，哈喽，能听到吗？啊，就是我想问一下实验里面，前几页。对，他这里比较，他有一个有一个点的，那个，对，这里他这里的实验他有说对齐。提到 recall 是多少的吗？那它这个 PAR 是是哪一个方法呀？对，我看，对，我看 PR 好像它的它的性能特别好。定型操作。对。对，联合是定型的检测。
啊，这个差不多，但是你大点声。
尾扩是 0.9 的。
EAR，但是 Pale AIBF。
是这个。就是完。这是叫 Pale IBF，我当时也没有去搜这个，它应该解释是当前最顶级的一个开源分区的过滤检索索引之一。没事。
哦，明白，好，谢谢。
我其实还是没太理解，说这个嵌入子树，就是所谓的标签树，它是怎么嵌入的？我，比如说标签树的。假设编树一个节点，它还是和基础树的一个节点，其实索引的数据是一样的，对不对？是吧？
它其实是对应的，像这个层级完全就是复用的，它像是在这个基础上面，首先比如说它这边它会，像这个图里面，它这个会有一个叶子节点，但是原文当中它可能不是叶子节点的，会在它的所谓的叶子节点里面加一个缓冲器把。 buffer 里面存储一下，符合它过滤条件的向量ID，它的层级，物理架构是完全和基础索引是一样的。
是，我没，没太懂，我看 base index 是，也是，现在这种两层结构，对吗？三三层结构，对吧？还是说。还是说这边，还是说是右边这个，只有层次，只有这个 16 个聚类的这种结构。
左边这个其实它就是一个层次化聚类，像是过滤器缓冲区这些，额外的东西都是都是。对，它是一个分区索引，分区，对，它是分区，对，它是一个 Kmeans 树。对，他不一定是三级，我也不知道他，他可能就是 K 级，比如两级、三级都可能。
我知道我知道， base index 是个层级聚类，是吧？不是，我知道分类，它是分层的kmeans，对吧？kmeans，就是说它本来就有这个三级了。
我知道，假设以这个例子为例啊，它本来就是一个，你可以说它 2 级，好，3，就是大概就 3 级吧，如果加上根的话，对吧？那右边的就是它这个，也是和它一，一模一样的 3 级结构，对不对？是吧？右边的比如说假设我最左上角的第一层的。左上角的这一块，它的索引里，它的索引里边会索引啥呢？比如说我左上角的 label 吗？还是索引啥？我有点没懂。对，举个例子而言。
对对对，一样的。
左上角会索引是吗？看一下，拿这个。
我看一下这个。拿这个图来看的话，它其实主要是只有叶子节点有这个数据，它叶子节点里面，它的其他节点其实只是访问过了，其他节点其实是没有存出什么有效的信息的。但是叶子节点里面呢，会存这个buffer，会存这些向量，上面这些就是指它，照。照着技术索引这样访问下来了。
这个所谓的buffer，就是说他们有一模一样的label，是吧？
它 buffer 里面存的不是，符合，对，有一模一样 label 的向量 ID 存的是，它，对，是的是的。
对啊，我就说，就这意思吧，就这意思吧，每个节点里面的每个节点里面存在，它一个buffer，这个词就其实就不对，这个 buffer 一般是写的，一般来说，有同样的。 label 的所有的向量，会放到同一个节点里边，对吧？这意思。
嗯，对。
是的是的，像是叶节点的话，它这里存的可能是下面所有，直接符合的。对对对，是的是的。
既包含比如说假设包含，比如说它的子集，对吧？可能是更深一层的，那它我想想，它会，它怎么叫嵌入到这里边？我怎么。就是说它只会包含，比如说在你再返回，张烨你再返回去那个上面那一个。对，就这个图，对对对，就这个图。那比如说还是右上角这个子，右上角这个块，对吧？那右上角这个块，如果你看，他现在举的这个例子里边， for label index，右上角这个大的类里面只有三个点喽，但是左边那个 base index 里边。它的聚类可能有很，左上角这个大块的聚类里面可能有很多个点，那为什么 party index 为啥只有三个点？就是说只包含这一个条件的只有三个点，那剩下的就往下再，相当于是再往下，到它的孩子。上面这个呀，上面这个呀，对呀。
第一个图是吧？
是说上面图还是下面？上面这个图。上面就是他剩下那些，他当看不见，是因为他，他可能访问了他的这根节点，问了。往这个分支走的话，有没有这个标签？不能过滤器如果给他说没有的话，他直接就是当看不见下面这个分支，就把这个分支也砍掉了一样的。他只看得见这些是可能有标签的这些路径。
OK，有标签的这些路径，我知道。我明白，我知道那就是说， per live index 里边，比如说右上角这个大的块，那它的所谓的 buffer 或者叫，它存储的 letter id 就只有这三个，对吧？但是实际上，这个大的聚类可能，如果是按层次聚类的话，这个大的聚类是应该是包含，比如说 base index 里边那个右上角的这十几个，对吧？其他就 label 就往再往下走了。
对，就其他的路径他其实还在技术索引里面，但是他访问的时候就当看不见，因为他访问的时候就是用过去告诉他说这里面没有你想要的标签，那么他。他直接这个就不要了，所以他的这个就会看到有一些其他的很多个分支，这些点他是当看不见的。
嗯嗯，对对，他可能是下面还有。挺多的。是的是的。
所以他其实也不像，也不用找聚类中，也不用算什么和聚类质心的比较，选聚类了，对吧？就直接按照这个数走。按这个数走，哎，这个好像另一个 label feature 的论文。对。那个是图索引的，也是建了一个类似于的这种标签树的东西，你找找，是不是叫我找一下，行，其他同学先看有什么问题。
对，他完全不用，对，直接跟着走，就是一条路判断下去就行了。
这么？
你说。又又出现你了。对。
喂喂喂，哈喽。就是他，我看他论文里有提到 range filter，他把 range filter 归为多标签一类的，他因为 range filter 就是可以理解为，他每一个数值都是一个单独的label，很多很多个 label and 在一起。
OK，能听见。
我在阅读的时候，其实没有特别去看他怎么处理range，但确实他是把像时间和日期，他都是包含在他的标签里面。
嗯，那他实验里有，针对 range 做测试吗？还是说只做了 label 的？因为我看他比较。
他应该是把 range 包含在 label 里面，他没有特别提及，他哪些是关于 range 的，我在看实验的时候没有注意到这部分，当然也不排除我看漏的可能，就我看的是。是实验的主干，没有特意去搜，没有恒睿相关的。
哦，明白，他应该所有实验都是做的label，对。
对，我会估计他是把 range 也是，就是当做标签，是因为他当时原文说的是会再向上注日期、价格等标签过滤条件，我当时在想这个日期和价格，他不应该是。这个范围过滤嘛，为什么归于这个标签过滤？但是这个是他的原文的一个，原文的一句话，这是第一句话。对我，所以就是可以看一看他是不是就是把 range 也归成了，像像像你刚刚说的，当成一个一个的数值。
嗯嗯，就是。
啊，OK，行。
还有一个小问题，就是既然它是针对第一选择度的，这种索引，过滤条条件。那他怎么判断什么时候用它，什么时候用一个高选度支持的图索引呢？他会有说吗？
就原文就提了一嘴，就我记在这个备注里面了，就其他地方他也没提。他其实他说的是在我们的集成实验当中，会假设用户执行选择率估计并通过离线分析确定固定选择率阈值来选择索引，说与 Acron 的方法类似，这句话我没看懂。对对对对对。
嗯，OK。
这是个，确实是个。这是个通用算法，简单来说就是你离线跑一遍，看看哪个程度下什么时候好，定一个阈值，以后就都按这个阈值来去来去选，对，选的那个估计就是一个采样的方法，就很简单的方法，通用法。对。是，这个延嵩还有延嵩一个老。熟了，因为他就做这个东西，然后我估计桂琪也挺熟的，因为他正，他们正在做这个东西。对。对，OK，应该就是基本上都是这个方法。我没有其他问题了，哎，大家还有其他问题吗？
是的，他也没有多好。那他就没有提出别的方法，他全都在讲这creator，他图索引之类的也没有提到。
啊，好，就是选择率估计，来选方法。
诶，我有一个问题，可以听到吗？就是我觉得师兄刚才应该是说这个方法跟UNG。有点像是吗？其实他在搜索的，他在搜索的时候也是会有一个树结构。那么 FF 索引相比于图索引，它一个显著的问题就是它在搜一个 partition 的时候，会有大量的冗余的 IO 和计算。那么我的据我所知， UNG 的话，它应该是没有这个情况的，因为它只要搜到某一个子集，那个子集里应该都是符合这个label，就是符合这个标签要求的点。但是这个。他是这种情况吗？我看刚才那张，师兄问的那张图里边，你可能可以通过布隆过滤器等等的方式来判断一个partition，一个分片，一个分区里边是有你所需要的 label 的向量的。怎么办呢？你会把这个分区里所有的向量都搜索了吗？如果我已经递归到它的叶节点，因为它的叶节点里也并不是只有一个向量了，对吧？它的叶节点其实是有一个 size 设置的。就是他在探索他这个叶节点的时候，是不是也会像传统的 IVF 一样，他会遍历到一些可能不符合他标签的一些点，或者有一些遍历或者IO。
嗯嗯，可以听到。
对，是的，嗯。
首先他确实进行了剪枝，之前那个师兄问的那部分过滤器就是剪掉一些大的一些枝，但是也确实会存在部分过滤器是误判，就他说下面有。标签，但其实就是可能没有，就是这样一个情况。然后像是我想怎么想怎么说。但是他做的比较好的一点就是他是应该是不会，越稀疏的场景，假设稀疏的场景，他可能要访问很多很多个节点呢，才能找到他想要的那些符合条件的向量。他比较聪明的一个做法。就是它越稀疏的话，它其实会把它提到越高的层级。这个应该是和算法二相关，如果它在扫描的时候，发现怎么说呢，它这些我这个标签的现在很少，那么它会把它直接的存在比较高的一个节点的位置，越稀疏它就在越高层，越稠密的话，可能才需要越往下。
搜，所以说下次他可能碰到这种很稀疏的向量的时候，他可以只访问就是比较浅的上面的几个点，就是他就能从这个 buffer 里面读到他想要的向量。就是像像这张图里面，举例来看的话，假设他访问到上面的这个灰色节点，得到他想要的标签了，想要符合标签的向量，就是因为假设他的阈值是3，那么说明往下再搜索的话，顶多就是有 3 个符合条件的向量。
那么直接把这三个符合条件向量都存在了这个高层的灰色的节点里面，从而保证了他只要访问到这，就从 buffer 里面读到了符合条件的向量ID，他就不用继续往下搜了。不是每次都是他，如果，除非是大于3，他才会继续往下搜，小于 3 的话，直接他就得到了答案了。我不知道这样说，我清楚，哦，嗯。
就是它，我好像理解了，它其实是会存在一个把 vector 的，把符合这个 label 的向量的 ID 做一个存储，但是。它也并不是并不是所有都存，它是有一个阈值的，就是像你刚才说的那个3，就可能它的上层节点会预存一些下层的就是符合条件的 label 的ID，它但是它可能也不会存太多，如果太多的话，它就会告诉你到下一层的节点中去找，这样。
是的是的。
是的是的，它就是设置了一个阈值，那么小于这个阈值的话，它就一定会存在这个节点当中，它就不用往下搜了。就他只要搜到这，就说明，就是下面还有更多。如果搜到这，下面没有很多的话，他直接就读到数据了。
嗯，好的好的，我理解了。
啊，也就是说，这个就是父节点和子节点，它其实并不是一个类似于子集的关系，对吧？其实父节点可能，可能那个 label 更少一些，子节点可能，可能 label 更常见一些，它其实不是个包含的关系。嗯，对，嗯，对。他就是说只是复用那个 base index 的父子的关系，但是其实在 label 上没有一个包含关系，不是成子集的关系，应该是。
他其实是，只在叶子节点。里面存的有向量，但是它可能会在父节点里面，基础索引里面不涉及这个哈，单标签索引它会在这个，这个父节点里面存储这个buffer，它确实不是包含关系，就是可能上面有个buffer，它上面已经写过了，它下面可能就不写了，因为根本就不用往下访问。
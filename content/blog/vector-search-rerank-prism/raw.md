---
draft: true
---

OK，那我现在开始吧。行。好，大家好。本次的论文分享由我来进行，这个是这篇文章的题目，它是今年 Eurocvs 2026 上的一篇文章，它是上海交通大学的文章，这个文章它主要的研究问题不是Ann，也不是 agent memory，而是工作流中这两个部分之间的重排部分，就是 Reranker 的部分。接下来我先介绍这个工作的一个背景。
首先当用户输入一个 query 之后，会经过稀疏检索和稠密检索，稠密检索就是我们熟悉的Ann，然后两方会各输出 10 个候选，但是这个时候这 20 个候选我们认为它的精度还是不够高的，所以需要再经过一个重排的部分才能输入给下游的应用，比如说下游的 agent 大语言生成模型，然后这个相似性的文件检索等等。
那么这个 RE rank 部分它就是。它会利用模型从更加细粒度的角度去分析 query 和候选人之间的相关性，从而做出精度更高的一个排序。但是从这张图中我们可以看出，目前 Rewanker 这部分在整个工作流当中已经占据了 96.3% 的延迟和 67.6% 的内存占用，它算是整个流程中急需优化的一个部分。然后 an 的输入只有一个query，但是整个rew。
ranker 的输入是 query 在之前找到的若干个候选，传统的处理这个方法的思路是 by encoder，它类似 an 处之前处理向量的思路，它是将 query 和候选分别进行embedding，然后对比它们之间的相似程度的。这种方法虽然高效，但是难以去进一步捕捉细粒度的 TOKEN 级别的。就是这个 query 和候选之间的关系。然后 Cobert 这个工作它曾经试图去做 per TOKEN 的 embedding 来缓解这方面的问题，但是这个带来。
非常显著的存储开销，目前主流的解决这个问题的方法是 crossing encoders 这个这种架构，它会将 query 和一个某一个候选，它们两个视为一个整体输入 Transformer 架构的模型中最终会输出一个相关性评分，它利用这个里边的注意力机制，从很深的层次，从更深的层次去捕捉 TOKEN 级别的 query 和候选之间的关系，并以此达到更高的精确度。
这种架构也分为两个分支，一个是 encode only，一个是 decode only。 encode only 用的是双向的注意力机制，它使得 query 和这个候选在编码的过程中都是彼此可见的。同时这个模型的推理代价更低，就是推理和部署的开销都更低一些。
还有一个 decode only 架构，它是本来适用于大语言模型 generation 那部分的这个架构，然后它能够更好地去回答对于目前的某一个。 query 这个候选是不是一个合适的答案？这种比较复杂的问题，它是因果注意力机制，同时它的部署和推理的成本更高，但是这但是整篇文章主要讨论的并不是这两种模架构之间的差异，所以在它的实验中这两种架构都有设计。这一页介绍的是上面所说的这个 RE ranker 部分目前主要的开销来源。这篇文章的作者将这个场景定义为边缘计算的设备，就比如一台笔记本电脑这样。首先，它延迟的主要开销是来自于计算的，来自于 Transformer block 每一个 Transformer 块之中大量的矩阵乘法，例如它的这个注意力机制就会有 OL 方 d 的复杂度。然后 SFN 网络有 OID 方的这个计算复杂度，所以它的计算量非常的高。另一层，另一个层面是它的内存占用，这方面的开销也非常高，即使我用一个 0.6 billion 的ranker，它整体的模型参数就可以达到。
几个 GB 级别以及它 Transformer 中间还会有许多的中间层张量，比如说 QKV 的投影， attention 的分数以及 FFM 的输出的等等，这些都是需要在中间过程中存储的，会极大地增加这个峰值内存开销。但是目前现有的一些有关大语言模型的优化，对于上述的 ranker 这个场景基本上是无效的，就比如说左边这个以底 coding 为中心的优化，但。但是 rank 它是它最，因为我们知道它最终是只输出一个相关性评分的，所以它相当于是一个 prefill only 的模型，它并没有 decoding 的部分以及这个长上下文的优化，因为 rank 的输入它已经是一个 query 加若干个候选了，它本身就很短，而且信息非常的密集，所以它也用不上一些在输入方面的稀疏处理或者剪枝等等思路。
第三个是 training base 的压缩这些这里面包含一些模型的剪枝量化以及early。 Accent 这些虽然可也可以用，但是一般来说比较昂贵，而且后续也需要持续地重训练或者微调等等。还有一个是量化，但是目前端侧方面的设备在硬件层面和核的层面，对于这个 3 比特支架的量化也是没有太好的支持。
context 就这种长上下文优化有，嗯，这个难道不是这，它不是长上下文优化吗？因为它也是把 query 和 query 的一块塞那 query 的。不能只是一个，它肯定也是一个候选的，要是一个文件的片段。
对对对，它应该是一个文件的一个 TOKEN 序列，这样。
对，但是我理解这样子用这种 long context 的这个优化其实也行，肯定也是可以快的。
应对，应该是可以。
大客户可以，大的话那个，对会多，那个应该也是可以的，而且他也不是说是。是每个 key 就是假设一个 key 是个文档，一个 key 是一个文档或者一段文档。你说咱假设切成 512 个TOKEN。每一段都比较长。对，那 10 个就 5, 000 多TOKEN，其实也还可以。
这边文章里面，这边这篇文章的举例里边的举例，一般用的那个候选都是 512 个TOKEN，就是它用得很短。
520 TOKEN，然后TOP10。
对对对，TOP10。
千多的做，说实话也能也会有这个。
对对对对对，他可能觉得对他这个场景来说不是特别的划算。
preface 刚才减脂是吧？
这个就是他在里面列举了大概有这 3 类的优化吧。
就是那种带量化的。训练的时候直接把量化了。造型它其实也是。 4 米的量化那东西，感觉这个东西它因为它是特别在端的场景。刚才说 4 比特的量化， 4 比特量化没有专门的 4 比特的页面或者甚至研发的。
他的意思是 4 比特量化已经算是一个比较通用的执行策略，然后在端侧的话，如果我想做到 4 比特以下的量化的话，但是 4 比特以下的量化加 prefill only 的这个架构本身目前就是一个还没有成熟研究的问题，然后再加这四比特之下，感觉在端侧也没有合适的硬件支持，他那段大概是这个意思。
就是 4 比特量化，虽然没硬件支持，但是。因为想好的解决了。你说你虽然可能只有 32 位的一个预测器，但是做四倍他应该也是处理，稍微处理也行。
那篇文章最后实验对比的时候，他是做了量化的，他有 free 加量化的版本的。
行。
对，他这个可能特别是强调他想做一，如果要是做更小的量化，对，所以他这里他其实是他在实验的时候是有用到量化，然后也验证了在量化的框架下有一些思路的，他可能就是想说明。我可以用，我就是说我可以垂直地使用这个量化的思路，但是我没有去研究，对。
因为我记得之前微软做过一个量化，特别的厉害。就是就直接查表，一直到，然后他们直接做了一个类似的那种嵌入式的一个冰箱贴一样的东西。贴在冰箱上，说你这个冰箱贴就是它可以实现一个大模型了，然后里面一直。
那我继续接下来基于上面所讲的东西，这篇文章的，这篇文章主要的关于 PRISM 的架构，它是基于它在实验中得出的两个重要观察，重要的实验现象。这里先介绍第一个，它发现在序列级别有一定的稀疏性，这个说法比较抽象，主要是看左边这张图展示了一个 0 到。 28 层的 Transformer 架构，然后在这个架构的每一层 HIDDEN state 的输出，作者把它单独取出来，然后过了一下这个模型最后的那个分类器，然后得到了一个中间层展示的分数，然后他把这些分数都呈现了出来。
观察这张图可以看到这个候选人的分数是渐进的出现了分支，并且分支到了完全不同的这个聚类之中，而且这些聚类之间没有太多的分叉，就是没有分叉还是非常稳定的，尽管在。聚类之内，你看这里，它也许会存在一些排名上下的波动，但是在聚类之间还是分得非常清楚的。这个就这就让作者产生了一种想法，他想要去按照聚类对这些候选进行一个剪枝，然后下面的这个实验是进一步佐证了他上面的那个观察结果。
他在 18 个数据集和所有主流的模型架构上进行了实验，并且用这个一个是伽马值，一个是 cluster 伽马值衡量了这个在这个过程中，在过程中候选人分数的一个收敛程度或者说稳定程度，这个伽马参数的定义是在上面是一个 NC 减 ND 除以 NC 加ND，这个 NC 指的是某一个中间层与最终层之间相对顺序保持一致的候选对的数目，而 ND 是相对顺序发生反转的候选对数目，所以这样计算出来的伽马值，它的值越大。就说明目前的这个中间层它的状态已经非常的接近，最终的结果就是已经非常的接近收敛之后的结果，并且整个结果是比较稳定的状态。然后它还有一个 cluster Gamma， cluster Gamma 的计算公式也是这个，只不过它只去记录某一个候选它的排名在聚类之间发生变化的情况就是如果它只是在一个聚类之内发生了一些变化，它是不记录的，所以 Gamma 它看的是聚类的这个收敛程度。
那么看左边这张图，这个，这b，这个蓝色的 BGEM3 是 decoder 案例架构，然后这个紫色的。毕竟这个是 encoder 类架构， decoder 类架构就是它是两种架构。可以看到，如果我只计算这个伽马值的话，整张图它的这个收敛程度肯定会肯定是逐步升高的。但是如果要是我只看聚类这个稳定性，它就是一直是飘在最上面的这条线，你可以看到对照上面这张图，不然不管我是在这个 7 层之前只有一个分叉还是两个分叉，还是 3 个分叉，但一旦分叉之后，在这个候选的排名在聚类之间变化的情况基本都是没有的，因为这个值一直都是一。这就这个实验就侧面佐证聚类之间的稳定性，所以我按照聚类去剪枝的话，对于精度的保证来说是比较安全的。
然后根据这个现象， Pre 的作者也给出了一个比较感性感官上的解释，就是 Transformer 早期的层，他们可能更加侧重于捕捉候选之间非常清晰的相关性上的差异，所以它会导致分叉会产生这种完全不同的这个clusters，但是到了更加深的层。每一层面对的候选其实都已经非常的相似了，所以它要捕捉更加细粒度的方向上面的差异，所以它可能会出现一些聚类内颠簸的情况。
这个，那个上面这个图一条线就代表一个category，是。应该是。然后就是会出现过了，到了后面层 20 多层就会出现有很多 category 在得分是很相似的。
就是它分成了几簇吧。参数这样，嗯，OK，然后刚才我说的是第一个实验现象，第二个重要的实验现象是他发现了一个可以做 l 计算重叠的机会，就是传统的这个自回归的架构，自回归的生成式模型里边其实它是很难去做 l 和计算重叠的，这是因为在自回归的架构里边它是一个TOKEN。很生成的，那么它的每一个，那么意味着它的每新增的一步，它的那些attention、l、 LMP 等等的计算，它只会在新的 TOKEN 上去做，所以它每层的计算量是很少的。
它的 decoder 过程通常是一些小批量的小矩阵的计算，这会导致 memory bound 更加的明显，然后同时 GPU 的利用率也打不高，同时延迟还是很长的这样一个问题。但是如果在 reranking 的场景。一下就会有所改观，因为我们看到了上一层，不是说他其实是想按照句类去剪枝的，但这个就会导致一个问题，就是你必须要把所有的候选一次性地都输入这些层，或者我是什么意思呢？就是我过完每一层之后，我现在希望得到所有候选通过这一层之后的临时分数，因为如果不是这样的话，我现在得到的排名不是全局排名，他还是没办法剪枝，也就是说他如果要想做个，要想利用刚才那个实验现象，就。
他就要把所有的候选一次性的用，通过一个 batch 传入这个模型之中，这样就变相的把所有的计算都聚集起来了，这样他做的就是一个，就是attention、MMP、 projection 这些的执行都是更大块的矩阵乘法，然后每一层的计算时间也更长、更稳定，这个就给 IO 和计算的重叠创造了机会。
所以其实前面那个就是它就是一个 prefill 的。架构的一个问题。就 decoder 本来就是一个 number 到的过程， prefill 本来就是一个计算报。因为它那个prefill，所以它确实它没有任何的number，几乎没。
对，prefill，对，因为所谓的 decoder 它其实可以用那些 key cache。因为它有key， cache 已经计算好了，它每次只需要现在一个TOKEN。一个惩罚，一个TOKEN，对应的一个 letter 的一个。 Tensor 的求法？但是如果是 prefill 的话，那就是你要计算的不大。
所以说 prefill 本身就有这种特性。
对，因为它就是个 prefill 过程。
就算它不是把所有候选一起输进去，它也是有这个特性的。
prefill 本身不是，那是你一个一个输，那确实也分散，你就是，但是你让他就说我不是一个batch，就是一个 batch 里面我不包含所有的参点。
比如说我分成 4 份或者 8 份做的这样。
然后再评分，是吧？他这个意思啊。那会，那计，那相对来说计算的报的会轻一些。因为你那个 testertester 那个张亮的规模没那么大，没那么大。那个矩阵的维度没那么，没那么高吧。然后他是说是，我就是不去分批。
对，他就他不分批，他就是一个批次，但是这样可想而知会导致他内存打得非常的内存，是就那个峰值内存非常的高。
峰值内存高，对 KV 开始会多一些。但是更多的是计算高吧。
计。
那这个不是不用抵扣，不是他，他也要算 KV 的矩阵啊。不是，这个也算 KV 的矩阵，他肯定是一个batch，进去之后他生成的那个 KV 就变多，但是他也不会超过 model 的大小，他 model 是个 0.6 B 的，大概率不会超过模型的成本。他不有 5, 000 多托管，怎么可能超过？
他指的主要是他峰值内存，他。他在这方面，他说，他在计算方面说的，因为他只其实只是把计算集中起来了，总的计算量没有变，他主要是他单次他这么做的话，他产生的中间张量就会非常的多，其实中间张量占据的内存会非常的多。
不断地加呀。对，对，我。一次性的。
我感觉他这个文章主要的思路就是他为了不让，他为了剪枝，然后使用了一个batch，然后后面又提了另外剪枝之外的三个优化来削减他的内那个内存占用，控制内存占用。
OK，但它其实还是太侧重于端侧场景。
是的。
就这个人他能占多少？ 0.6 B model，然后几千个TOKEN，一个 TOKEN 出来 KV 兆有一兆吧。反正就是相比前面那个 an 和后面那个过程。他可能是尽量想把那 number 给降下来。其实整体来说肯定没有模型参数。
其实前面这块它也就是一千兆，你看这里它也就是这么多。
对，因为 0.6 B 的 model 本来就不大。 TV CACHE 加KV，加上 KV 的话其实也没有那么高， KV 肯定不会比插入更大。其他的投。
对，所以他可能这个就是他为什么要定这个限定端侧场景的原因。
对，行。
嗯，好，接下来这页我简单地说一下他的这四个优，他这四个优化，然后面详细介绍他们分别是具体怎么做的。第一个就是前面提到的。渐进式的，根据聚类的剪枝，这个是右图中这些，每一层过后你看它会都会连一个分类器，然后尝试判断一下聚类，不是尝试判断一下简单剪枝的条件，如果符合就进行剪枝了。然后这个 overlap layer streaming 是刚才说的那个 IO 计算重叠的部分，这个很好理解，就是我在算这个 layer 10 的时候，我去预取 layer 11 的数据的那个模型权重参数，然后其他层的就全放到 SSD 上，然后还有一个。
chunk 的execution，这个指的是你看层和层之间的计算，它也是分成了很多小块的，这个是为了控制它峰值的内存占用，然后还有最后是一个 embedding table cache，这个是针对模型最入口处的那个 vocabulary 词表去做的，它在它给这个词表做了一个简单的 LRU cache，那么接下来我来详细地介绍。
首先是剪枝这部分，它主要是有两个任务要完成，第一个是。他希望判断减支的条件，第二个是具体的执行减支，这个减支条件判断主要是每过一层之后，我不是已经，我是会算出这一层所有的中间候选人的中间分数，然后他在这里计算了一个变异系数，就是所有候选人的这个右上角，所有候选人的这个分数的这个标准差，除以他们的平均值，这个值，这个变异系数是用来衡量这些分数之间的离散程度的，这个值越。高就说明分组数之间越分散，然后达到了一个他提前指定的阈值，之后他就认为这些分数已经足够分散了。所以我现在触发剪枝这个阈值的设定，我下面再说。
然后如果他确实触发了一次剪枝之后，看到的是左边这张图，首先他会把这些分数拿回来，在 CPU 上做一个kmeans，分成这个若干个不同颜色的这个聚类。然后他会定义一个边界聚类，意思就是比如我要找的是TOPK。对，那么 TOPK 的那个 DK 个候选，它落在了那个，它落在的那个聚类就被称为边界聚类，然后以这个边界聚类为限，这个聚类之上的它就直接选，它会直接选入它的 TOPK 是select，然后这个边界之下它就直接丢弃，这两部分都不需要再经过后续层的计算。然后还有如果处于这个边界之中的聚类它没有办法判断，所以它就需要再经过后续的层，这个东西的停止条件就是当我的 TOPK 已经选齐了，比如说我。
我要找TOP10，我已经找到 TOP8 了，然后目前的这个需要继续计算的这一部分正好是两个，然后填上去就结束了它的这个剪枝条件判断里边这个阈值的设定，它是提前人为指定的，这个东西其实是一个精度和延迟之间的Tradeoff，就比如我把这个阈值设得比较低的话，那么就说明我会更早地触发剪枝，以及这个剪枝进行得更加激进，但是它的好处就是有更多的候选它。不需要经过后续层的计算了，所以整体的延迟会降低这个值。
不准。对，会降低精度，因为它减只减，它可能减，只会减掉一些有用的东西。然后这个值它是允许用户去人为地指定的，当然用户也可以仅指定一个最小的精度要求。然后 Pre 怎么架构是支持去根据用户指定的精度调整的？大概就是它会离线采一些query，然后做剪枝，不做剪枝分别算一下，然后看一下。精度下降的情况，如果下降得太多，它就提高这个阈值，大概是这样做。
但是这个有啥问题？就是他做剪，他这样剪的话，比如说我已经进了 TOPK 的那个class，然后直接就放到结果里。但是有一些查询的那个场景应该是要精准的排序，就是123456，但是他放的TOPK，比如这个假设这个文章里面有个绿色的，这里。他说选中了你这个绿色的，比如这个绿色的 class 里面有两条query。就是两个分类的，然后在这两个分类的它其实后面的排序可能还会变。
是这样的，所以他这个文章前面说他针对的场景就是不需要这精确排序。
对，因为有一些算分模型，它是算分模型，它最后那个普适普遍的规则就是你要有一个排序，就 12345 这个序，这个序也很重要。他这个不是有一个明确的打分，真的就要这个分。但是那个分就不一定准，就可能更准。
因为他减值。知了，他要是完全不减知，应该是真的。
对，更准了。
他这个出发点就是他认为这些模型、这些下游应用其实只是想要找TOP5，但是并不在意 TOP5 内部精细的排序，他是基于这个前提去做的，可能我前面没有说清楚这点。
那跟咱们现在的 NA 差不多，咱们现在 NA 就 top 就好， top 就是。
是的，那是的，顺序不确定的，那我继续，然后这些介绍。那个它为了控制这个内存占用的一些手段，一是这个overlap，这个就这个 overlap 本身很简单，就是我在算 layer i 的时候，我去并发地去预取 layer i 加一的那个权重参数，然后 layer i 一旦计算结束，我就把它的权重全部丢掉，然后用那个空的 buffer 再去取 layer i 加 2 的权重参数，但是这个东西它是跟下边的这个 chunk execution 两个融合在一起执行的，然后对于这个 chunk 的SA。
q 是对，这里先说一下它的这个内存占用会，它那这个内存占用汇报是因为比如说它 60 个候选的话，每一个候选有 512 个TOKEN，然后它用一个 0.6 B0 的模型，那么比起这种将所有权重都放在内存里边的这种，这个执行架构，也就是后面实验中的一个叫 HF 架构，它会将整体的内存占用提高44.8%，也许实际没有提升多少，但是这个比例还是比较高的，然后它这个分块的执行。
呢？就是希望我在计算每一层进行计算的时候，我把它切成了若干块，然后这些块之间是串行地去执行的，也就是说当前我的内存占用其实是一个块的，这个一个 chunk 里边的所有的候选，它产生的那些中间张量，中间结果它用这个东西来控制了一下，因为 chunk size，对， chunk size 的设置，如果设置的，如果设置得太高的话，它的内存方面可能没有明确的缓解，如果要是设置得太低的话，它就不太利于利。比如说 GPU 这样设备的计算能力了，对，还有一点就是除了它这个，它我刚才说的这个chunk，我们直观的理解它是切分了输入的这几十个候选，但其实它还有一些具体的数据可以切，就是它从上一层输出的那些 HIDDEN States，其实这些 HIDDEN States 也是可以按照 chunk 去分别切分出来的，然后这也是下面说的它提的那个。
Dynamic overloading of hidden States 所以它最这个事情最终的结果就是当我去计算一个 layer 的这个数据的时候，我的内存中目前同时存在 3 个，最多同时存在 3 个chunk，一个在进行计算，一个在overloading，一个在prefetch，然后分别它们都是在处理自己目前 chunk 对应的那些 hidden states。这部分大概是这样，然后是最后一个小的优化。这个优化的前提是他在做完前面那么多的优化之后，他发现在整体内存占用的瓶颈反而变成了这个 re ranker 它本身的那个词表。这是说，比如说我是一个 0.6 B 类的模型，它的词表里大概会有 15 万条TOKEN，但是由于这个 re ranking 的任务，它本身的输入就是一个 query 加几十个候选，这里用 20 举个例也可能稍微多一些，然后每个候选大概就是 512 TOKEN 这样的样。样子，所以就算他们里边每个人的词都不一样，最多也就是 1 万多个不同的TOKEN，那么你没有必要把 15 万个 TOKEN 全部放进来，于是他就基于这个思路取了整个 vocabulary size 10% 的大小做了一个 LRU cache。
这个size，这个词表是啥做的？哈希。
哈希，好像是。
对，因为感觉像是哈希。
对，我记得是哈希。
那应该就是做一个TOKEN。就是切下来。
然后这个，对，然后这个就是整体的优化，如果一个 query 进来的话，它会先遇到刚才说的那个 L1LL 1 CACHE 的优化，然后它在计算每一层的时候是分层分成不同的 chunk 去计算的，然后同时层与层之间的模型参数的 load 存在一个预取的，这个就是计算和 l 重叠的部分，然后同时每一层的结果计算完之后都要进过一遍这个分类器，然后得一个中间层分数，然。然后去判断剪枝条件以及具体的实行剪枝，这个基本就是 PRISM 总体的一个架构。那接下来我来介绍它的实验部分，它的实验在英伟达和 apple 两个平台上进行。
英伟达是一个笔记本电脑，带了一个 8G 显存的显卡。 apple 它好像是一个迷你 apple 的一个迷你主机，然后它主要对是 Mac mini，对。对，对，然后它主要对比的指标有延迟精度，还有内存的占用，它主要对比了五种架构，一个是 reason 它自己，一个是HF，这个还是 HF 是一个 Transformer 的标准实现的 Python 库，它在运作的时候会将模型全部的参数都存在内存和 GPU 显存之中，没有任何其他的优化。
然后第三个是 HF offload，它是在 HF 的基础上将所有的参数全部都卸载到 SSD 上。然后每次在计算之前将它们取回来，计算之后立刻丢弃。第四个是 HF 加量化，这个是 HF 架构，加了 GPTQ 量， GPTQ 量化这个量化跟前面的这些 IO 方面的优化都垂直的，所以最后 Pre 的也给自己实现了一个 Pre 加量化的模式，用来跟这个 HF count 去一起进行对比。然后左下角是他使用的 Revanker 模型，这个模型的 size 从 0.6 billing 到 8 billing 不等。然后架构既有 decoder only，也有 encoder only。
他的实验大概分为两部分，一个是在 microbenchmark 上进行的，这个里面有 18 个常见的标准数据集，然后同时还有一个真实世界负载之中的实验。他在其中他用到了 RAG agent memory，还有这个大语言模型、长文本筛选这三个场景，这是他第一个实验的结果。
从左边可以看到，左边这个 module 代表上面说的五个module，然后 system 和 baseline 它分别是分成无量化和有量化的版本去对比的，同时它给出了 Precision 150 的三种配置情况，然后这里给的是延迟的降低情况，还有精度的损失情况。这一天所指所谓的这个平均值最大值是那个 microbenchmark 里边 18 个数据集取平均或者取最大等等的一个结果。然后这张图上可以得出的信息，首先就是观察这些精度，其实 Premium 架构对精度的损失一般是不太明显的，同时它可以非常显著地降低延迟，最高的情况可以降低达到在这个 Encode 案例架构上能降低达到 89.2% 的延迟。
它精度是还会有提升吗？你说千万 38D 的 top one 的精度在。
是是是，是，这个是吧？这个是后续的一个神奇的现象，就是他认为千万 38 B 可能因为参数太多出现过拟合了，所以它的剪枝可能会反而帮助模型绕过后续层的一些执行。
它的记录是根据比，它的那个结果是记录的结果是根据比。
这个我也看了一下。
难道不应该是跟千万 38 B 最后。
这个我也看了一下，但是我没有找到他。
说这种。
他应该是有标注的Benchmark，我感觉是。
就是说这个TOPK。
对，对，因为我在这里面没有明确地看到他的TOPK，他写 TOPK 的生成。
有这样的，因为千问当时发展的时候。
对，就是数据集已经有标注的这个结果。对对对。对，他应该是跟那个东西比的。对，他认为千问三芭比。过拟合了，然后他是反对的，他反而有一些绕过的情况，然后由于 h 在量化这一组，他这边的延迟降低的比例明显低了一些，因为量化本身就是延迟，有很大程度的优化，但是 PRISM 架构依然是生效的。然后对于千问 34B 和千问 38B 这两个比较大的模型， HF 的话，他要把所有参数都 load 进来，就会在端侧的设备上。 OM PRISM 也这次给出了一个比 HF offload 效果要更好的方。
方法，然后第二个实验主要它是对苹果平台和英伟达平台做了一个比较，但是总体来看他们的趋势大致是相似的，这个图左边这个图的每一个小图会对应左边的一个模型，以及上面的一个 Precision TOPK 的这个 k 的配置。然后这里面的这些柱状图代表的是延迟的优化，这个延迟一共半 7 组柱状图，他们。
的含义分别是HF、HFO， HF 加卸载，然后 HF 加量化这个 preemption low 表示的是preemption。使用较为激进的剪枝策略 preemption high 表示的是 preemption 使用较为这个保守的剪枝策略，然后后面的就是这俩分别加量化，然后他们的这个延时是以 HF 的这一组为基线的，它这里是设成了一，然后别人的这个比例全是跟 HFO flow 的去计算出来的。然后可以看出来 Pre 在整体的延迟和效果是最好的，然后 Pre 加量化的那个延，这个效果次佳。
所以他做了量化之后，比不做量化还要慢。
为啥？怎么看出来的？
延时高感觉是吧？就是 APP 上。你说这 monitor high 和那边那个倒数第二个比较high。
是说这个小图吗？对，他那个是倍数跟 HF upload h，你看这上。上面是1。
对，那其实还是corner，做corner，那个做 corner 比不做corner，对吧？
对，好神奇啊。
英伟达 Nvidia 的话，因为咱是差不多的，那个 28 也高。苹果是 0.22 和0.25。那算子，那些算子实现不好能问问大家，问问怡兴，你。你们当时，你当时做做 quota 那个时候，那个就做完之后那个算子会比正常的比如说全精度跑得要快了，如果说没有那个，比如说半精度的支持或者什么。一般更快吧。一般更快算有问题，我感觉 HF 的别的。
对，这个好反常啊。
人脸识别的，对，人脸识别的一些。感觉资料会不会低呢？我看这个是的。 BGM b g m 三十，但是上面的那个。
上面这个其实也对，有，也有这趋势。
也是量化位置差，好奇怪啊。量化的延迟比不量化。是比的量化。
这没看他具体说。
还是说他这个 low 和 high 其实和前面那个 low 和 high 不一样的参数？这个是在不同精度下的那个延迟，就是说。对，就是。他可能做了，因为做了那个量化之后，他可能就是。要达到那个精度要更。对，他的简质可能不能那么激进，就导致他这个，其实这个就相同精度下的。对，他可能过了更多层，然后就是同样的，就是这个过了更多层TOKEN，过更多层 TOKEN 多。
但他实验里确实没有详细地说这个，他也没有说为了为在这个量化与不量化的情况下，他这个参数，他这个阈值的设置有什么变化，他就是仅是说这个图，他对比了两种剪枝阈值的差异。
这个是他都说的是一样的参数，对吧？
他从始至终就没说过他参数设的不一样。
还有upload。
再说啊。
对，然后这个就是师兄刚才说的，前面会有那个过拟合的情况。就是在这个芭比领这块，你可以发现 Pre 怎么它的阈值提高了，然后它的精度反而从 0.985 降到0.965，它认为这个是模型过拟合，然后它绕过了一些层。
这个我感觉它应该是漏和嗨，就是两个漏之间应该是参数是不一样。对，为了达到同样记录，那个作为量化，一般来说比不用量化要差。
但是我也发现在这个图里边，其实它对阈值做一些变动，大部分情况。是效果不那么明显的，你看这个 0.995 ~ 0.996，这个 0.997 ~ 0.998，感觉他这个阈值调得好像这两个平台上，不是对这个精度平台无关的，对这个精度也没有太大的影响，只有这个四冰岭签了四冰岭的这个模型，只有这模型的这一行，好像你看它会有一些 0.705 ~ 0.757 的这种变化，但是别的我感觉它这个阈值好像也没有什么影响。
那影响，对吧？他可能还是他可能会漏。本身设置的那个前置策略也不是问题，或者说他可能，比如讲到一半可能就比较稳定了。
对，我觉得他，对那个阈值，要是他阈值本身的那个设计，那个变异系数，其实我也不确定他是不是能很好地衡量那聚类的情况。然后还有一点就是我不确定他过了一个分类期以后会变成怎样，但是如果他得出假定他这个聚类得出的分数不变的话，那应该越靠后他那些分数。
越接近，那它这个阈值是不是应该有一些调整？如果它一直是最开始那个阈值，会不会导致后面就再也没办法触发减值条件？也有可能。是，然后这个是，这是，对，这是第三个实验，然后 5 张图代表 5 个模型的内存占用，其中绿色这条是 PRISM 加量化的线，跟 PRISM 特别的接近，所以它就没有画。然后从这张图上，从这个。这个图的结果其实已经非常明确地显示在了右下角的这个表中，不管是从延迟的层面还是整体内存的占用层面，可以看到 freedom 都是一个非常优秀的态的状态。
Pre freedom 这边这个 pick 代表的是它的峰值内存，然后 a 是平均内存占用，然后当 freedom 设成一以后，剩下的三者都是明显比它高的，然后 HF 最高，然后这个最低，然后量化的夹在中间，这也非常的符合直觉。然后接下来是三个在真实数据场景下的这个它的几测试结果，其实这三个场景比较这三个得出的结论比较相似，我主要说一下它这个在 agent memory 上面的实验，它这里这个 agent memory 场景是一个 vision language model，这个模型主要做的是类似，比如说用户给出了一个 UI 界面，然后让模型去通过推理生成一个操作步骤，或者用户给了一张图让你去判断按钮在。在哪里？然后这个模型是指对。
就是类似于就 GY 的。
对，就是对这个模型负责这一部分，然后这个模型本身它的推理会非常的耗时，就是它会有一部分跟你看这张图上有一部分跟环境交互用获得用户这个意图的时延迟，然后一大部分是推理时间，然后还有rank，然后在这个场景下， agent memory 的目的就是他想要去记录一些以前已经成功的 action 的轨迹，然。然后 RE rank 在这个过程中的作用就是帮助 agent memory 去筛选到跟当前的这个目标语义相似度最高的那一条轨迹，然后用这条轨迹去绕过这个非常耗时的 VLM MODULE 的执行。
然后看左上面这张图，它用的 video 和 community 两个workload。我们看 community 这个 workload 可以看到第一个柱状，它在我们禁用 agent memory 的情况下，它整体的延迟是非常高的。同时它的瓶颈在于这个大模型 VLM 模型的推理，然后当它使用 agent memory 之后，这个推理的时间立刻就降下来了，然后现在新的瓶颈是 re rank，然后当它使用 brain 之后， re rank 的这个延迟也有非常明显的降低。然后右边是在整个这个系统之中的内存占用，可以看到蓝色的这一块和绿色的这一块分别是使用 HF 这个 RE rank 和 PRISM 这个 RE rank 可以看到 PRISM 在整体的。延迟和这个内存上面都有非常明显的优势，从数据上讲就是它把延迟降低了 25.2% 到43.4%，分别在两个 workload 上，然后峰值内存占用降低了 6% 3.0。
他选这个很巧妙。这个东西是他们分组做的，可以看看那个冯二波老师，他们在做，就是在做那端侧的agent，就是做这东西是一个agent，就是VR。
这是 VR 文档模型，是吗哦？
而且它是很巧妙，为什么它会在这么多？因为它只要命中了之后，它就不需要再做大模型。是的。所以一般的做 agent number 会再做一次对比。
为什么一般？还有为什么一般还要再做推理？
就一般来说做 agent number 它是要找完之后再去加上你的下这个，你的自己的这个query，然后一块送到模型上，再推理一次得到答案。但它这个是那个 action 的agent，它就说我命中之后不要了推理。
而且它其实是只捕捉了成功命中之后。后的那一次。
然后 rag 和下面那个基本上最终的结论都是类似的，都是延迟的和峰值内存占用都降低了很多。 rag 这边它就是它在离线的时候收集用户数据，然后。建了一个用户的数据集，然后部署了一个 rag 整体的流水线，包括 query 进来之后稀疏检索、稠密检索，然后两个 topk 输入rewrank，然后最后输出TOP10，然后再给大模大语言模型去进行生成。它是跑了一遍这个流程，然后把 Rewrank 置于其中，然后观察它对于整体延迟和内存的一些占用，就是跟这个其实过程和结论都差不多，然后这个 LCS 差不多也是这个意思。
然后最后有一个。update， update study 这个这张图里边的基线是 HF rank，也就是蓝色的这条线可以看到它是所有情况下整体延迟最高的。然后左边展示的是它目前的一个内存占用情况，然后我当加了 preani 这一步之后是黄色的这个线，可以看到它的峰值内存占用瞬间就比这个 HF 还多了49%。然后但是它。他也成功地从横轴的这个角度将整体的延迟降低了49%。刚才说错了，是比刚才升提高了44.8%。然后他马上引入了 chunk 的 execution 这个东西来降他的峰值内存占用，然后就是深蓝色的这个，他成功地把这个内存占用降到了比这个 HF 多 7.2% 的状态，然后这个时候，这个时候才相当于所有的参数还是在内存中的，然后接下来他使用了这个overlap，这个部分将绝大部分的。模型权重参数全放到了 SSD 上，仅留当在当前参与计算的两个layer，这个进一步将内存的占用降低到了 HF 的57.8%。但是这个步骤也导致了一些延迟，这个是因为pruning，现在 pruning 的步骤导致后续的那些层它的计算量可能不太够了，所以计算就没有办法完全地掩盖 IO 了，所以导致了一些计算等 IO 的延迟。最后是他加了那个关于词表的 LRU cache。进一步地将内存的占用降低到了271%，这降低到了百 271.5 兆比。最后总体的一个结果就是在内存上有 78.4% 的降低，然后在延迟上有 48.5% 的优化。
主要还是参数降。做参数是比较。
是。
是正常的减少，因为它本身消耗是比较少。
是的，然后最后是我对这篇文章的一些思考，一个就是我刚才说的，也许它的。那个公式可以引入一些它公式的设计，是不是可以引入一些聚类相关的性质以及阈值？如果在后期，在这个过的层数的后期不做调整，因为你看它的分数已经很接近了，那么它是不是在靠后的层可能就没有办法触发剪枝了，所以我就想它到了后期，如果这个聚类，如果这个候选人候选之间已经非常的接近了，然后聚类的现象也不太明显，那么它是否可以引入一些二阶段的处理方式呢？比如说我可以考虑一下一个。
点的历史排名，或者一个点的一个候选的，一个候选，一个候选的历史排名，或者一个候选的排名变动的情况，然后根据这些情况去影响它的减值，否则的话我不知道它这个减值到了后面是不是还会有效，因为它并没有给出明确的实验，都是在哪些节点触发了哪些资减值。然后还有一个想法是关于平台的，因为他在实验中其实是使用了英伟达平台的，虽然那个 GPU 的显存比较小，但是他也可以在每一个黑。的 layer 中都进行一个 GPU 的加速，但是如果要是它每过一层都要进行一次 prompt 的判断的话，那就意味着它必须要先输出那个中间层的分数，然后分数要输，要放回到 CPU 去做 prompt 条件的判断，然后将结果再传给GPU，这个其实变相的是会打断 GPU 的并行的，并且还会有一些数据传输的开销，但是在它这个场景下，我觉得可能它本身是一个端侧的设备，所以它那个 GPU 就算打满性能可能也不会给系统带来太大。太高的性能提升，但是如果我能够成功地进行剪枝的话，它对计算量的是消区别降低是非常显著的，所以它这个优化看起来还是效果比较明显。但是就比如说它在每一层之的执行之中，把一整块的执行，然后切身切分成了若干chunks，然后这些 chunks 之间它是串行地去执行的，这个其实也不太利于发挥 GPU 高度的并行性，如果是在比较大规模、比较性能比较强的 GPU 的。
这个环境下也许这个优化需要更多的斟酌，就是他的这个方式，我觉得针对上面的这个问题，他也许也可以不用每一层结束都要计算当前的score，就是频繁地去打断 CPU 的并行，就是感觉没有必要每层下来都算。然后还有一点，就是这个文章中提到的方案有一点定得非常死，就是他必须要在。
在一个 batch 之中处理全部的 query 和候选，否则的话，他得不到全局排名就没有办法进行剪枝。那么这个问题就是如果他的候选比较多，他的 k 比较高的话，那这个东西本身内存的占用就比较高，那么针对这个问题，我觉得也许他可以试一试，分就是还是分几个 bag 去进行，就是我还是分几个 batch 去执行，然后我利用目前的局部排名进行一些剪枝，但。
这简直就不要像他这个一样做得这么彻底，就是我找一个 boundary cluster，然后它以上的全要，它以下的全删掉，也许我可以只是把这个 boundary 之下的全部删掉，然后这个 boundary 这一块和它以上的，然后往后再有一些更加细致的排名，也许这个方案可以帮助它去兼容 k 更加高的情况。
但这比较多，其实说白了确实比较少。
对对对，在这个场景下，确实我对这个问题感觉暂时没有暂时的现实的参考，如果他的 k 最高就是 20 ~ 60 这个范围的话，感觉也没有必要，因为他本身的几个 cluster 分支和收敛的都很好。
对，OK，然后我说这个 k 是那个cluster。
不是，就是。
收入就是你的income，你给他多少算个后面有。这个不是取用户嘛？万一用户就是想召回前 100 个，就是就。
可是他已经有了稀疏，前面的稀疏检索和重密检索，他给你的。
风险怎么去筛啊？
对，给到他的可能就已经没那么多了。
你说用户就要 100 个户。
但是我总觉得这个东西我们迁移一下应该是可以用作优化另一个model，但是可能方法不一样，肯定和这个方法不一样，但是，诶？但是就是你，主要你放第一点就是你看他做了。一个他测时间，对，他测时间了，他说你这个 latency 前面这个 retrieve 的时间很短。我总感觉他没有算 embedding 的时间，或者 embedding 的是个非常简单的embedding，但是他 run record 的是record， run record 是个非常复杂的。就是端到端。就是千问，当年发他这个文章绝对是受了千问3，当时发 react model 起的就是去年的时候千问三发 react model，然后就是零点。
6B 的 3B 的差异都在那发的，然后他们在发人家的模的时候，同时发了一个 bad model 也是0.30.6， 0.6 也是 0.6 B3B 还不是 0.6 B4B 发了好像也是这样，就是然后出来那个 bad 好像是多少倍？ 22048 好像还是更高的，还是 4096 倍，就是他是假如说 rank 是个很复杂的model，但是你前面那个 bad model 也说很复杂。
拆上它之后，也许 rank 都不是瓶颈了。
不是，它肯定也是瓶。理解，但是说我就想说如果咱们如果能再研究的话，我因为我们做了检索比较多。做 embedding model 优化其实是有搞头的，因为 embedding model 在越来越复杂，以前比如说 CLIP 那个，之前我说 CLIP 做的CLIP，其实大家都觉得 CLIP 比较大，其实 CLIP 还是没有那么大。就千问三那套东西，它也是有embedding，你可以发现那个是它和文本一起发布。
一个embedding，一个文本。那个 embedding 它其实也可以做点优化。就是去加速，对吧？他如果放到一个大家都很，大家肯定都是想要一个高维的向量，就是因为高维他可以把这个东西刻画得更准确。但是你刻画准确的代价就是你这个埋点长、时间长。当然， recycle 的时间也会变长，因为计算量变大。现在那些你埋点模型，它都是那个向量表示，都是那种俄罗斯套娃的形式前缀表示的就是它支持生成一个完整的时间酒类向量，然后它也会建议就是。比如说如果你对这个场景要求不是很严格的话，就是你取前 256 位、 512 位，这样就可以。是，然后我甚至在想说，如果我们做一个优化的话，我们是不是可以？因为我们之前都是，你看咱们现在都相当于说数据集给定，什么叫给定？就是已经有了完整的 minding 之后的。就你只拿到这个向量，但是如果我们有原始数据的话，其实你可以用不同模型形成不同的败类。然后什么大项目，什么精确的埋点和粗的埋点结合，然后甚至还可以做embedding，这个就这这个 embedding 过程就是首先是，嗯，数据集本身你可以生成几种不同维度的embedding，对吧？它可能对应不同的这样一个精度。
然后你在这查，然后其实在查询的时候你也要必须要过一点 embedding model，那个时间你要算上，对吧？其实这个和现在咱们做的东西不太一样，我感觉这个东西应该是可以去看的这个方面。对，对，他，而且这个千万千，他有个三千万的这个 rank model，那个千万的 rank model 是一个。
师兄说的这个 embedding 是指优化 embedding 这一步，还是我要在优化 embedding 同时也考虑到后续的 retrieval 的一些情况？
其实都可以，你都可以，两个人都OK。就是单纯的 embedding 本身其实也是可以做的，然后那他其实就单纯做一个rank。对吧？然后。然后那个啥，这个，但是这个主要是买卖猫探索，可能和这个探索思路会非常不一样，对吧？因为这个它可以解，但是买你怎么解？你说用中间这一层去当白领，是不是也有点不太靠谱啊？你这个不太靠谱，然后但是就是可以拿一些模型优化的来去做。
就比如说它里边有一些 offloading 的，然后计算的 overlap 的。
对对对，但是你一定要找一些白领，它特殊的一些优化点。因为所谓的他这些 operator 或者说暗层加载就是大部分通用的用。
对，我也觉得他的，其实他，他，其实我感觉他整一套里边最有创新性的还是那个prompt，因为剩下的那些都非常容易想到。
对，那些通用的，然后所以说你那里也可以做一个，就是你要想一个它的特殊优化，嗯，比较好，但是当然了通用的也可以做。
那就得像这个减脂一样，它得在某些实验中有一些embedding，独属于 embedding 的现象。
特色，一个自己的特色。然后再就是你说我们如果拿到这个大量的原始数据、原始文档，然后我们有好几种不同的埋点model，对吧？我们能不能做一些东西来去优化？就是我，就是我先不考虑说它占空间多大。我假如把所有的这个文档我都过两种埋点或者三种埋点。对吧？那他们可能也是出的时间或者埋的时间。是，但是他们对结果也是要求可能也是不太一致的。
假设我是变一个，变成一个漏斗模型，比如跟漏斗一样的，我先用最差的，比如说假设我这个现在我跟这个量的 128 位、 256 位和 120 四。我先过一遍 1128 位的这个索引，这里面可能要查，可能我是对一个 1B 的这么大的数据量去查 top 100。我先查TOP100，行，可以，我粗算一遍，然后我这个每次计算可能就是 100128 维。然后拿到 TOP100 之后，我能不能。他们也再做一次精排，精排的时候我就厉害，我，我就用二维就精排了。对吧？然后我再玩一遍，对吧？这样子。你这也可以留给验证，那你先做 1128 维的那个embedding，然后 128 维定做检索的时候，你这个时间再做个 128 维。对对对，我在想如果咱们有真实数据，有埋包的话，其实可以做这种事啊。
所以现在缺的是真实数据吗？
不缺，有写。一般下面也有。其实有，但是可以试试把它们全都。但是话，其实这个词有点费算力，但是还好，你如果是量小的话，其实还好。量大的就。
那这一部分要是实验的话，那用 GPU 去算。
对，肯定就会算，因为你大量的东西做的埋点，它其实量很大。把它全部下了。对，我在这，当然这个是不考虑过任何的这个所谓的 IO 成本的。但是刚刚说的那个漏斗，觉得漏斗模型其实有问题，漏斗模型你首先你还要出两个埋点参数，就是你。一块来之后你要做两次呗。
那这个东西如果算端到端时间的话可能会有问题。
不一定，因为你可能你在那个索引查找的时候计算量很小。所以就看能不能换，值不值得换。比如说我做两次这个就是我，我是两次白领，然后加上一个大的规模的，但是维度小的一个索引查找大家加上一个小规模的、维度高的一个索引平台。这是出来一个时间和进度，然后另。另外一个就是我做一次大的埋点，再加再做一次大规模的。
对，这个应该是没有。
是。是有一些可以看、可以做的点，但是因为可能之前我们做商业化可能过于看重于这个向量化后的东西了。
我感觉可以做一做这方面，包括现在我感觉工业界关注的也是这方面更多一些，而不仅仅是单纯 an 本身的执行。
对对，就可以看，然后包括 demo 本身。就是你可以看一个这个流程上的东西，其实会更好玩一些。
好，OK，我也没有那个。
线上同学有什么问题吗？
线上同学有问题吗？
诶，没有，没有，没有。那就行，没有的话那我就这样，好吧？那结束。没有，没结束。行，那就结束吧。好。

---
draft: true
---

就是传统的就是普通的discan，就是最早的那个discan，就是它在，就我们以这个图为例，就是从 12 点，就从 12 开始搜索，它将压缩后的向量就是放置于内存，一般压缩的这个向量使用这个。
等等等等，志远，你可能过完之后又不行了。算了，你还那样吧，你过完之后好像是共享这个小图，对。就这样。
那就这样，行。就是discaln，它是将那个压缩后的向量，就是一般都用 PQ 或者用那现在，嗯，更多的用 Rabbit q，把这些向量放置于内存，然后这个压缩率一般都是 1/ 16 或 1/ 32。就是一般这个压缩后的向量只占原始全精度向量的 5% 到 10% 左右，然后其他的将原始向量和每个向量的邻居就是顺序地放在 SSD 中，那就是搜索时候，就是一般在那个图搜索引中有两个队列，一个是 candidate set，它决定你下一步用哪个向量搜索这个部分，使用这个量化后的距离去计算，因为这一部分并不需要精确，只要这个路径大概的符合即可。然后对于结果队列则使用全精度的距离，就是它，因为它将那个原始向量和向量的邻居放至于SSD，所以它每一个后可能会读取。
当前那个离查询最近的那个向量，然后去 IO 磁盘，从磁盘中拿到这个向量对应的原始向量和它的邻居，然后这个原始向量就顺便去计算它的群路距离，并把它放到那个结果队列中，然后利用刚才的邻居，对于刚才的邻居计算这些邻居对应的 PQ 距离，然后将这些距离全部加入那个候选集中，就是 diss 和 Ann 的给的思路，然后就是那 diss 和 Ann 就是一个显然的问题，就是它每一个 Hop 都需要 io 一次。
假如说我们 EFX 就是一般在一个普通的比如说 SIP 或者 DIFF 数据集中。他想达到一个 recall 大于 recall 100，大于 0.9 或者大于0.95，他都需要一个 EMS 等于 100 或者 200 的一个左右的参数，那意味着他就需要 IO 磁盘，大概 100 多次，就随机 IO 磁盘 100 多次，所以就是为了对他优化，就是因为就是我们在 SSD 读取中一个 block 都是 4KB 的，这一般都是 4KB 或者 8KB 的，就假如即使你只需要 1KB 的内容，他读取也是需要读取 4KB 的，就是你读取 1KB 和读取 4KB 的 density 是一样的。
那比如说对于 sift 的这种数据集，一个原始向量只有 128 维，你可以在一个 block 中放多个向量，但是在原来的 diskann 中就是访问 1 io 一个向量它。它会顺带 IO 其他很多，但是其他很多都没有用，就是直接 IO 上来就被扔了，只拿那一个。所以我们通可以通过这个 shuffle 让它这个访问具有空间局部性。
就是比如说我们可以让这个就是右边这个图，这个 12 和8，我就假如说我们它放在一个 block 中，那一次 IO 相当于可以解决两个hope，然后对此就是 starling 它给的一个重排算法，那个原论文中那个重排算法，它给的那个公式挺花里胡哨的，但是那个公式就一句话就可以说明白，就是给定一个图，这个图记就是那个图索引。
将这些点分配到多个 block 中，一个 block 大小一般是4KB，就是最大化这个每条边两个端点都在同一个block，那边的数量就是它的重排算法就可以，就是只是最大化这个边的数量即可，然后这个该重排算法都是论文中也证明了它可以被归约到 NP 完全问题。
后来他提出了一些进贪心算法去近似求解，那这个算法就这里就不再详细讲解了，他们都是那个理论，计算机那边的常见思路都是照搬来的。这也就是给出了一个那个示例图，就是对于 list 和 Ann 来说，它的每个 block 就是按照那个编号顺序排，就是A1、A2、A3、A4。然后对于 Starling 来说，它把相似的向量尽可能的放在一个 block 中，就是这个图上相似的向量它就放在一个 block 中，这样可以利用这个搜索的空间局部性。然后下面讲一个对 Starling 重排算法的优化，它只，它这个论文是 Voldb 2025 的论文，它单纯地只优化了这个重排算法，就是对于 Starling 的重排目标就是最大化两个端点都在同一个 block 那边的数量。但是实际上就是每条边就是不应该同等看待，就是相当于那个 workload 有偏写，比如在图上的这个搜索过程中，就是某些边或者某些点它更容易被访问到，而某些边很少被访问。
这，这个是真的吗？
对对对。
如果要是这样的话，其实边也有局部性的地方。
例如说边有局部性啊。对对对，就是很显然的，就是首先点肯定是有的，既然点有，那个入边肯定也有。
诶，为啥点有？
点，你点就是一个很显然的就是。
你说 EP 的。
不是，就是每个点它的入度都不一样，虽然出度是有上限的，入度都不一样。说这个，是吧？对，比如说入度少的点，它访问的肯定更少。
但就是说我之前做开始的时候并没有观察这个。
在做判决的时候觉得它访。
这种局部性。
包括访问点也多，它非常的随机。
就反正怎么说就这个，当然这个他假设都是 IND 的。
对，我们也是，基本上也是用的 IND 的。
但是他又做了一个实验，就是假如查询和 base 完全重合，这个权重就是不一样的，就是这样子的。就是假如说，假如说有 n 个base，然后你现在来了 n 个查询，这 n 个查询和 base 一一对应。对，然后去做这个探寻搜索，然后这个边和点的这个访问权重就是不一样的，就是把搜这个 n 次搜索全部加起来。
那肯定，因为它是和图的连通的那个。
对，对，它一，它就是这么个意思，就是认为这个某些连通性更好的那边会更容易被访问，所以应该优先去给他们一个更高的权重。
拿 query 集去测的时候好像。
对，拿 query 集测的时候可能没这个现象，是不是然后还是我们 query 集有点偏？
我记得我之前在 SIP 上做也没有看到。
但这个论文的 IO 提升那个并不高，它大概是提高了这个，就是并不是特别多，提高了一点点。
这个毫不明显啊。
对。
对starling，尤其是 sift 和一对 sift 基本上是重。
对，但它反而在这个 OD 提高了，还比还可以。
就那个下面的tiny。
对， tiny 这个。 tiny 也是O。 tiny 是in，但是它好像那个查询难度比较大。
那个数据。
不对，不对，你，你应该看它上面的那个 IO 次数啊。
对，就 IO 次数啊。
这个 IO 次数也很明显， IO 次数就。对。但是它也可能差了一。差的不是。
对，但是重排它也就那样，它能提高一点也不错了。其实就是这个重排，因为它只优化了这一个东西。
对，因为我觉得这个它这个性质有点是，按理说是有，但是真的 query 级会有吗？
真有这个性质，它反送钱的。
就先说一下它的那个算法，它怎么这个权重怎么设置的吧？我就讲一下这个权重怎么设置它，就是它这个 WP 和 p 星的设置，就是它等于这个就对一条边，比如说UV，它就是等于这个 UV 这条边被经过的这个次数就能通过 UV 这条边，这个就一条单调路径，如果这条单调路径经过 UV 到达v，就给它的权重加一。对，就是然后这个 p 点，就是说能通过单调路径到达点 v 的这个点 p 的数量。对，然后他把这个两个权重一乘，对，就是通过就是任意一个起点，这个起点能通过一条单调路径走到p。对，就给它。就是给他这个权重，就是点的数量就给他加了，对。
能通过单调就建图的时候就可以遍历所有。
对对对，就它这有一个引理，就是说对于一条单调路径，这个单调路径一定是通过裁边形成的，就是那个 Rming 的裁边，比如说这里 UV 被裁了，它就会有一条 u 到 u 撇，然后从 u 撇到 v 的路径，对人家说明这个 u 撇能到v， u 也能到v，都通过一条单调路径到v。
所以。
对，对，它就通过这么设置这么一个权重，然后这个权重它是裁边的，在裁边的时候那个顺便计算了，它也没有额外开销。
然后这个的话它会怎么去做？排就重排了。
对，然后重排的时候，就是刚才我们说那个 Starling 的算法，就是最大化这个边的数量，就能这个边满足两个端点都在同一。
号中，是吧？
对，他又给这个边乘了一个权重。对，就是对于，然后这个 i 也就是一个那个指示函数，就如果 BP 等于 BP 星，它就是一，否则就是0，对，它是最大化这个。
优先考虑权重大的。
对，优先考虑权重大的，然后这也是个NP，完全，这显然是一个NP，完全就 Starling 那个扩展，然后它近似解决也很简单，就是它构造了一个无向图，就是点集和原图是一样的。然后边集合就是将那个原图索引的有向边全部变成无向边，然后如果原图那个边 p1p2 和 p2p1 都有，那转换后那个无向图的边就是两个加起来。
对于这样一个无向图，它就对于每一个空block，它首先就选择一个就贪心权重最大的两个端点，然后把那个两个端点那个对应的边给分配到一个 block 中，然后就相当于 u 和 v 在一个block，然后在 u 和 v 的邻居中也不断地去选择权重最大的边加入这个block，直到这个被填满，然后重复上述的过程。对，你不是给一个图嘛？就上来，就是那个。 P6 和 P11 那个权重最大，就把那个 P6 和 P11 加到一个 block 中，然后在 P6 和 P11 的那个邻居中找到边那个权重最大的那些边给它加进去，然后不断迭代，就是。
对，这个论文，然后他后面也提出了一些，比如说那个编的排序，他因为他这里要选最大，所以他对编。边进行排序，然后那个对边排序的开销挺大的，相当于就是一个 64 乘n，然后再乘一个log，然后他就用一些那个划分，先把图划分成小图，每个小图分别去用这个算法去贪心的去划分。对，这个论文就这样。
这个权重图好像我们那个前段时间在把那个点放到内存里面加权重的，就是。
他要是给点加的，他是给边加的。
他两个都加了，然后。
太多太多，加了。对。因为他是，因为他一个一个点，他有好多条边，他就直接求和，就。然后我们之前是把点或者边放到内存里边，然后我们觉得内存的权重应该是高，然后磁盘的权重应该。
但是我们的这个东西它是，它其实对于它就是对于那个 AN 路径本身的性质并没有做太多的探究，我们只是根据这个点的位置，但是它这个相当于它先做的预实验，然后它观察到这条边可能是会被更多的查询路径经过的，然后它照此来设计权重，虽然它对于这个边有热度，这一点实验上我并没有观察到，但是比起我们的方法，它这个确实有一些实验的依据，然后才是。
它有一些理论，主要它有一些理论，那个单条路径结合那个理论。
但是这个也很简单，能想到就是把一个就是没有权重的图变成一个带权重的图。对。然后咱们那个调位置其实也是把它带改成一个带权图。
对，我记得它里面有一个实验。就这个 number of vertices that it canmonotonically reach。对，就是能就到达这条边的点，对，能单调通过单调路径到达这条边的点，然后这个是能通过单调路径到达这个点的边。
然后所以它其实本质上也是把它变成一个带权图。
对，就是带权的。
但是它并没有干涉 an 本身搜索的那个路径，只是相比于 scaling 来说，虽然也。是他单独追求的是这个两端都在一个，一个 block 中的这种边数量最多，但是他可能为了他这个目的，他会牺牲掉一些关键的访问很频繁的边，所以最后导致反而不如这种方法的效果好。对。我记得他是有一个实验做过这个的，但是本身他并没有干涉 an 的搜索路径，他只是尽量把更重要的边放到了。
对，他没改那个搜索。
但是我们是改了，所以我们造成了非常严重的 requery 降低。
对，改那个路径可能就确实他不是最优的那个。
对，他，因为他不再是单调的了。明白，好的。
那这个东西就是IND，我没做过实验，如果 OOD 输一级，这个那个热度是很明显的偏。
那肯定。
对，然后他这个东西也没讨论OOD，他这个东西肯定是只能对 IND 有效，就如果是 OOD 可能就是需要一个预训练嘛。
但是对 OOD 来说，感觉这种方案也可以，也可行啊。
对，应该是可行的，对。
对吧？就是原来OOD，就是说比如把 OOD 放到 DIFF 层上，那它肯定那个性能更差了。如果这种普通场景，对，然后你把它给那个绕远路的那些东西给放一块，然后 l l 次数那个不降更狠了。
那个实验结果就去他看过，就。
是他，结果反正就是。
对，他提升得很有限，但是就考虑到他只做了一个小优化，那就这个还能接受。
可以有，如果有同学有兴趣的话，可以把咱们那个nginx，不是把那 search 那数据集放他那个上面试一下。对，把 OD 数据集放他那个方法上试一下。
然后就这个重返就到这我要讲下一个，这是一个比较偏综述的东西，就是首先它有一些很有意思的发现，就是第一个发现就是 diss 和 an 的静态缓存，就 diss an 静态缓存，它只缓存从查询点开始，然后走一跳两跳嘛。然后 Starling 是一个导航图，就这两个，无论是这个静态缓存还是这个导航图，它随着这个 cache 容量增加，它的这它的那个 QPS 几乎没有提高，它只是从这个 0% 到1%，还有一个提高，后面就几乎不变。
诶，它 DN 是缓存，它这个缓存啥缓存点？
从 entry point 开始，那个前几个 Hop 那个边就是那个最原版的那个。
对，就只缓存边，是吧？
不那个点。
点点点，边点边都要，对吧？对对对。
因为它 DP 附近应该是聚比较聚集的，但是它再往后搜就非常分散。
对，对，就是因为差评就分两阶段，就它只能加快从入口点到查询附近那一个一小点。对，那后面就是它那个解决不了的starling，同理这个，它这个 navigation index 它也仅只能就是说把那个入口点放到那个查询附近，然后后面还是在 iOS 的这个几乎就没用了，就 1% 和 10% 并没有明显的区别。
它这个 cache 是，就是单纯做了一个类似于 block cache 就直不接把块。
对，都就对，就。拿上来。就是把整个那个 block 拿上来。
OK，就是 block cache，类似 page cache。
也不是那个box，就那个点的位置相当和0。你就把那个点所有信息就拿上来。对，不是block，不是block，就是那个单纯那个点。
行，其实做 block 也一样。
对，差不多。然后再第二个就是一个，就是我们之前也讨论过的问题，就是对于一个高维的向量来说，那个 Starling 的那个重排没有用，就比如说这个是 line 的向量，它 512 维，那它一个那个就是一个 block 只能放一个。对。就那个重排就没用了。对对对。所以它那个 Starling 和 diss 应用在这里，就是那个 QPS 完全一模一样了。然后那下一个，那个发现就是这个量化向量这个压缩率，它随着这个压缩率的这个8，就是 1/ 848，就 1/ 48，它随这个压缩力的那个增加，它有一个极值点，那就反正就是也很好理解，就是后面的东西它就是相当于这个距离计算已经大于 IOU 了，因为它那个压缩的，我觉得它那个前面压缩低的话，就是它那个距离计算大于IOU，就比如说只压缩到 1/ 4 或者 1/ 8，就是它那个计算比较大，它实际上大于IOU。然后到后面就是因为它压缩率太高了，那个精度下降了，还有 IOU 更多次。因为存在一个极值点，所以这个论文它最后一个方法就是说做一个那个预训练，就是先拿一些sample、一些 query 去称一个最好的压缩率。
对，然后对于 OOD 的数据集来说也是一个比较显然的，就是它的那个量化距离压缩率会要更高一些，要更低一些，就是 1/ 8 才就是极值点， IND 的话可能就这个 1/ 241/ 16 左右，就是 OOD 它那个量化会偏嘛？然后下一个就是一个很关键的内容，就是这个，这个就是就假如说我们在搜索时候能不 io 任何的原始向量。
我们在统一的，在这个就是候选队列和结果队列，我们都用这个量化距离维护，然后只在这个搜索最后统一进行一次rewalk。就类似于这个 ivivfpq 的那个操作。但是这个时候就是发现这个 rewalk 60% 的候选队列和 rewalk 100% 的候选队列的那个 recall 基本上几乎是一样的。
rewalk TOP 60。
对，就是假如说那个搜索队列大小是 1, 000。OK。最后， Rewalk 1, 000 和 Rewalk 500500 是差不多的，就表明那 d 次 an 相当于就是每一步它都要 REWALK 一个，因为它每一步要 io 一个原始向量，它每一步都会 rewalk 一个向量，那就说明这个 d 次 an 其实它这个搜索过程中存在大量的这个对原始向量的 IO 浪费。这个东西在高维向量当中更明显了，你相当于你很多人，比如说一个 960 维的向量，它有很多都 io 都是浪费掉了，就这个 react 根本没有必要。
就是没必要一直维护很精确的result。
对，所以这个实际上是第四维那个布局的问题，它原始向量和边放在一起，既导致那个不能shuffle，也导致这个大量 io 浪费。
你每次要读边，你都要把这个向量给读进来。对。就是，那他意思就是说我只读边不读向量了。
对。然后这里他测一个，这个就是只缓存边的cache。对，就是跟，就是按刚才那个思路，就是搜索时候不使用任何的原始向量，就是它是这样子的，就是如果这个边在缓存里它就不 rewank 了，它如果这个边不在缓存里，它 IO 的时候就它。
还是顺便 Rewank 一下？
对，顺便 Rewank 一下，然后它在最后再统一地把那些刚才没有 Rewank 的向量再检查一下它是不是在那个 candidate top 上。对，然后再统一地进行一次Rewank，对，然后这个 cache 的它又随着它可以随着那个内存容量增大而显著增加，这个原因也应该也很容易理解，就是因为它只缓存边，这个边是很小的，相对原始向量来说是很小的。所以他 10% 这个边几乎可能大部分的边都被缓存到了。
对。然后他这里就讨论了三种这个布局，就首先就是他使用的是 a 类 a 的布局，就是说首先这个是原始向量，然后这个是邻居，然后他后面缓存的这个邻居的邻居，就是比如说这个也不是缓存，就是放在那个 distance layer 的，这样他一次 IO 他不仅读到了这个向量的邻居，还读到了这个他的邻居，这样就是他如果在搜索时候，如果这个邻居的邻居刚好在 candidate 中。那下次搜索他就直接拿了，他不用再 IO 了。
但这样的话，但这样的会有这个邻居边的一个，就是重复的一个。
对对对，他会有一些词和 SID 容易他那个最后结果这个 SSD 可能要比 dervn 要大一倍左右，这也是个吹豆腐嘛。对。对，就是对，然后就是他，如果，就是如果那个邻居他刚好就已经被 IO 过了，他就不再算精确距离了，他就直接搜了，就如果搜索结束后，那些刚才没有被算精确距离的那些向量他还在看，在他的前面就是至少我们在同一的 IO 磁盘性Rewark。然后这个时候 IO 磁盘那个带宽也很容易打满，相当于所有东西都固定了一下，就能把那个带宽全部并行到 IO 流。
这块我们之前还做过一些优化，就是还能再打高一些。
对，然后他另一个题说的就是这个，就是点边。边分离的这个思路，这也之前那个德嵘一直在用的那个思路，但是他这个论文里说这个不行嘛，因意思就是在你在搜索的时候好歹还能进行一部分Rewank，如果假如说你用点边分离必须再统一，最后会造成 IO 浪费。但实际上如果假如 IO 浪费，就是你开始那么多，你这个 graph 排了很多边，这些边 IO 上来不一定有用，排得太多了不一定有用。是。对。然后你最后要把所有的 canidate 全要再统一的 io 一遍。对，对，就是会造成 IO 次数增加嘛。但他这里就这个论文，他没有用重排，所以我觉得这个 b 不一定比 a 差。
对。对，但是重排的话一个 4K 里边也有很多老。
对，就是但是重排这个相当于局部性会更加的明显，这个边的缓存还会更好。它可能会一次IO，它可能是能解决四五个 Hop 的边。
对。
所以。上面那个算是带了重排，就保存了邻居的。
对，邻居的邻居他。
但是上面那个你看 3072B block 200B 凑了一个4K，是吧？
凑一个4K，当然它就是如果现在有多高，它就用8K16K。对。就是，但是如果原来的 diss 可以用算法，比如说他一个向量 512 维就一个，后面可能这个一个向量占了 3/ 4，他剩余 1/ 4 又当废掉了。对。对，这个就是这次他讨论这两个布局，然后就是他整个流程，就是首先第一点就是他依旧使用了这个导航图，但他只用了0.5%，记得斯大林他应该用的挺多的东西，挺多的边，反正 5% 左右，他只用0.5%，意思就是说就是上面那个图，就是他发现这里有机构，就用那么一点点，然后这个 Vector cache 也是一个思路，就是也是。只用一点点，然后另一个就是那个 GRAPH cache，就是它存的那个边的cache，它 cache 只 cache 边，它选择距离在导航图有一些点，然后在这些点附近的点，它认为这些点就更容易被访问。
认证。
对。导航图中附近的点。
就是一个小导航图，然后导航图到下层图，然后下层图那个最开始那些点。然后就另外就是缓存容量非常高，如果能存下所有的 GRAPH cache 的话，它又继续存那个Vector，但这个一般不太，但其实如果是高维向量是可能的，比如说 3, 000 维的向量这个。 10% 足以存下所有的 grab CACHE。
对，反正其实也不是很大。
然后他这里就类似于派文恩，他也考了一个异步IO，就是一样的，这几乎一样，就是假如变位置为 b 的话，他每次发送 baseio 请求他有两个队列，然后一个队列是准备 IO 进行 IO 的，还有队列是完成的，他就每相当于每次都轮询一下这个队列，如果某个请求完成，他就直接开始搜，就是一个循环的一个过程。
跟那比那个派文，恩，感觉差不多，就一个意思。那这个论文他没有比派文。恩，我不知道为啥他。就是segment，它是 segment 26 的第四轮中的。那你比派文发那个发已经过一年了，它没比它，但反正它跟斯大林比的话，它那个 recall 就是它提高还挺明显的。
应该这个派文可能差不太多。
那也不太好说，派文，恩。
不是派文，恩，只要是它那个没达到 SRD 的那个带宽上限，就性能就一直是。
对。感觉看看，我觉得看SSD。
对，就看它列，它的性能到底有多高，就硬件性能。
对。对，难道太有这个东西？我感觉就是他这个思路和派文恩那东西完全是正交的，其实。
是，其实我觉得泰文那个东西就是他没改任何的重盘，然后他其实就是一年，一年性能多高，他又能打多高，反正。只有到一年受限之后，才会有什么重盘的优势出现。
泰文，他用的索引是 diss 位。就 diss 位。
就纯 diss 位。我也讲一下这个，就是目前一些新的东西，就是原来的这些索引都是以点为单位进行搜索的，那这些都他这些东西相当于他是以配置为单位进行搜索的，就是他在建图的时候考虑配置之间的关联性，就是第一个就是那个 Bam 技术， ICDE 26 那个刚中的那个，他的主要 idea 就是说也很简单，他会业务说优先保证 block 内有边，比如这个图他优先保证这个 block 内有边，因为 block 内有边他是不需要进行 IO 的。然后他利用 block 内的边加这个 mring 的性质去裁去 block 间的边，也就是这么这么一个思路，然后就尽可能利用同一个 block 内的向量，然后进而降低 IO 次数。就是考虑要在 MMRING 中，就是裁边时仅考虑单个点和它的邻居，然后在 BMRING 中就考虑每条边，要裁的时候就考虑其端点和端点所在 block 内的所有点，就它只保证这个 IO 的单调性，它不再保证点的单调性。对，就是一个很简单的改进。就是它只要保证这个每一次 IO 之后，它存在单条路径，就 IO 好几个点，然后这几个点组合起来有个单条路径。
对，它就是这么一个优化，然后这里东西它是一样的，就是它也采用了点面分离的存储，就它也同样考，就是考虑到这个高维的向量就是存不下，没有沙跑，没用，就是它使用了点面分离的存储，比如最后，但是它不是最后统一进行rank，就是它每次搜索完一个整 block 后，然后再根据 candidate 读取原始向量进行重排。对，然后它这个边就是批量读取，所以这个也是一样的，就是 SSD 带宽很容易的打满。那它的算法也很简单，就是说它从看的 candidate 中拿一个，第一个没有被，就是没有被那个扩展过的向量，然后把这个 box 加载上来，然后先用刚才建的图在那个 block 内先搜，搜一，搜完之后再。
根据这个 c 的结果，把那个，就是把刚才的那个搜到的结果的 top 多少个给它重排一下，给它有这么一个思路，对，然后它的边就是分开存的，它同时存了一个 in 和out，这里的 in 和 out 就表示块内的边数量和块间的边数量，它搜索块内的时候只用块内的边，然后搜完之后再访问，在块之间进行搜索这个论文，那这个论文的这个实验就比较奇怪，这个派文这个性能很低。
那它很可能是用了一个差的盘。
对，这个 SAE 不好。
对。
那就不如这个Starling，嗯。
那个差的盘是这样，因为差的盘反而会更稳定，比，甚至比DCN。
对对对，那个配解原因的时间我记得就是比 DCN 还要长。
对，如果你有盘非常差的话。
那你可能就队列多了，他反而更那，他在那，一直在那抢。还有竞争。对，那说一下这个配解原因，那个其实跟这个 BMG 的思路很像，就是它首先也是进一个重排，就上面就重排相同颜色的代表，它是相同的配置，它同样是以配置为单位进行搜索，就是它每次搜索会把这个配置整个 load 上来，然后把配置内的所有向量全部算一遍，原始区间距离。但是它还有一个那个基于那个局部敏感哈希的一个导航索引，但我感觉它是纯凑创新点的。纯凑数用的，应该没什么区别。
就是选 EP 用的。
对，选 EP 用的。我应该，我它的好处可能就是开销比导航图少一点，它不用维护边，因为那个局部敏感，哈希的内存占用比较少，然后其他的都是一样的，它这些 SSD 的布局就是Vector，这个每个 page 先把 Vector 都存了，然后这个存整个 page 的出边，然后这个 page 的出边就是等会说。还有一个，这个就是他这个论文还考虑一件事，就是说假如说这个内存放不下所有的压缩向量。压缩。对，他的 SSD 上，后面紧接着存这压缩向量。
极致化的。
对对对对。
是不是 SSD 上放压缩项？
是。
对，就是极致化内存。但是那个论文的代码的默认设置里面，就我看他代码默认设置里面就是还是把所有的量化放在内存里，放 SSD 里，他这个也会造成冗余嘛。会。对，他也会造成冗余，就 SSD 这样冗余，然后就说一下他那个重排算法，就是他这个重排算法很朴素，就是对于每个配置。
就首先要随便选一个没被分配的点，然后以该点为原点，在那个图上直接做一次BFS，就也不是贪心搜索就单纯的BFS，然后将刚才 BFS 的结果就是选择距离 u 最近，然后且没分配的点，然后把这些点填充到这个配置中，直到它填满。对，它就比 starling 要朴素很多，就单纯的一个很简单的贪心，对，就随机选点，然后做BFS。这个也很快。然后就对于每个配置的邻居，就是它对于每个配置，就是就相当于把配置中每个点的邻居给它合起来，对，就是合合起来，就是，所以这样就导致。它每个配置的邻居数量就是高达这个几百 200 到 500 左右，它每配置大概有这么多邻居。
就是每个配置会连接 200 ~ 500 个配置。
对不点？
点点，对，每个配置连接 200 ~ 500 个点，它每次搜索就是算，就这样，就它每次搜索对于这些邻居都算这个量化距离，然后根据这个量化距离找一个最近的，然后下次 IO 就 IO 那个最近的那个对应的block，对，就是对于所有的邻居都是去算 estimated distance，对，然后对于所有的配置中的精确量就是算exact，对，然后这些那个候选队列相当于每次每个 hope 候选队列增加几百个嘛。
对，就搜索几乎与这几天一模一样，只是将原来是指 IO 一个点，现在 IO 一个配置给他的思路，就这个思路。但是我做的实验就是我发现他这个一个，他那个 IO 计算占80%， IO 只占 20% 左右，但跟他那个实验也很粗糙有关，因为我他那个实现的不好，就他那个代码实现有很多常数的问题，但估计最后就是那个计算和 IO 各占一半。对，他大概，而且是在 DIFF 数据集，就是在那个 96 位的数据集，那个计算和 IO 就只占一半。所以他那个计算是很多的。主要就在那个，他每次 hop 要计算好几百个。那个 PQ 的那个。
诶，但也不一定，如果你换高维的话，每个配置里面的这个平均数可能变少了。
那也确实，那。
因为每个配置里面装的位数少了。
他没考虑这个事，就我不知道，假如说他对高维把那个配置改成 8KB 或者 16 KB，这个怎么样？他也没讨论过这。
一般来说是 4K 比较正常，嗯，他默认4。高的就少。
后就这个实验，就他那个派维恩就性能很差，在他那个上面就比 deta 恩还要差，就。
他应该是选了一个那种把，比如说把这个一盘有点差。然后线数打贼高。线数很高，然后就 io 非常大、非常多，然后竞争特别激烈，就把这些盘打爆了。
对。
后的下一个论文就是不再，就不，这个不是已配节这个还是单纯地去这个优化的，是那个磁盘 SSD 上那个计算的问题，就是首先他发现了一个东西，就是。
鲁棒模型是不是？
对，维度非常高，对。
这不就是那个鲁棒模型吗？就是体积波里面常用的那个屋顶模型。
对。
对，屋顶模型。
对，就是他发现，比如说对于 96 维的向量那个。就是此时还是 IO 占跟主要，但是对于到一到那个 960 的时候，这个东西就计算不过来了。
诶，这个是给文章发哪了？
sigmod。
他竟然用了个体结构一样的方，我以为他发 isa 了。你说。
对，就是然后他这里有一个东西，就是这个s，这个 s 表示start，就意思就是他这个 96 维向量 start 更高的意思，就是start， block 纱布，他每个后边会计算那个block，那个前几个他都会算向量会更大，也就是说他认为就是他发现，但是我觉得这个也跟 SSD 有关，他就 SSD 比较差的， SSD 比较好的话就计算就占主要了。
对，这很正常。就这个顶线，这个如范模的它其实和硬件有关。对。他原来就他本来这个模型，原来提出是为了选硬件。
也就这边给了几个图，就是这几个都是没有流水线的， pipeline 带流水线，但是它计算占得比较多，OK。然后就说这个，它采用它的核心idea，就是这个冗余存储，就是因为 diss 和 Ann 计算 PQ 的时候是随机访存，就它没法充分利用 SMA 的加速，就那个发字scan，因为发字 scan 必须要求那个 PQ 是连续存储，对，所以它就是把那个邻居给冗余存储，这个就内部 ID 6268，然后这些 2 就后面存一个PQ，这里存 1 PQ，这里存 1 PQ，同样是通过放大 SSD 的存储来去减少计算。
对，这样，因为这样他 IO 上来之后那个内存是连续的，就是他可以用发的词看这里，就是那个发的词看那个重排，就是把它按照那个利于那个 SMD 那个交叉的那个组织下来，然后又可以用那个发的词看组，然后他说就是这个为了减少这个，因为这个 SID 占用很大，所以他怎么做？就是先用一个 PCA 对原始向量做一个降维，然后有一个主成分，主成分和一个剩余的，然后对这个主成分向量进行PQ。
这个东西的原因就是高维向量对 PCA 降维后，各维度的方差它会成一个长尾分布，然后这个是别的东西的一个图，只有这个。你对于原来的一个原始向量，它的每一个维度的这个值都是随机的方差几乎是一样的。但是你 PCA 降维后，它 PCA 降维显然是找那个特征向量，它那个特征向量显然是方差最大的那一条线，降维后这些方差又成一个长尾分布，就所以说就是前半方差大，所以它需要更需要进行量化，量化维度就是需要一些更多比特的量化，后面维度方差低就随便搞搞就行了，就不会带来多大的误差。
诶，那 PC 会损， PC 本身会损失精度吗？
就是它把 PCA 的主成分和原剩余的都存了。
对，明白。
它只是单纯量化，精度肯定是更低的，但是它相对于这个比特数来说可能更有优势一些。
OK，然后就是采用不同的量化方案了，对。
对，然后这里就是额外提一下，就是有一篇 sigmoid R6 对 Rabidq 优化的论文叫Saq。这个 SAQ 它就是利用这个方差的这个长尾分布，意思就是前一半维度它用rabidq2bit，后面一半维度它用 rabidq 一bit。
诶，这个挺有意思。
对，对，然后这个是第二轮就中的 sigmoid R6，第二轮中的，然后。他搞了一个，比如他搞了一个背包DP，他用那个背包 DP 去决定，一共说给定一个限制的bit，他用一个背包 DP 去决定那个划分。对，就是说把那个方差性质给应用起来。
那这个也，其实计算的时候是不是也有用？比如说我就做了我对主成分分析，那我是不是主成分那几个维度我算完之后我可以减支了，比如说算一个。
对对对对。
对吧？就是我算一个距离的时候，我可以只算前几个维度或者剪掉就完事了。
我觉得这跟 ad 三步的那个 AD3 步的是一样的， ad 三步的也是投影是一样的，就我觉得那个思路应该是。
用了什么 PC 效果？差不多。你们一参或两行，后面。这还有另一个工作。
对对对对，确实他后面有个标 DB 的工作用的就是PCA，就原来 AD3 目是随机投影，后面改成了PCA。
因为这个我们前段时间做一个 p 上的工作，本来是想做剪枝，然后最后没剪成。当那假如因为我没做 PCA 降维，我们就直接用那个 row 的 Vector 去打算做剪枝。
那个剪枝很，然后几乎没用。
然后又几乎没用。
对， AD3 不零也讨那个论文里讨论了这个事情。对，然后下一个就是他的那个 PK 量化，就是他把那个量化放到了 SSD 上，意思就是他那个没有任何的内存消耗，所以他就是之后就是他可以。这个他那个 CACHE 容量可以更大，然后他这里选择的 CACHE 方案也比较朴素，就他只他做了一个实验，就是入度高的节点更容易被访问，所以他就直接把那个节点按照那个入度排个序，从高的开始缓存一个静态缓存。然后他同样也是有一个重叠，就是对他那头，他跟那个拍文件差不多，就是一个同样的那个重叠 IOU 计算几乎一个意思，然后他这里他还有一些别的算法，有什么EP、section，那什么什么东西，那反正优化都有限，就这里就不细说了。就他主要的优化就是一个这个 SMD 的layout，加一个这个 integrate cache。就是这俩，然后他的这个实验，对，他的实验跟那个几个都比了，他都挺高，他这里这个拍文就还可以。那有他这里拍文，他有的也是比这个 starling 差的，所以我就反正跟还跟这个盘有关系。
他基本上比的高维的多。
对，都高维的，都他时间里只有高维的。
也还行吧。
对，都是 960500 多往上的。
低维估计就够呛了。
然后他这个论文里还有一个很关键，就是他的 on DISK 的版本是比 HNSW 和韦曼娜的内存版本快的。其实这个算法就相当于上次说的那个 Symphony QG 的 DISK 版。对 Symphony QG 它放大了很多倍的内存，所以它就是只相当于一个 Symphony QG 的 DISK 版。但我感觉就是如果把这个韦曼娜和 HSW Lib 给用上一个 rapid Q 8 bit 可能就差不多了，跟它差不多了。对，所以就这样，因为它毕竟这个东西算得还是很快，我觉得它那个 PQ 它即使 IO 一次磁盘，但是它那个发磁看带来的收益应该也能把那个 IO 磁盘抵掉，相较于纯内存版本，它们应该能抵掉。
我们就，我们是不是能做个缝合怪啊？做缝合怪就是还做沙盒，还做。
对，感觉现在就是这个缝合，怪没有。
对啊，感觉就是做缝合怪。嗯，沙盒也沙盒，然后计算也加快，就啥。
对，因为现在这些 layout 什么的，感觉大家都是在那个排列组合。
对，哈哈，对。
对，各种优化排列组合，现在。
感觉现在是时候做个缝合怪了。然后我其实上之前还想过一个想法，就是我觉得现在的那个做重排，是不是我们考虑的这个东西太眼界太窄了？就是我们现在都是对于一个query。然后去找，比如说 Starling 吧。确实是。就类似于说我这个 Vector 它的邻居，然后。
因为我感觉这个应该还是就是没有那个实际 query 导致的。
我不是我，我那天和周然我突，我想一个事，就是你说实际里面，实际的这个系统里面肯定不是一个 query 查，肯定是一个 batch 查的。就是你能不能利用一个 batch 的这个特性去做上？就是你一个 batch 你下去的话，你原来你比如说你现在这种现在方案就算是一个batch，比如说 100 个query，那你也是一个一个处理的。然后或者分 100 个线程去处理的。对。但是如果说你能把这一，但是一，这 100 个 query 里面它有没有一些重合的IO？它肯定也有。
对。
然后你就能不能利用这 100 个query？不是一个 batch 的query，然后去做重开。它可能会好，就是我尽量的有计划，目标不是说是这一个 block 满足这一个 query 它的这个路径上的命中，而是这一个 block 我读一个block，我满足这一个batch，它的就是 query 的这个命中。
但其实我觉得这个跟现在这个重排算法应该不冲突，因为现在重排还是基于访问频率的，这个访问频率高的肯定多个 query 都更容易访问到，其实我感觉。
应该还是基于 base 向量做的重排，没有说针对query。
但那个肯定是OD，就相当于 OD 了，其实。
对，但是会有，比如说你要是你因为咱还这个，其实还是因为对阿玲的场景，因为阿玲的场景他那个 block 太大了。对，他那个 block 不是什么4K8K，他那是特别大。
对对，他们那个不就网络IO。
就是他那个是网络 IO 特大的话，你就算是只对base，你充分利用了所有的 base 的这个信息。
他那个肯定要考虑并发查询的问题。
对，就你那个填不满的话，那你就不能只用这个一个 query 或者是 base 的一个信息，查信信息去重排，你可以用一个batch，就一个p，你都可以把它当做一个信息，把这个重排，然后你这时候第一是重排那个向量，第二是你那个batch，你怎么组batch？它也可以做调。调度，就比如说我现在候选里边有 1, 000 个query。
电脑里那个富域应该不太能调度，它那个单独应该还挺高要求。对，它那应该不太能做调度吧？
不太能做调度。
对，就是如果是搜索引擎那种平台，搜索引擎应该不能调度。
搜引也可以调度，就是比如说有就是线来服务的，你可以跳一跳，就是你可以不是线来服务的，你可以大概混一混，就是，但是不能，不能那啥，不能那个就是就跳太多，隔，隔太多嘛？
那个相当于用单独换QPS。
对，就是来换QPS，但是向量湖本身来现在就应该要求不高。
对，因为就 1 秒左右。
它那个东西本来就很难。对。你调度一下，对吧？你完全还好一点。就是如果说能调度的话，第一就是就可以用 batch 去做一个 batch 的信息，去做重排。做做做，shuffle，就是这个可比现在的要信息要多得多，现在 4K8K 就是只，就是调度空间太小。
对，但它那要求不是一兆一兆，其实这个 shuffle 肯定不能跟现在的这个目标一样。
对，肯定不能，现在目标一样。是的，然后我觉得就是要么就做个缝合外，就是把这个所有都揉一块，要么就是看看一个 batch 呢？就是一个批次的这种query，其实应该没有人去考虑这个事。
你内存半个都没有，现在。
对，但是实际上肯定是一个批次做的。他不可能是一个 query 做的，就是这种请求，这么密集的场景怎么可能一个 query 一块地进行呢？肯定就是外边的接口肯定包了一批，但是可能内部因为没实现批批处理接口，所以就是一个一个一个一个get，一个一个query，这样肯定是有批处理的空间。然后还有就是这东西他怎么做写，我想知道他们做了大量的这个 shuffle 之后，他们，对，就插入。
做的 shuffle 肯定没法插入了这个。那就只能用传统的，先等个20%，然后再统一干。
是啊，那怎么做什么怎么插入呢？这个玩意就麻烦了。
这感觉这个已经不多做，插入就不现实了，感觉就他们那些 layout 布局这个，这就那个把邻居放在后面的那个东西已经就已经不好干了，这个东西就已经不好查了。
对，就是高优，是可能还是最好，最容易做实时插入的。
对。
对，然后你除了插入之外，还有更麻烦的事怎么删呢？
对，这个，那个删， all 那个都没干，没搞那个，删吧？
你，所以我，诶，对，我虽然三个专家发了篇文章，他们搞了删，就是他们是搞update，就是先删后插。
对，他们是删掉那个点，然后他先去找这个点所有出度的点，然后他会入度用到这个点的点，反正就是他会找所有被这次插入影响到的点，然后因为插入了他损失了一些边，损失了一些图连通性，然后他又设计了一个方法，然后重新把这个图连通性用一些比较少的点边再弥补起来，大概是这么做的一个。
这个是通用的思路吗？
是，但是他其实没有，就是跟沙盒那边没有太多的关系。
他没做沙盒，其实删除这个通用思路，只是搬到 disk 上感觉没什么。应该没什么特别的。对。
他是原本是那种特别大批量的这个东西。
原来是因为那个删除太烂了，那 fresh DCN 那太烂了，做得太差了。
他的意思其实就是原本的插入，他想要去找所有受到这个插入影响的点，他可能需要扫描整个数据集，然后就是随机 IO 非常多，然后他现在通过就是一个内存的，是我们也是一个内存导航图，还是一个什么方式，他能够快速地定位到会被影响的那些节点的那个区域，然后他在那个区域进行一些少量的。
那他为啥不直接把入边也给存到 SSD 上呢？就是他把如果他把入边直接从 SSD 上，不就马上全找着了？对。
对的，对，他可能考虑到空间占用。
SSD 空间占用感觉无所谓了其实。
所以其实有点，这个，有点trick。但是做删除确实是有点，这个确实可能是个需求吧。反正我感觉是现在做DSN，感觉现在大家做 DSN 已经做到了，这个，已经把读已经做到这个很难的东西了，你做得太，做得实在是太花里胡哨了。
对，我感觉基本上能考虑的都考虑到。
除非你做个超级缝合怪，对吧？你终极缝合怪把它都缝起来，然后，对，嗯，然后要么就是考虑新场景，就是一个，就是你盘下边怎么变？就是就阿里那边东西。
我感觉考虑下不同 SSD 硬件就做一个那个就相当于对。
对一个de，对个 deptive 的什么也自适应的啊。对。主要考虑它的这个不同点，然后就得到一个它的计算和 IO 的这个 bound 的那个界限，然后考虑一个。
做一些特别的专门的优化，就是没，就不同的 SSD 选择不同的算法。
是 Adaptive 的，这个是一个，然后要么就是做阿里那边的，就是那种新的盘。然后大的，然后不同的 l 的特性，然后做重盘，然后要么就做大的，有我觉得要么就我，因为我依然一直想法就是做 batch l。这个肯定是有空间。就是用 latency 去换一些QPS，然后要么就是做动态图。
就现在，单纯他们并发查询，他们也不会考虑两个 IOIO 重的吧。对。不考虑这个。
不考虑这个事，但是一旦考虑这个事，应该是有优化空间的。对，嗯，然后就是图可能不好做，但是 IBF 一定好做。对，然后还要么就是动态图做更新，做删除，做插入，做更新，删除、做插入，这个再加上结合一下那个数据的布局，应该有不少东西要做，就是。因为这东西它都是针对读做的，然后一旦做写可能就全完了。即使是做 buffer 写可能也很难受，因为有些东西的可能构建的成本很高。这个。
他们那东西，他们那些图构建成本都是还是在那个韦巴纳本身，对，他们那些重排什么都挺快的。重排可能就占个 1% 都不到，大概本身还是那个韦巴纳。
那就说只能做 buffer 写。对，就可能 10% 重重排一个。那做 buffer 可能还不如直接做update，那个像Audio，不是Audio，他们做一个实验不是说就觉得 buffer 还不如直接。
那直接写那个重排，搞不定。
对，所以就得想个方法吧。
你也可以，就是只或只能说就新来的东西就只能先放一边，先不重排，过一会重排一下。
是的是的，那应该是可以弄，反正感觉最应该出东西的还应该是要么就是志远刚才说的就是快速地出东西来，可能就是做一个带 table 的自适应的，这根据硬件来的，根据计算和 IO 比来的，然后要么就是做点新场景的，包括什么新的硬件的？新的。 query 的 block 的新的读写大概就这些。

---
draft: true
---

大家好，今天我分享的论文就是这篇这个 Backflow temper，这是一篇 sigmod 26 的文章，主要就是针对于现在在这个大模型还有生成式这个 AI 的这种时代，除了我们一直在关注研究的这个 an search，就是其实发现就是捕捉一些更细粒度，就是 by TOKEN 的那种多向量的这种检索，其实也引起了比较广泛的关注，所以读了一些这方面的文章，这种方式在带来一些精度飞跃的同时也会引起比。就是比较大的计算的开销，包括一些复杂的候选过滤，还有比较高昂的内存成本。
然后现有的一些系统，他们就会尝试通过 GPU 索引，像我们之前了解到的，比如说一些 ivfpq GPU 来缓解一些性能问题，但是这些都是会以牺牲，因为它做了 PQ 就会牺牲检索精度作为代价，并且 GPU 的利用率也会比较低。然后这篇文章就是提出了这样一个 Web low temper 的这么一个系统，它是用了一个比较好的这个硬件设备，就在一个现代的一个超级芯片，就是一个 super chip，英伟达的一个芯片上面实现了一个比较低延迟，然后高召回。还有一个就是这种端到端的一个检索优化。然后他说他的方法在后面实验的过程当中，在一些比较传统的一些设备当中跑的效果也都比较好。然后我们具体来介绍一下，首先就是用一句话来概括现在的这个局面的话，相当于就是多向量检索，他就是彻底打破了我们传统的 an search 这种架构的舒适区，就是虽然我们在追求一些比较极致的这种召回精度的这种道路上取得了一些进展，但是随之而来的就是底层的系统，它在计算能效，包括索引的结构，还有一些存储的模型上面就会面临着灾难。所以说。
接下来我们先介绍一下它的background，还有一些 motivation 相关的。首先就是传统的 an search，它就是遵循了一个单向量的这种重新方式，相当于就是我们会将我们的整个的 query 或者是 document 给它映射到一个单一的这种向量空间当中，这种做法就是检索的效率肯定是比较高的，但是它就会损失一些细粒度的这种 TOKEN 级的这种语义信息，所以说像近年来为了追求一些比较及时这种检索的质量，就出现了很多比如说 codebert 之类这种多向量检索的方法。
就是它就是会把相当于就是把文本给它打散到 TOKEN 级别，然后就会用一堆向量来表示一个document，像这种的话就是这种相当就是 set to set 这种匹配就会让检索的质量飞跃，就比如说在一些多跳的 QA 任务当中就会可以带来，就论文当中背景说就带来 20% 的提升，但是就是天下没有免费的午餐，就这种精度的提升就会是一个这种极度膨胀的这种系统上的开销和代价。
然后可以看一下这幅图，就是一个大概介绍了一下 Bert 的一个编码的过程，就比如它会把这样一个一句话，然后它就被这个模型给它拆分成，比如说什么 the cat，就是之类的这种多个比较独立的这种扣分向量。这些维度上就是确实丰富了，但是就是留给我们底层系统去处理这些数据量还有对比次数，就会指数级的就是爆炸，然后更是比如说数据量如果更上来的话，那些端到端的延迟就会从我们能接受的，比如说一些亚毫秒级别的就会剧增到十几或者是几十毫秒，这种就难以满足现在工业场景下一些比较严格的这种SLO，然后就先了解一下它的前序的一个知识，就相当于我刚我们刚刚提到的一个，就是它主要复杂。就是复杂在这样一个天分的这种打分的机制。然后其实这种东西叫倒角打分，就相当于就是左侧这个公式，就是表达也比较清楚，就是我们在查询当中对于每一个词源的向量，比如说这个QJ，我们系统必须要遍历目标文档当中所有的词源，比如说从 TI 到 l 寻找一下内积的最大值，然后在全局去做sum。
右侧是我画了一个这样的一个图，就可以看得比较清楚，相当于它是单向链的，这种点到点的匹配不一样，它是比较稠密的，就是这样一个 set to set 的这样的一个计算。如果说举个例子，比如说查询当中我包含 10 个词源。文档当中有 150 个 TOKEN 的话，那么我单个的文档打分就要进行 1, 500 次的向量的内积计算嘛。
如果在精排阶段的话，我们在筛，比如说筛 1, 000 个候选文档的时候，单次一次查询就会造成 1, 500 150 万次的这样一个向量的计算，所以说这个逻辑还会伴随着一些非常不规则的一些访存的跳跃，在常规的一些硬件上面就会有一些性能上的瓶颈，所以说相当于就是在落地的时候就会造成有这三方面的主要的问题吧。
首先就是我们可以看这个图，就是对比了一下当前就是 GPU 检索系统当中的一个叫applied，相当于就是。 Sota 以这样一个 Sota 的算法。就 fly 的这个。算法的论文我也去看了一下，他，其实他的目标跟我们的论文一样，就是一个比较想要加速，我们刚刚提及到的就是比较耗时的，刚刚那个倒角打分，他的工程上我们可以理解为就是一个IVF，然后加上一个 PQ 做了一个多级流水线，就是类似，就相当于他有一个传统的那个倒排索引，然后会通过 kmeans 把海量我们刚刚说那些 TOKEN 给他聚类成一个一个粗粒度的簇，然后来进行初步的这种候选的这个检索，然后还他就相当于他非常依赖于有损压缩。
就比如说VQ、 PQ 这种逻辑，因为多项上的特征肯定会把，就是把显存直接撑爆了。对，这个 plied 它就必须要对原向量进行必比较，不可复杂的这种压缩，就是比较严格，然后它会把这个向量压缩成一些比较小的 VQ 或者是 PQ 码，然后在查询的时候它会把这个 VQ 就是做一个初筛，甚至比如说它可能会做一个阈值，就把一批文档直接筛掉，然后再把剩下的文档的 PQ 码再给它解压出来。
就像这个里面的这个 decompression 这个阶段，就是这个红色的这一块，相当于，就是在，相当于在那个 BPO 上算那个transfer。所以大家可以明显地看到，就是他为了把海量的这个多向量塞到 GPU 里面，就会强行进行 DQ 成绩量化压缩，就会导致像这个红色的延续了大概有两毫秒的这个解压的，光这个解压的这个耗时就有。
诶，你说话了吗？立骁师兄，OK，就是相当于这个解压缩的这个时长就比较长，就已经达到 2 毫秒了。然后整体跑这一遍我们也可以看出就已经是 13.54 毫秒了，所以这个长度对于现代的这种检索的这个系统就太沉重了。然后时间主要都在哪呢？就大概就我下面总结这三点。
没有，你说啊。
首先就是这个计算强问题，就相当于我们刚刚提到的只要相当于 ok 乘 n 的这样一个穷举嘛。会导致这个 BPO 的算力就枯竭了，然后就是传统相当于对 Max sum 这种像聚合机制就是没有，就针对这种的一个比较，就是融合优化这种内核，然后 BPO 的带宽也会闲置，然后接下来就这个索引墙，就是这个蓝色的区域这个位置，因为传统的 IVF 它的结构没有对细粒度，就是 TOKEN 级别这种选择性，因为它候选集那个冗余度就非常高，就会增加后面阶段的一些压力吧。然后接下来就是一些显存墙，还有就是延长5，就很明显刚刚那个红，我们为了适配这个显存，就现在的系统就被迫一定要用 PQ 压缩，就会就是引入了刚刚我们说的这个就是 depression 的overhead。就会，而且还有就是观察一下它的召回率。
你可心那个我有问题，就是它这种多向量召回的话，就是我建索引的时候也不是说对，比如说原来咱们做 an 的时候可能是对一个 trunk 或者对一个 doc 统一建一个索引，不是就做建一条向量，对吧？现在是，比如说对一个 doc 只要拆成一堆TOKEN，然后每个 TOKEN 做个向量，是吗？那它最后你就是，那它最后算的时候也是说我这个 query 里边的每个 TOKEN 和这个，就 query 里边每个 TOKEN 都做一次an，是这意思吧？
对。对对对对，OK。
对，可以这么理解，相当于我要对于每一个去做，就是BN，就是相当于近似打分了，就不是那种刚说千分那种精密的了。是的，是这样理解的，嗯。
对，就是说每个做一个an，然后再比如说就相当于是多路召回，然后再 merge 再合并。OK，那它这个就是这个parameter，它是，我看它是decrement。 02 说的是啥。你问哪个？就是那个 02 是什么意思？索引墙那个是什么？叫粗粒度聚类，在 TOKEN 级别选择性极差。可心，你能听到琪琪说话吗？对。对，就那个痛点，第一句话就是能，就是没太看懂。
对对对，是的。
能听到，但是就是说这个索引墙这块的问题吗？就是因为我们就是传统CVSPQ，它不是，它是针对单向量做的，我们就是一条一条的那种向量做的，就他们他并没有做那种比较细粒度的这种 TOKEN 级别的这种选择，所以他那个就看他的那个数据集，那个候选集里面就这个冗余度非常高，然后后续的阶段就会比较有压力。高了。
我想。那就是一个文档是拆成多个TOKEN，然后那在 IVF 就是考虑的时候是对于假如说有很多个文档，那就把那些文档那么多向量它要怎么聚类？就，就是那个是。这个案例就是那个。是一个 doc 里面所有向量作为一个整体，它们一定在一个簇中，还是说那些一个 doc 里面的那些 TOKEN 的向量也分散了？
就是传统的 IVF 的方法吗？就是说白了就是。
就是比如说他一个文档可能有几十、几百个这种TOKEN，他聚类的就不是文档了，就是刚刚拆出来那些 TOKEN 的，他就相当于还是用那个 kmeans 选知心，然后之类的就是，对对对，是的。
就是 TOKEN 的向量，只，就是把 TOKEN 那个向量单作为一个单独的，然后直接放到 FF 里边去，对吧？那数据文档还要做个去重之类。那为啥？那他做去文，那为啥说索引墙是粗粒度聚类头？这已经很细了，就是因为 TOKEN 级别，当然是很细，这应该是一个很细的量，为什么叫粗粒度聚类？
就是我理解的就是因为我就是传统的IVF，它就是要对所有的文档的所有的 TOKEN 都做 key means，但是就是正常的 IVF 的结构设计是为了单个向量去做的，我输入的这 IVF 单位应该是正常，应该是单个 TOKEN 的Vector，我是这么理解的，比如说你一个文档d，它是T1T2，一直到 T100 就会把这 100 个向量都给它拆开，然后每一条独立地去进这个IVF，但是 IVF 它并不知道它们来自的是同一个文档，就是我当时的理解。
对。
那就是说这个双是多向量召回，它不仅要考虑TOKEN，也要考虑文档喽？就这意思啊。那它是怎么做召回呢？我在想那它最后的给的反馈的结果应该是文档还是 TOKEN 呢？返回的是文档。就是假如。
对对对对，我读论文是这样理解的。
那我肯定是要返回文档的，就是像我们现在大模型的话，我整个流程就是我查TOKEN，然后要找相似的TOKEN，拿这些 TOKEN 去找对应的文档 ID 统计，比如哪个文档出现最多召回这个文档，这么一个流程我理解。对，返回的还是 TOPK 的文档。对，但是我这个召回就不再是向量的那种，就是要考虑文档内部的 TOKEN 了。
对对对对，我的意思是就是说他最后结果还是要返回文档呗，对吧？就最终结果。OK。我明白，其实就像之前做关键词查询的时候那种打分的机制一样，就是什么那个那些什么根据词频的这个东西，就是还是那套东西。嗯，其实我还是不太理解索引项，没事，你接着讲吧。
是是是，嗯。
好，对，这个实际上论文里面总结的，他说这个就会存在这三方问题。我当时的理解就是因为我们传统的这个LAF，当时觉得他是为了单向量设计的，但是他又有文档跟这个 TOKEN 向量之间的这个关系，就会就是我看他候选集就比较多的冗余，然后接下来就是这个显存墙这块，然后就是我，我发现他这个文章他建立的一个基础，就是他用了一个这个非常非常牛的芯片，就这个英伟达的这个芯片。
OK。
就这个算是借了这个的优势，我觉得它可能达到刚刚我们看到的这个0.97，就是达到这样 14 倍的这个优势，就是它这个芯片就相当于就是我们传统的做一些架构肯定要受限于那个，比如说Pcie，它这些链路带宽可能有几十个GB，大概它这边的这个它是一个相当于 in the link c to c 的这么一个优势，它把那个 Grace 的相当于，把这个 Grace CPU 和这个 copper 的 GPU 相当于直接耦合在一起，它的相当于有 900 一个 GB 每秒。大概是这样的一个双向的带宽，然后所以说就是它这里面就转换了一个思路，大概就是。他相当于利用了他 GPUCPU 里面有 480 个 g 的，相当于大容量，然后统一把这个内存放在一个就是全精度的这个向量，就是全部都存放这种全精度，不做那种这些压缩啥的了。
然后他把这个通过这个GPU，然后按需去直接读，然后就把这个什么有损压缩的这些依赖就给取消了，然后这里面大概就是又借助了一个硬件上的优势，然后接下来就是他的一些系统，然后就是他的系统大概分为三个部分，就是这个优势，他后面文章当中说的就是他的方法在做了一些同比较的实验，就是放弃了这个优势，仍然很厉害，但是他的。
这个就是测试的结果，我看大部分都是在这个硬件上面做的，然后就是这个相当于就是这个图，就是他做的整体的一个系统的这样一个一个架构图，就是他大概分为了三个步骤，就是有三级的这种像漏斗状的这种结构，跟我们的这个正常搜推那种链路就有点像，就是他这个首先他有一个 Max IVF CGRA，这个 CGRA 正常，他一开始就是英伟达的一个库里面的，就是相当于针对于 GPU 的一个专属的非常快的一个图索引的算法，就是专门用来做 an 的。然后他做了一个 Max IVF CGRA，然后第二步的话就是。他在 GPU 内部用了一个轻量的Anchor，然后就是代理向量做了一轮的粗排，然后砍掉了，就是非常大量的那种长尾的文档，我理解，然后最后一步的话就是比较精华的，就是他用的就是相当于借助了一个 Grace store，然后就是直接从 CPU 上面拉取一些幸存的文档，然后就是全精度的向量不做压缩，然后直接送到那个千分炮里面，就是最终实现了这样的一个精排。
然后具体来说的话，就是先说这个 Max IVF Cagra，就是我读这篇文章，我们先考虑就是他为什么不直接搬现有的一些索引？首先第一个就是因为我们既然说，比如说图索引，像CAGRI，还有一些我们常用的一些HNSW，就是为什么不能每个跟每个 TOKEN 相当于建一个图，这样行不行？然后就是答案就是不行，因为在这个多向量的条件下，它这个 TOKEN 数量相当于是文档数量的上百倍，然后它就大概级别就是到亿级左右，就是建图的话就非常麻烦，然后更重要的就是它没有，就是文档级别那种空间感，它搜起来会比较慢，然后会召回非常大量，因为它召回的结果是文档就会有大量的重复的文档。
然后接下来就是用传统的我们所说的这个 IVF 做一些细粒度划分，是这样也不行，就是我比如说我们聚类的中心那个 k 变得大的时候，就它每个 TOKEN 就会找最近中心点，这个计算就会。就是计算量就相当于大概就二次方那种爆炸，然后他在实验，就是作者在这个论文当中也做了个实验，就是他在 GPU 上他用标准的 kmeans 给了好像是 2, 000 万个，我记得是TOKEN，他聚类要花什么 4 个小时左右，他说然后接下来就是相当于，就是这对于这种情况作者就设计了一个，就是比较新的一个混合的这种架构，他相当于就是想要具备这个图索引的路由能力，还能实现这个倒排划分，其实是一个拼接，我理解。所以他做了一个这样的一个 Max IVF CA Bra。
就是说因为既然我们纯图索引它显存爆炸了，然后纯 IVF 它又它这计算就会陷进去，然后它是怎么做的呢？就是对于这种架构，可以比如说看左边的话，就是比如说我们以前的系统，像那个 PLY 的刚刚提到的，它用的就是左边这种比较传统的这种IVF，就像我们理解的把一篇文章给它拆成 100 多个 TOKEN 之后，然后就是如果还用这么粗粒度的那种聚类中心，就是它找出来的候选集，非常糟糕，就是我们这期我只能用最笨的，相当于就是那种 root boss，那种感觉就是给它暴力地去筛一遍，就效率比较低。然后它就用了一个方法，就是它没有被，没有对所有的 TOKEN 建图，就不是所有的 TOKEN 去建，而是它抠了，就是在这些里面去抠了 1% 到 5% 的 TOKEN 作为一个。 Anchor 就像锚点的这么一个东西，然后它就保证了能够进行。刚刚我就说的不是粗粒度嘛？它就是这样说，它就是能够达到比较细粒度的，能够区分，然后把那层开销。降低了，就是他还给这些 anchor 做了一个我刚刚提到的，我们刚提到的适配于，就是英伟达推出来那个Cagri，就是又给每一个 Anchor 去做了这个图的路由，所以说这个图就在这里面，它就会有两个，相当于有两个功能，就比如说我们在建索引的时候，它 TOKEN 就会去找对应的Anchor，就不用跑，这个相当于就是欧文方的那个 kmeans 了，就直接顺着那个图的边就可以去搜过去，速度就会比较快，然后在查询的时候那个 query 也不用去。
就是去查所有的锚点，就是各种每个 anchor 了，就会利用这个图去搜，然后就是说就可以瞬间就可以命中我们想要的这个候选簇，这种感觉，然后它就是相当于融合了，其实我理解就是它把 IAF 和这个 CAGRI 融合了，然后没取全部的TOKEN，去取了一些锚点。
诶，那之前那个就是他之前比那个功能，就是那个十几毫秒那个功能，他是用的啥？用if。
对，就是那个 fly 的。好像是用的就是IVF，就是传统的这个就是 IVF 加 PQ 压缩那套，还有那个他论文里还对比那个扣分，他 V2 他用的也是，就是用IVF，然后暴力地去扫新，然后 PGO 压缩多阶段过滤，所以才能那么慢的。
对， IF1 大概就是比图嘛？这个也很正常，然后但他其实也他东西其实没什么，这个所谓的ifkarura，这个这maxifkarura，我。这个好像之前做 disk 上面的索引的那一套东西，好像 span 就是一个子图加上一堆聚类，然后每个子图的点代表一个聚类。
对对对，我当时做的时候也有这种感觉。对，我当时读这篇论文的时候，我感觉这篇论文它主要就是提出了，就是因为现在大模型它可能就是包括一些生成推荐这些，它就想用这种 open 级别的这种，我感觉就是它这个背景，就把之前用的那些方法在这个上面改一改，串一串那种感觉。反正他跟那个 SPN b 侧的人都是那种一类的。
对，那之前是那之前说的所谓的 index 的开销，那就是说 if 它那个 index 那个开销比较重，是吧？就这意思，是吧？那它确实也对，因为它确实是因为它if，它就算是那个 n probe 设得比较小，它也要它召回的点也非常非常多的，然后那些点可能都要读，比如说读一下，然后。
对，就是，嗯。
做一些处理，那他应该把那个东西算算的 index 开销，那他就是说这个簇里面有多少点？我懂了，就是簇里有多少点才会使它 index 开销越大，然后他又用一个这种图索引的话，其实就是说那他图索引里面的每个簇还再去算吗？不算了，就比就不算了，是吧？他他这个意思是吧？
对，把这个算，对哦。
对，相当于就不算，对嗯。就是粒度比较粗，然后他觉得这个 TOKEN 比较大，就几亿个，然后他觉得复杂度比较高， BPO 也。救不了了，因为它是平方级别的，大概他说是这个意思。
明白，我还以为他是要什么做什么文档过滤，我觉得好像没错，好像没必要做，是吧？
对，没有，他就是相当于就不用拼路径了，然后用那个他所说的这个 CAGRA 的图搜索做了分配，大概是这个意思，我感觉就不用去暴力地去扫那个置新了，用了这个图做路由，我理解是这个意思。
用cover。但是他对他建 cover 的时候，他提这个anchor。这种锚点的话，它，那它怎么知道这个锚点对应的这个 cluster 呢？就是比如说假如这个图里面A0，它里边有4， A0 这个簇里还有其他四个点，它指不，它怎么知道这四个点是在 A0 是和 A0 是一块的？
对，这个我当时。
就是首先我先说它这个，就是比如说它的anchor，就相当于这个是这个 cluster 的这个代表的向量，就是这 cluster 就是所有被分配到这个 anchor 的，就是这个 TOKEN 这个意思，所以这个CGR。他就是想要快速地去找最近的那个Anchor，比如说他会先随机地选一批Anchor，就是从所有的向量当中随机去抽 1% 到5%，这些就让他作为 Anchor 向量。然后每个 Anchor 就是天然就是一个 cluster 的一个中心，一开始他没有 cluster 就只有Anchor，然后他再对所有的 Anchor 去建这个Cagira，然后把每个让每个就是文档的 TOKEN 进来，他进来的时候他也不暴力地去算，就是所有的那个就是 Anchor 的，就是到所有 Anchor 的距离，他直接用那个 Cagira 搜。
索到最近的那个 Anchor 是啥？然后这个 TOKEN 就属，就是就属于这个 anchor 的 cluster 的这个意思，然后迭代去更新。嗯，对对对，OK，然后就是他，这就是他所谓的第一个创新点了，就是相当于就是用他写了个名字叫 i Max IVF CGRA 这么一个这么一个方法，然后接下来就是他的一些工程上的细节，就是我为。为了方便理解，我读了一下他这个论文跟代码，就是抠了一，就抠了一段伪代码，这样出来就是这个就是他工程上实现的一个亮点，就说白了就是他想要在这个候选生成的时候，我们顺着读，就是找到了很多anchor，其实刚刚也提到了，就是自然的也会带出来非常多的那个 document ID，因为我不同的这个 query TOKEN 就可能指向同一个文档，其实中间就会有非常多的冗余，就是不同的 query TOKEN 就可能指向同一篇 document ID，就是如果在 CPU 上的话。
明白，明。明白了，明白了。找明白了，找 top one，对吧？明白了，行，OK。
我们。你可能随便写一个就是 STD set 之类就去重了，但是它在 GPU 上面的话，它会有，比如成千上万个线程，它同时往一个 set 里面去塞数据，如果我们加锁的话，它整个 GPU 的高并发可能就瞬间变成串行了。然后它是怎么做的呢？它就是把每个任务就是分发到的那种 work 级别的，就看你比如代码的第一步，它就是每个 work 它会认领一个anchor，然后接下来就是让这个 work 里面的 32 个线程分别就是并行地去拉这个 Anchor 下。下面的文档 ID 我比较，他说比较就是比较精髓，就相当于是这步，就第三步就是做了一个就是原子线性探测，就是他开辟了一个全局哈希，然后就这个哈希是不加任何锁的，然后他让这个线程就是用那个 optimake CAS，就是那个，就是这个硬件级别的一个原子指令去抢占这个卡位，如果这个坑位是空的，我就塞进去，然后我如果发现这个坑位里面就是已经是自己想要塞的那个 ID 了，就说明其他线程已经干了。然后就直接 break 去重，就是哈希。一冲突的话就向后加一继续探测，就是它相当于在工程上它做了一个这样的优化，就是除了这俩，其实说白了就是刚刚所说的，就是为了防止这个 ID 映射的冗余，大概做了一个这个东西。
然后文章当中介绍的也比较多的是这部分，然后接下来就是一些就是存储设计方面的，就是相当于就是我，我要怎么就是像我们刚刚做到这个，但是我们要适配到这个就是一个 C2C to c 这样一个一个存储环境下嘛。就会有这个碎片化访存，刚刚讲完了，刚刚第一步，我们这个候选集生成去重嘛？我们现在手里面拿到了一，就是比较少量的精准的候选的 document 的ID，下一步的话我们就需要去这个 CPU 的内存里面，要把它们，它就是全精度存的，把它拉到地球里面去算精确的chamber。
我一开始就当时就是想，因为我既然这个 G 它用的是 GH 200 芯片，它有 900 个 G 每秒的这种 C to C 的物理带宽，那我直。直接调一个，比如说CUDA，什么momomomocopy，就直接把数据拷过来不就行了吗？然后当时他就是里面说就是这个存储设计是有问题的，就比如说他论文里面提出来了，就是用我正常所说的这个CUDA，就是这种 m map 的这种copy，就是会它只有在搬运大块的这种连续内存的时候，才能把我们刚刚所说的 900 带宽跑满。但是我们多向量检索里面一篇文档，它的向量其实非常小，可能大概就是只有37.5D。 b 不是 KB 左右，就是更要命的就是相当于这些文档，它在内存里面是随机散碎分布的，就如果为每一个 37.5 kb 的这样一个小的碎片，我都频繁地去发调用这个东西的话，就是缺一就会引起那个什么缺异常，还有一些internal，就是启动的之类的那种开销，然后结果就是相当于可能刚刚所说的 900 个 GB 随机跑出来的，就可能就是只有几个 GB 那种感觉，就它跑出来，然后。
就是又抛出来一个相当于就是一个系统级的问题，就是要怎么设计一个一套这种存储布局，然后就是想要访问 API 才能说绕过刚刚所说的这个拷贝的这种开销。就是他提到的第二个创新点，就是 Pre store，就是他做了一个这种 API 的内存锁定和 WARP 的掩盖直读，说白了就是我们刚刚分析的那个碎片化保存的那个问题，就是他提出来的这么一种方法，可以先看一下这个图片，这图片。
就相当于我们既然传统的拷贝方法走不通了，他就想要就是直接把 CPU 跟 GPU 中间隔的，就是那层墙直接给推翻，就是大概的意思就是他在软件层你可以看这个位置，诶，就这个位置，就是首先在软件层的时候，这个就是作者他们并没有用比较常规的这种显存，我们经常用的一些显存分配之类的，他就直接在那个 greatcpu 那边直接倒调一下malloc，然后就是他，但是他关键就是在于他呃。
闭合了这个 CUDA 就是什么 advice 和这个就这两个 API 相当于就是以类似一个定身的感觉，就它强制告诉操作系统，就我这几百个 g 的全精度的向量，就不需要钉死在这个 CPU 的这个物理内存里面，我不能因为 GPU 访问了就自动就是把它往显存里面去迁移，这样的话它就用这个去保住了 CPU 的这个内存大容量，然后还获得了比较统一的地址空间，然后中间这个相当于这个。
这个设计它主要就是源于还是源于它这个硬件的这个特性，就是它会依赖于这个共享页表，就是它，它会把这个 Grace 和这个 Hopper 就是这种，就是这，它这个特性有一个共享的一个 share 的 page table，它这个 GPU 的线程就可以直接发起这个访存的指令，然后顺着刚刚所说的这个 900 GB 每秒的这个 c to c 的线路去直接读这个 CPU 里面的数据，然后这意味着啥呢？就相当于我们就直接废弃了原本的那个库档。 pipeline 就直接形成了这种 Zero copy，这种直读就是用了这个硬件的这个特性。
然后接下来就这个大家可能就有点担心，就是我当时读的论文时候就会觉得他要跨设备读内存，他的延迟肯定要比，就是要读本地的这种显存肯定要高，就是后面读发现确实他确实是这样的，我们物理的显存去就是这个延迟一定会存在，这就是，但这个就相当于是 GPU 架构比较擅长的地方。然后。然后这个作者就用了一个这种 WARP 的调度机制，就是我当一个 WARP 我要因为跨，要跨那个 c to c 读数据的时候就会挂起等待，然后 GPU 它就会瞬间去切下一个 WARP 去执行计算，然后就是实现这种高并发，然后当我们所担忧的这种物理的延迟就会就被，因为我们刚刚看那个篇幅，那个计算指令会比较繁重，就直接就被掩盖了大概一个这种想法，这就是 WARP 什么调度来掩盖延迟？就是。
掩盖就是刚刚我所说这个延迟，然后他在做这实验的时候，他在做实验的时候，其实也验证了刚刚说的他说的这点，就是他在那个greystore，他在读取1万篇显就是比较散碎的文档的时候，就他几乎就打平了，就是搬运连续大块内存的这种极限，好像就是他说他比传统的这个拷贝方式快了 50 倍左右，然后相当于就把就到这为止，就把这个索引跟存储的一个障碍，就。就平了，然后接下来就是就看比较核心的了，就到底要怎么在 BPO 的。
OK。没事，可以，你说。
个 API 我看一下，等下它这个 API 内存锁定，我感觉是不是它这个 API 内存锁定是因为它架构本身的一个问题，就是它架构本身可能就是说我如果是 GPU copy 了一个memory，从 CPU 到 GPU 的话，它可能自动就把这个内存页给释放掉了，是吧？就是在这是它架构的问题，应该是感觉因为是，对，因为传统问题，没这个问题，然后。
是的，你应该是跑的，跑这个架构的特性，对。
这个东西就是个大号版的CXL，就是n， n 那个 c ToC 这个它是比 CXL 那个带宽要高很多，然后它其实本质上也是CPU、 GPU 共享内存，然后两边就是都能互相看得见，而且是那种能够保证开始一致性的这个共享内存，但是它带宽比 CXL 大很多，现在 CXL 也可以做成这样，就是架构类似，但是 CXL 那个 CPU 和 GPU 之间的那个链路带宽受限于 Pcie 的上限，可能就是个几十 GB 每秒。就没有很大。
是的，咱说的目的其实就是想要那个 GPU 就能直接读 CPU 的内存，就不用拷贝，不用那种，就是那种显式传输之类。对对对。
对， CXL 也是一样能干这个事，但是就是说可能不能用英伟达的这个GPU，英伟达 GPU 好像因为他们没加入 CXL 那个联盟，然后英特尔那边的 GPU 加，包括一些其他的 GPU 好像都可以。然后这说那个 WARP 做调度的掩盖延迟这玩意，这不就是并行课基本操作嘛？这是并行课上基。基本课上有讲过的基本操作行。
对对对，是的。对对对，我感觉他用的这个像 invent 和 c ToC，他就比，应该是比那个CXR，可能就是带宽延迟上面优一点，其他的感觉也都比较类似。对，然后接下来刚刚前面的就是他前置的一些硬件上面的优势，还有什么索引存储，接下来就开始算，就是他比较核心的那个，就是那个刚刚提到那个千分那个分数了。是他想要，就是把要的这个，就是用那个通过那个 great storm，就把要的这个什么全进度向量用比较低的延迟拉到 CPU 旁边了，然后现在就开始算了，那算又有一些问题，就首先第一个就是可能第一个想法，比如说调库，就像那个，就是那个什么库， Bio s 还是什么来着？我当时看他就是算了一个，这就是一个矩阵乘法，然后调一个那个 reduce 函数，求一个Max，就那种想法。
是，他主要有这个 Grace Hopper 这个东西是啊。
就我们这种常规的想法，就是当你算完这个矩阵乘法的时候，它会有比较庞大的一个中间结果，我必须要写回到 GPU 的那个全量的这个内存，就是 global memory 里面，然后我再读出来去算最大值，就是在这个相当于这个内存比较，就是可怜的这种带宽的面前，像这种反复的 IO 就会有问题。然后接下来就是面对我们相当于长文档会带来一些计算量爆炸，然后如果算力被耗在了，就是内存这种读。
写上面根本不可能把延迟压到刚刚所说那种一毫秒之内，所以说这个作者的优化就比较明确，相当于他写了一个算子，就是他抛弃了，就是现成的一个算子组合，他自己手写的一个有全新的，然后一个软硬件协同的这么一个融合算子，就是跟我们最近那个项目比较有趣，比较有意思，就是这个算子就是center。
那我觉得那可以，那如果说我transfer，我，我不想做精确的向量的重排，我直接。用那个量化的那些点去 rank 可以吗？应该也可以，就像我们之前，比如说就做一个量化的 an 索引一样。
这个可以，就是你说这个方法，其实现有的所有的系统，像Ploid，就是 Cobert VR，什么WARP，我看他们都是这么干的，就是他们工业界感觉都是这么做的。
所以他就是，他又说我，那他的意思就是说我是还是做精，做全精度的，然后我就。提高你的提高召回率，对吧？所以就必须要有个掺扶这种东西。
对，就是他发现可能像我们之前提到的这些方法，他放到单向量检索上面就是召回率什么的保得比较好，但是放到多向量的上面就是他损失，损失精度会损失得更多，他重排的时候那些分数什么的，他召回率好像都在 90% 左右，上不去，我看。对对对。然后他这个论文里面就说自己能够实现 95% 以上，其他的像。那个就是我们刚刚说到的那些用这些方法大概都在召回率都在 90% 左右，我看没有上到 90% 的，然后他出来的标就是用这样一个一个方法，就是能实现全精度，然后回率我觉得他才会升到那么高的，然后就是他所说的这个分子其实就不用特别多说了，就讲完数据怎么搬，就是相当于说白了就是一个这个东西，这个东西就是我在任务分配的时候，他用了一个就是什么。 work 是抠，就是用了一个这么一个，一个一个计算，相当于我每一个 CTA 也就是一个线程块，我只认死理，就处理一对 query 还有document，就这样的设计的话就能够保证 CPU 它在处理，比如成千上万个候选文档的时候，就一能一直能保持最高的占用率和并行度。
OK，行嗯。
OK。
然后还有就是这个，这里面这个底线主要是在两个方面，我觉得第一个就是IO，它用了一个 128B bit 的这种就是向量化加载，就哪怕是比如说。我数据读取以，就是也要爬到这个硬件的这个节拍上，一拍就抓起四个flow，是吧？然后第二个的话就是算力上面，他没有用传统的那个扩大核心，就一点点去算，点击他是直接调了那个 Tensorflow 里面有一个，就是那个 WMMA 那么一个指令，就相当于他就是直接在 128 维这空间里面进行多轮的，就相当于 32 乘 8 × 16 的这样一个矩阵，乘法的一个迭代就会把这个硬件的这。
一个峰值的这种算力给炸出来这种感觉，就是这是他算子内部的设计了，他又写了个算子，然后就是接下来就去做算子融合，就跟我们那个横向那个项目还有点像，就是相当于如果说前面靠的就是硬件上，他这里面就开始靠了一些算法，然后这个就是为了理解那个算法，我大概画了一个类似这种图，就是我们比如说GPU，它共享内存，它带宽比较高，但是它问题就是它可能它容量比较小，我们要想面临，面对现在是。比如是。上百个 TOKEN 这种长文档的话，我把它全部都装到那个 shared memory 里面，还是会溢出，所以就是作者设置了一个就是叫做 shared memory aware peeling 这个东西，就是我既然 query 比较小，我就把它盯到这个 shared memory 里面，既然 document 的比较大，我就把它切，然后切成一小块一小块的tile，像流水线一样，就是有一个这种流式的感觉。
然后比较关键的就是这个preallocation，就是算子层。融合就是我们传统代码就可能说算一遍分，然后写回到显存，然后再读出来，算一遍Max，再写回，然后再读sum，然后就是这中间就是他没有做这些，他是在计算那个 DEMM 的同时，他直接在这个寄存器里面做了一个，就是那个 Butterfly pattern，就是叫什么蝴蝶归约这么一个东西，它配合了那个 WARP shuffle，就是有一个指令，它就是算了一个那个极大值，然后它那个数据它从读出来到算出就是最终的那个我们那个。
那 Tempo score 就中间没有任何一个字节，就会像我们刚刚所说的要落地就写回到显存里面，就做了一个这种设计，就是不用有那种读写的这种感觉，就是让计算延迟，就直接从毫秒级到看它那个消融实验，就这个措施就可以让它从毫秒级到微秒级的感觉，就相当于节省了这个内存，就是 IO 的这种感觉，就是它的最后一个创新点，就是一个一个中间态的一个IO，然后一个8。 Butterfly 的 RE 那个 reduction 大概是这样。
re 这个东西 BYTE fly reaction 这个东西在并行课上也有，对的。对非常教科书的人，对非常教科书式的操作，我感觉。对。对。
是的是的，就我才发现他这个东西做的还挺底层，全是比较基础的这种方法，他全给揉进去了。对对对对对对。我发现他跟他还发了一个 figmod 二6，我觉得他就把很挺有意思的，把经典的一些东西全部都揉进去了，然后接下来就是他做实验，不对，现在不是做实验，就是，然后就是他的一些。
对。
接就是大概就像这样一样，拉起来了一个端到端的一个检索流什么候选生成，然后过滤，然后打分，这就是它整体的这么一个流程了，然后他这个实验做的这个也挺细的，就感觉他就是比如说他第一步先测了一个也在他的这个环境下，就是也是 200 的这种环境下，做了一个端到端的这种性能上的评估。然后大概就是比如说他说的这个，他自己的这个 byteblow chamber，就是这个绿色的这个部分。他在所有的数据集里面全部都优于现有的这个，他跟这些 Plied 之类的就是 Sota 的去比，在这个相当于这个库的这个数据集上面，他大概 plied 达到了 84.3% 的召回率，大概是他那个延迟就有 20 多毫秒，他这边就是低到1，非常非常低，好像是一点 1.8 左右， 1.8 毫秒左右，就是大概是这感觉。对对对，是的，所以他才没有弄上去。是的。然后反正就是我感觉他这个东西就是因为他本身就是依托于这个环境下做的优化，所以在这个上面他毕竟是就是优的，优的还挺多的。
诶，那他那个 TOKEN 来。 level cover 在后边 4 个是不爆显存了。没了。行。
然后是这个感觉就是原因，大概就是刚刚我们所说的这个plaid，就是 plaid 没有办法突破90%，就是现有的这些都有这种瓶颈，我觉得现在这个优化。优化的这个点还挺多的，就现有对这个东西做的目前还没有非常多，然后其实就是它在过滤阶段，相当于用了那种 VQ 就会有误差，然后它最终重排的时候就是PQ，它压缩向量就会精度损失，就是，然后就是这个 Cadra 就是纯 TOKEN 级别的这种CAGRI，就是它虽然能达到比较高的召回率，但因为它就是由，就是它扩展性比较差，就它这个延迟就接近于这个的 3 倍左右，就是相当于。他就觉得在这个环境下他是非常非常好的。
然后接下来就是他就说我不在这个环境下，他现在是拿到这个 A100 上去跑了，就是相当于这里面就用了我们传统的这个 Pcie 的这种接口的这种 GPU 吧？就是大概上面就是他说就是也可以明显地看出来，他就这个里面就只跟那个 prvid 比，也都是比较优的，就是大概。那他没有跟他比，就只跟了他。我还纳闷他俩图放到一块，他只跟这些都没有比，然后就是只跟了这个东西比。你好像是单科100，我看了，我当时还特意看了一下。确实。
他为啥不跟 Kaggle 比呢？我感觉这个 A100 他更应该跟 Kaggle 比啊。
他是说是单块c，单块 A100 吗？还是说是好几块？你大块的话可能，那可能也报显存了，因为最多有 80G 嘛？那它这个红的这个就是这个pilot，这个它这个东西也是一个，就是它为啥能够放进？ 100 的是因为他要做了一个非常狠的量化，是吧？
对，他量化得相当狠了，我看他这个好像都拿来跟这个东西比，就是他 100 代是 40 个 g 嘛？就是 40 个 g 的显存，这对他就是 PQ 量化压得非常非常多，这好像是 32 个子空间乘8B，那就是每个头可以直接压到 36 个字节了，压得非常非常狠。然后接下来就是他这个，这东西就相当于跟这个东西比了，比完之后就发现他召回率都是甚至能达到90%，九点几嘛，就是召回率非常高，然后那个 plied 他速度就慢了十几倍，然后在正常的这种环境下也差了，就这个百分点的这个差距嘛，就是大概是这个，然后还有他后面文字上好像还说了跟那个MEMUVER，就是跟这个算法。比他就这个近似误差也比较大，就都比较严重，然后就是这几个就是延迟上面也都差得比较多，然后这个是正常的，这个就可以在这上面看啊。嗯，找着了，就是在这个召回率是相当于 at 10%，就是他我不保召回率，他就去跟这几个去比了一下，就是。
对， 40 的， 80 的，对，最多就80。两个版本是。
OK。
Again, I'm u, v, E, r. Easily.
得跟这种去比，不找，就是这张图相当于就是他。延迟误差比较多。对对对，他这个单独就是提了一下，相当于。
所以哦。所以他 recall 他找 TOP 100 的时候差很多，找 TOP 10 其实就和 cover 差不差，差的不是很多了。我看看 TOP10 投影它那个。开关也是全精度向量。开关是开关图投影，对，就是开关，你可以理解为这个，你可以理解为 GPU 上的 h n w。全精度。全精度索引，对。所以它就是图。 TOP100 差得多， TOP10 其实还好那。OK， TOP100 多是不是还是因为计算量有点高？就是然后。
对对对，是专门的，无所影对。
这个，因为他，对，大概是这。他要保召回的话，这个计算量太高了，然后一他不敢量化那么狠，就是效果上差得非常多，就召回 10% 的这种就可以接受，对于正常的算法来。
对，你看 pilot 它其实那个感觉找。跑 TOP10 的好像 10 的时候好像 recall 也稍微好一点。好像诶，也差不太多，也是9，大概是90%。
对，是好一些，但是他的延迟还是最高，就是他好像出来比较早，我看他那个论文里。
是他，我感觉他最该比的应该就是cugar，只不过是 cugar 没有做这种GPU， CPU 的卸载他是全都放 GPU 里边了。行。
然后接下来他就做了一个消融实验，就是相当。问题就是想要明确它到底是哪个模块做出现的收益，然后他就看了一下这个Max、IVF，那个 pregraph 的这个候选生成，就是我同等的覆盖率下，比如说就是 99% 的就覆盖率下，它这个系统就是选择性上就是生成延迟只有零点几个毫秒，然后你这个 fly 的快，然后他还验证了大概好像是2.5%。我记得就那个锚点的那个比例，它配置的比例还有五次迭代，就是。发现就是它这个最佳参数是啥？就是锚点设到百分之 2.5% 左右，就刚提 1% 到 5% 设的比例，大概是到这的时候性能上是比较好的。然后它这个建索引的速度比传统的 kmeans 要快 50 倍左右，它这样的话抽着建就是比 kmeans 也快，就建索引的时候，然后其次的话就相当于就是我这个代理就是过滤的这个时候，相当于就是它这个系统就是降低了这个覆盖的损失，然后这个，对，这个Ms。
Mar- Marco，就是它这个上面这个损失只有0.28%，然后 apply 的是这么多就远低于了。然后在效率上面的话，就是这个代理过滤，它端到端的延迟也都就是 1.9 到2.4，就是自身延迟大概比基线一个数量级大概等，对对对，是的。然后接下来就是这个访存跟算子加速，就是它对于每一步都去看一下，刚刚不是说这个搬运跟算分，就是从通。
这个图它也可以看出来，就是这个 response 跟我们常规的，比如说就是一些嗯，复杂的那个 copy 的那种表现，就是对于一些散乱的分布的这种文档的时候大概能高到特别散乱的时候能高到 50 多毫秒，当时是说然后就没有办法正常实时地搜索了，用了刚那个方法就压到 1 毫秒左右了，就是性能上就是比较厉害，然后在算子端的时候大概就是全精度，就是同台。
来比这个量化之后 PQ 大概要快 3 倍的，就是相当于就是用了它这个方法，不一定就是比那些压缩，就是 PQ 还快了，就是量化，因为在这个过程当中可能就是量化解压，就是刚刚那个红色那一段，就是那个延迟就是带来的 IO 还有一些计算上的负担，在这个 GPU 上就是比较，就是延迟的长尾，就是长尾大概就在这，然后它没有了这个就反而配合它这个东西就更快了一点。
它就，嗯放出来的它的这些资源上的开销，就是比如说你 top level 的那个paragraph，就虽然快，但是它大概有 152 这么多个点存占用，然后这个系统就 OM 崩溃能力，然后 BYTE 的话就是它通过压缩降，就是压非常狠，就压到了 16G 左右。然后这个 Webflow 的话，它就是有一些静态索引之类的，它就用 6.3 个 g 就能运行，就是真正我在搜的时候，然后就。
是实现了他这些效果，大概就是相当于这篇文章，就是把一些并行课上学的一些硬件上的优化，包括算子融合，什么统一的存储、直读这些都揉进去了，就是相当于这一个多向量检索，就目前比较全面的这么一个搭起来的一个端到端的这样一个系统，反正就是主要想要了解一下现在这个多项的检索，主要就我感觉我们这单项的检索向这个方向去迁移，感觉也有比较多可以做的点，所以大概就是想了解一下。这个方向。大概就到这。
OK，挺好的，挺有意思，然后他这个硬件也挺有意思。对，OK，行。今天可能谁不在？严松不在，严松做过他们前序的工作，叫workflow。 workflow 是做 filter 的，在 GPU 的，然后这个是他们后续的工作，我看人都没变，就还是那几个人，其实对， OK 行，挺好玩的。诶线上同学还有什么问题吗？大家，我们这边有啥问题吗？没了。行，OK，行，那我们就这样啊。好，我们谢谢可心。好，那我就结束了。
